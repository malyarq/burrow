---
phase: 04-delivery-cache-accounts-and-stats-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/services/cache/imageCacheService.ts
  - electron/ipc/handlers/cacheHandlers.ts
  - electron/preload/bridges/CacheBridge.ts
  - shared/contracts/cache.ts
  - src/services/ipc/cacheIPC.ts
  - src/components/ui/LazyImage.tsx
  - src/components/modpacks/ModpackBrowser.tsx
  - src/components/modpacks/AddModPage.tsx
  - src/components/settings/tabs/LauncherTab.tsx
  - electron/services/cache/__tests__/imageCacheService.test.ts
  - src/components/ui/__tests__/LazyImage.cache.test.tsx
autonomous: true
requirements:
  - FLOW-05
user_setup: []
must_haves:
  truths:
    - Modpack and mod icons are resolved through a persistent disk cache instead of relying only on remote `img` URLs or transient session cache.
    - Users can inspect and manage the image cache with size-aware cleanup or limit controls from an existing launcher settings surface.
    - Cached-image behavior fails safely to fallback visuals instead of breaking modpack and mod browsing when cache entries are missing or stale.
  artifacts:
    - electron/services/cache/imageCacheService.ts
    - shared/contracts/cache.ts
    - src/services/ipc/cacheIPC.ts
    - src/components/ui/LazyImage.tsx
    - src/components/settings/tabs/LauncherTab.tsx
    - electron/services/cache/__tests__/imageCacheService.test.ts
    - src/components/ui/__tests__/LazyImage.cache.test.tsx
  key_links:
    - Reuse the existing cache bridge and launcher settings surface instead of inventing a second cache-management UI path.
    - Borrow proven design ideas from `ContentManager` for size accounting and cleanup, but keep the remote image cache separate from mod-file content storage.
---

<objective>
Establish persistent image caching and cache controls for the launcher’s modpack and mod icon surfaces.

Purpose: satisfy the release requirement that imagery loads from a persistent disk cache with size management and cleanup controls, without rewriting the entire renderer image layer.
Output: an Electron-backed image-cache service, typed cache IPC for inspection and cleanup, and core modpack or mod surfaces routed through cached image resolution.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-delivery-cache-accounts-and-stats-hardening/04-RESEARCH.md
@.planning/phases/04-delivery-cache-accounts-and-stats-hardening/04-VALIDATION.md
@shared/contracts/cache.ts
@src/services/ipc/cacheIPC.ts
@electron/ipc/handlers/cacheHandlers.ts
@electron/preload/bridges/CacheBridge.ts
@src/components/ui/LazyImage.tsx
@src/components/modpacks/ModpackBrowser.tsx
@src/components/modpacks/AddModPage.tsx
@src/components/settings/tabs/LauncherTab.tsx
@electron/services/content/contentManager.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a disk-backed image cache service and typed cache contract</name>
  <files>electron/services/cache/imageCacheService.ts, electron/ipc/handlers/cacheHandlers.ts, electron/preload/bridges/CacheBridge.ts, shared/contracts/cache.ts, src/services/ipc/cacheIPC.ts, electron/services/cache/__tests__/imageCacheService.test.ts</files>
  <action>Create an Electron-side cache service for remote images stored under user data, with deterministic keying, size stats, cleanup, and configurable limit support. Extend the existing cache IPC surface so the renderer can query cache state and trigger safe cleanup without conflating image cache with unrelated launcher cache behavior. Cover the service with focused tests for persistence, size accounting, and cleanup semantics.</action>
  <verify>npx vitest run electron/services/cache/__tests__/imageCacheService.test.ts</verify>
  <done>FMCL has a testable persistent image-cache backend and typed IPC contract rather than only a broad "clear cache" button.</done>
</task>

<task type="auto">
  <name>Task 2: Route core modpack and mod icon surfaces through cached image loading and expose user controls</name>
  <files>src/components/ui/LazyImage.tsx, src/components/modpacks/ModpackBrowser.tsx, src/components/modpacks/AddModPage.tsx, src/components/settings/tabs/LauncherTab.tsx, src/components/ui/__tests__/LazyImage.cache.test.tsx</files>
  <action>Layer cached remote-image resolution under `LazyImage` or a thin wrapper around it, then migrate the release-critical modpack and mod icon surfaces onto that seam. Add user-visible cache stats, size-limit, and cleanup controls to the existing launcher settings area instead of creating a new standalone cache page. Keep the rollout scoped to the main modpack and mod browsing flows, not every image in the app.</action>
  <verify>npx vitest run src/components/ui/__tests__/LazyImage.cache.test.tsx && npx tsc --noEmit</verify>
  <done>Core icon surfaces now benefit from persistent disk caching with usable launcher controls instead of only in-memory or browser-session behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run electron/services/cache/__tests__/imageCacheService.test.ts`
- [ ] `npx vitest run src/components/ui/__tests__/LazyImage.cache.test.tsx`
- [ ] `npx eslint electron/ipc/handlers/cacheHandlers.ts electron/preload/bridges/CacheBridge.ts shared/contracts/cache.ts src/services/ipc/cacheIPC.ts src/components/ui/LazyImage.tsx src/components/modpacks/ModpackBrowser.tsx src/components/modpacks/AddModPage.tsx src/components/settings/tabs/LauncherTab.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- FMCL persists modpack and mod imagery in a managed disk cache with visible controls instead of treating icon loading as a best-effort renderer concern
</success_criteria>

<output>
After completion, create `.planning/phases/04-delivery-cache-accounts-and-stats-hardening/04-01-SUMMARY.md`
</output>
