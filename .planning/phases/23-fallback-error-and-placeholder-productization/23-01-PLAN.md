---
phase: 23-fallback-error-and-placeholder-productization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/DegradedStateView.tsx
  - src/components/error/FatalErrorView.tsx
  - src/utils/displayError.ts
  - src/utils/safeUiText.ts
  - src/contexts/settings/i18n.ts
  - src/locales/en.json
  - src/locales/ru.json
  - src/components/layout/__tests__/EmptyStateView.branding.test.tsx
  - src/components/layout/__tests__/DegradedStateView.test.tsx
  - src/utils/__tests__/displayError.test.ts
autonomous: true
requirements:
  - FALL-01
  - FALL-02
  - FALL-03
user_setup: []
must_haves:
  truths:
    - Phase 23 needs a new calm degraded-state seam; it must not reuse the existing brand-owned `EmptyStateView` hero treatment as the fallback contract for errors or empty states.
    - Suspicious placeholder values, raw IPC prefixes, and mixed-language degraded copy must be sanitized or mapped before phase-owned surfaces render them.
    - The outer crash boundary needs a language-aware copy path that still works outside `SettingsProvider`, but this plan should only create the seam, not finish crash adoption.
    - This plan owns shared degraded-state primitives and sanitization rules only; it must not reopen Phase 20 branding or absorb route-specific async-state adoption.
  artifacts:
    - src/components/layout/DegradedStateView.tsx
    - src/components/error/FatalErrorView.tsx
    - src/utils/displayError.ts
    - src/utils/safeUiText.ts
    - src/contexts/settings/i18n.ts
    - src/locales/en.json
    - src/locales/ru.json
    - src/components/layout/__tests__/DegradedStateView.test.tsx
    - src/utils/__tests__/displayError.test.ts
  key_links:
    - Research confirmed that `EmptyStateView.tsx` is intentionally logo-forward and already protected by `EmptyStateView.branding.test.tsx`, so Phase 23 needs a different primitive for calm degraded states.
    - The screenshot audit and code scan identified raw `${file.jarVersion}`, mixed-language `Changelog` placeholder copy, and `[namespace] method failed` wrappers as repeatable user-facing leakage seams.
    - Phase 23 route work depends on one reusable contract for empty, zero-result, unavailable, and inline error states; otherwise the phase will collapse into per-route wording tweaks.
---

<objective>
Define the shared degraded-state and sanitization contract for Phase 23.

Purpose: give the phase one reusable primitive for calm empty, unavailable, and inline error states before route adoption starts.
Output: a product-owned degraded-state component family, user-safe error and placeholder sanitization helpers, and localized copy that later plans can reuse.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-fallback-error-and-placeholder-productization/23-CONTEXT.md
@.planning/phases/23-fallback-error-and-placeholder-productization/23-RESEARCH.md
@.planning/phases/23-fallback-error-and-placeholder-productization/23-VALIDATION.md
@src/components/layout/EmptyStateView.tsx
@src/components/ErrorBoundary.tsx
@src/services/ipc/ipcError.ts
@src/contexts/settings/i18n.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a calm degraded-state primitive and shared display-sanitization helpers</name>
  <files>src/components/layout/DegradedStateView.tsx, src/components/error/FatalErrorView.tsx, src/utils/displayError.ts, src/utils/safeUiText.ts, src/contexts/settings/i18n.ts, src/locales/en.json, src/locales/ru.json</files>
  <action>Introduce a new shared degraded-state component family for empty, zero-result, unavailable, and inline failure surfaces without repurposing the existing brand-heavy empty-state hero. Add small helpers that map technical IPC wrapper text, suspicious template placeholders, and mixed-language fallback copy into product-safe display text, and expose whatever minimal i18n helper the outer crash boundary will need when it renders outside `SettingsProvider`.</action>
  <verify>git diff --check -- src/components/layout/DegradedStateView.tsx src/components/error/FatalErrorView.tsx src/utils/displayError.ts src/utils/safeUiText.ts src/contexts/settings/i18n.ts src/locales/en.json src/locales/ru.json && npx eslint src/components/layout/DegradedStateView.tsx src/components/error/FatalErrorView.tsx src/utils/displayError.ts src/utils/safeUiText.ts src/contexts/settings/i18n.ts && npx tsc --noEmit</verify>
  <done>Phase 23 has one reusable degraded-state seam plus shared sanitization rules for user-visible technical strings and suspicious placeholders.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared regression coverage for degraded-state variants and sanitization rules</name>
  <files>src/components/layout/__tests__/EmptyStateView.branding.test.tsx, src/components/layout/__tests__/DegradedStateView.test.tsx, src/utils/__tests__/displayError.test.ts</files>
  <action>Protect the new degraded-state contract with focused tests that prove empty, zero-result, unavailable, and inline-error variants stay distinct, the old `EmptyStateView` brand contract does not get silently repurposed, and suspicious placeholder or technical wrapper strings are sanitized before display.</action>
  <verify>npx vitest run src/components/layout/__tests__/EmptyStateView.branding.test.tsx src/components/layout/__tests__/DegradedStateView.test.tsx src/utils/__tests__/displayError.test.ts && npx tsc --noEmit</verify>
  <done>The shared degraded-state and sanitization seam is regression-protected before route adoption begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/layout/__tests__/EmptyStateView.branding.test.tsx src/components/layout/__tests__/DegradedStateView.test.tsx src/utils/__tests__/displayError.test.ts`
- [ ] `git diff --check -- src/components/layout/DegradedStateView.tsx src/components/error/FatalErrorView.tsx src/utils/displayError.ts src/utils/safeUiText.ts src/contexts/settings/i18n.ts src/locales/en.json src/locales/ru.json`
- [ ] `npx eslint src/components/layout/DegradedStateView.tsx src/components/error/FatalErrorView.tsx src/utils/displayError.ts src/utils/safeUiText.ts src/contexts/settings/i18n.ts`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 23 owns a calm degraded-state contract and user-safe display helpers without reopening brand hero work
</success_criteria>

<output>
After completion, create `.planning/phases/23-fallback-error-and-placeholder-productization/23-01-SUMMARY.md`
</output>
