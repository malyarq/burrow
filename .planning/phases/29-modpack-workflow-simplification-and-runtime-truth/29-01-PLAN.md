---
phase: 29-modpack-workflow-simplification-and-runtime-truth
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/modpacks/ModpackCatalogControls.tsx
  - src/components/modpacks/ModpackList.tsx
  - src/components/modpacks/ModpackBrowser.tsx
  - src/components/modpacks/__tests__/ModpackCatalog.controls.test.tsx
  - src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx
  - src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx
  - src/components/modpacks/__tests__/ModpackCatalog.density.test.tsx
autonomous: true
requirements:
  - MODPACK-01
  - MODPACK-02
user_setup: []
must_haves:
  truths:
    - Installed and remote catalog surfaces must read as one compact search-and-filter system instead of two unrelated compositions with stacked filter cards.
    - Search stays primary, and reset belongs beside the active controls rather than in a detached summary strip or heavyweight status panel.
    - Modpack cards should keep only the minimum scan-first summary data before the user opens details; dense technical metadata belongs elsewhere.
    - Browser-only concerns such as history, pagination, import, and provider state may remain local, but they must wrap the same controls shell rather than creating a second layout language.
  artifacts:
    - src/components/modpacks/ModpackCatalogControls.tsx
    - src/components/modpacks/ModpackList.tsx
    - src/components/modpacks/ModpackBrowser.tsx
    - src/components/modpacks/__tests__/ModpackCatalog.controls.test.tsx
    - src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx
    - src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx
    - src/components/modpacks/__tests__/ModpackCatalog.density.test.tsx
  key_links:
    - `29-CONTEXT.md` locks one compact horizontal composition and explicitly removes loud summary blocks such as `Modpacks`, `Active`, and `Results`.
    - `29-RESEARCH.md` identifies `ModpackList.tsx` and `ModpackBrowser.tsx` as duplicated seams where composition and density drift together.
    - `29-VALIDATION.md` requires a new Wave 0 controls test before the shared shell can be trusted across installed and remote surfaces.
---

<objective>
Collapse installed and remote modpack catalog scanning into one compact controls language.

Purpose: make browse and list views feel like one coherent workflow by sharing a compact controls shell and cutting card noise down to only the high-value summary data that supports scanning.
Output: installed and remote modpack surfaces reuse one compact controls seam, cards stop front-loading secondary metadata, and regression coverage locks the layout contract.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-modpack-workflow-simplification-and-runtime-truth/29-CONTEXT.md
@.planning/phases/29-modpack-workflow-simplification-and-runtime-truth/29-RESEARCH.md
@.planning/phases/29-modpack-workflow-simplification-and-runtime-truth/29-VALIDATION.md
@src/components/modpacks/ModpackList.tsx
@src/components/modpacks/ModpackBrowser.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the shared compact catalog-controls shell and its Wave 0 seam</name>
  <files>src/components/modpacks/ModpackCatalogControls.tsx, src/components/modpacks/ModpackList.tsx, src/components/modpacks/ModpackBrowser.tsx, src/components/modpacks/__tests__/ModpackCatalog.controls.test.tsx, src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx, src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx</files>
  <action>Create the shared presentational shell that both installed and remote catalog surfaces use for search, filters, sort, and reset. Browser-only controls may stay in wrappers, but the primary controls row must no longer fork into separate visual systems. Land the missing Wave 0 test that proves the installed and remote surfaces consume the same compact composition contract.</action>
  <verify>npx vitest run src/components/modpacks/__tests__/ModpackCatalog.controls.test.tsx src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx && npx tsc --noEmit</verify>
  <done>The catalog controls shell is shared, compact, and regression-protected across installed and remote modpack flows.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce card summary noise and lock dense scanability</name>
  <files>src/components/modpacks/ModpackCatalogControls.tsx, src/components/modpacks/ModpackList.tsx, src/components/modpacks/ModpackBrowser.tsx, src/components/modpacks/__tests__/ModpackCatalog.density.test.tsx, src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx, src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx</files>
  <action>Trim installed and remote modpack cards down to the minimum scan-first summary surface: Minecraft version plus updated timestamp. Remove or demote standalone top-of-list summary weight so the catalog reads as tools plus results rather than dashboard panels above the actual work.</action>
  <verify>npx vitest run src/components/modpacks/__tests__/ModpackCatalog.density.test.tsx src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx && npx eslint src/components/modpacks/ModpackCatalogControls.tsx src/components/modpacks/ModpackList.tsx src/components/modpacks/ModpackBrowser.tsx && npx tsc --noEmit</verify>
  <done>Catalog cards become visibly calmer and faster to scan without regressing installed or remote catalog interactions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/modpacks/__tests__/ModpackCatalog.controls.test.tsx src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx src/components/modpacks/__tests__/ModpackCatalog.density.test.tsx`
- [ ] `npx eslint src/components/modpacks/ModpackCatalogControls.tsx src/components/modpacks/ModpackList.tsx src/components/modpacks/ModpackBrowser.tsx`
- [ ] `npx tsc --noEmit`
- [ ] Manual desktop walkthrough completed: installed list and browser read as one compact controls family, no clipped or vertically stacked primary controls remain, and cards only expose high-value summary information
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Installed and remote modpack catalog surfaces share one compact controls contract and visually quiet cards
</success_criteria>

<output>
After completion, create `.planning/phases/29-modpack-workflow-simplification-and-runtime-truth/29-01-SUMMARY.md`
</output>
