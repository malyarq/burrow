---
phase: 31-guided-content-browsers-and-capability-expansion
plan: "02"
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - src/components/modpacks/AddModPage.tsx
  - electron/services/mods/platform/modPlatformService.ts
  - electron/services/mods/platform/types.ts
  - electron/services/modpacks/modpackService.ts
  - electron/services/shaders/shaderService.ts
  - src/components/modpacks/__tests__/AddModFlow.async-recovery.test.tsx
  - src/features/modpacks/hooks/useModpackRuntimeSummary.ts
  - src/features/modpacks/__tests__/runtimeSummary.truth.test.ts
  - src/features/modpacks/__tests__/contentManifestTruth.test.ts
autonomous: true
requirements:
  - CONTENT-02
  - CONTENT-05
user_setup: []
must_haves:
  truths:
    - Installing a resource pack or shader from the guided flow must not silently register that content as a required mod dependency in `manifest.files`; users should not inherit fake runtime requirements because FMCL reused a mod-only finalize path.
    - The guided browser must only call manifest-updating mod finalization for real mods; resource packs and shaders stay installed at the instance level without pretending to be manifest-managed dependencies.
    - Shader guidance in later waves must be able to start from config-first runtime truth rather than active-file presence, archive names, or provider metadata pretending to guarantee support.
    - Phase 31 stays bounded here: non-mod content remains instance-scoped secondary content rather than drifting into cross-pack library or marketplace semantics.
  artifacts:
    - src/components/modpacks/AddModPage.tsx
    - electron/services/mods/platform/modPlatformService.ts
    - electron/services/mods/platform/types.ts
    - electron/services/modpacks/modpackService.ts
    - electron/services/shaders/shaderService.ts
    - src/components/modpacks/__tests__/AddModFlow.async-recovery.test.tsx
    - src/features/modpacks/hooks/useModpackRuntimeSummary.ts
    - src/features/modpacks/__tests__/runtimeSummary.truth.test.ts
    - src/features/modpacks/__tests__/contentManifestTruth.test.ts
  key_links:
    - `31-RESEARCH.md` calls out non-mod manifest pollution and shader runtime blindness as separate blockers that must be solved before UI guidance is trustworthy.
    - `31-01-PLAN.md` owns the shared IPC surface for Phase 31, so this follow-up plan stays on internal finalize/runtime groundwork.
    - `31-VALIDATION.md` treats runtime summary truth and manifest truth as the Wave 2 gate before canonical guided entry starts.
    - `src/components/modpacks/AddModPage.tsx` is the real guided finalize seam today: it decides whether a completed install also invokes `modpacks:addMod`, so non-mod manifest pollution cannot be fixed without touching that route.
    - Shared contracts, `windowApi`, preload bridges, and renderer IPC wrappers remain intentionally out of scope here; this plan only succeeds if the non-mod truth work lands through internal finalize and runtime seams.
---

<objective>
Finalize non-mod install truth and shader-runtime groundwork before the guided browser becomes canonical.

Purpose: prevent guided resource-pack and shader downloads from behaving like hidden mods, and expose the minimum runtime truth needed for later shader capability guidance.
Output: non-mod finalization paths that stay instance-scoped and shader-runtime summary inputs grounded in config-first truth.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-guided-content-browsers-and-capability-expansion/31-RESEARCH.md
@.planning/phases/31-guided-content-browsers-and-capability-expansion/31-VALIDATION.md
@src/components/modpacks/AddModPage.tsx
@electron/services/mods/platform/modPlatformService.ts
@electron/services/mods/platform/types.ts
@electron/services/modpacks/modpackService.ts
@electron/services/shaders/shaderService.ts
@src/components/modpacks/__tests__/AddModFlow.async-recovery.test.tsx
@src/features/modpacks/hooks/useModpackRuntimeSummary.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove accidental mod-manifest coupling and expose shader capability groundwork from runtime truth</name>
  <files>src/components/modpacks/AddModPage.tsx, electron/services/mods/platform/modPlatformService.ts, electron/services/mods/platform/types.ts, electron/services/modpacks/modpackService.ts, electron/services/shaders/shaderService.ts, src/components/modpacks/__tests__/AddModFlow.async-recovery.test.tsx, src/features/modpacks/hooks/useModpackRuntimeSummary.ts, src/features/modpacks/__tests__/runtimeSummary.truth.test.ts, src/features/modpacks/__tests__/contentManifestTruth.test.ts</files>
  <action>Refactor the guided non-mod finalize seam so resource packs and shaders no longer land in `manifest.files` by default or depend on mod-only bookkeeping. Use `src/components/modpacks/AddModPage.tsx` to gate manifest-updating `modpacks:addMod` calls to real mods, preserve instance-scoped installation truth through the existing install path, normalize active-shader runtime state, and expose the minimum shader-capability inputs needed for later UI guidance by anchoring them in config-first runtime state instead of archive presence alone, without reopening shared contracts, preload bridges, or renderer IPC wrappers.</action>
  <verify><automated>npx vitest run src/components/modpacks/__tests__/AddModFlow.async-recovery.test.tsx src/features/modpacks/__tests__/runtimeSummary.truth.test.ts src/features/modpacks/__tests__/contentManifestTruth.test.ts && npx tsc --noEmit</automated></verify>
  <done>Resource packs and shaders stop behaving like hidden mod dependencies, and later shader guidance can build on honest runtime truth.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/modpacks/__tests__/AddModFlow.async-recovery.test.tsx src/features/modpacks/__tests__/runtimeSummary.truth.test.ts src/features/modpacks/__tests__/contentManifestTruth.test.ts`
- [ ] `npx tsc --noEmit`
- [ ] Manual sanity pass completed: a guided non-mod install does not create a fake manifest dependency, runtime summary inputs remain config-first, and no shared Phase 31 IPC surface had to be reopened here
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Wave 2 finishes with honest non-mod content truth and shader groundwork rather than mod-path carryover
</success_criteria>

<output>
After completion, create `.planning/phases/31-guided-content-browsers-and-capability-expansion/31-02-SUMMARY.md`
</output>
