---
phase: 13-launch-trust-and-modpack-workflow-ergonomics
plan: "04"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - src/components/modpacks/ModpackList.tsx
  - src/locales/en.json
  - src/locales/ru.json
  - src/components/modpacks/__tests__/ModpackList.quick-actions.test.tsx
  - src/components/modpacks/__tests__/ModpackList.actions.test.tsx
  - src/components/modpacks/__tests__/ModpackList.keyboard.test.tsx
autonomous: true
requirements:
  - MPUX-03
user_setup: []
must_haves:
  truths:
    - Installed modpack cards must prioritize the next likely user action instead of forcing an extra details hop or burying the common path under a generic select CTA.
    - This plan reuses the Phase 11 anchored-overlay contract; it must not reopen menu geometry or viewport-placement work that is already complete.
    - Quick actions must stay stable under keyboard and pointer usage and must not depend on accidental focus behavior or hidden context about the currently selected pack.
    - The work stays within installed-card ergonomics and action hierarchy; it must not drift into a full local-library redesign.
  artifacts:
    - src/components/modpacks/ModpackList.tsx
    - src/locales/en.json
    - src/locales/ru.json
  key_links:
    - Phase 11 already fixed the anchored overlay geometry seam, so this plan should focus on action priority and quick-flow ergonomics.
    - Phase 13 research separated remote browser work from local-card work specifically to avoid mixing scan ergonomics with installed-pack action hierarchy.
---

<objective>
Make installed modpack cards faster to act on and less awkward in everyday use.

Purpose: promote the common local-pack actions users actually want, preserve stable menus, and make keyboard or pointer flows feel intentional instead of indirect.
Output: improved installed-card action hierarchy, lighter quick flows, and focused regression coverage for the local-card seam.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-launch-trust-and-modpack-workflow-ergonomics/13-RESEARCH.md
@.planning/phases/13-launch-trust-and-modpack-workflow-ergonomics/13-VALIDATION.md
@src/components/modpacks/ModpackList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebalance installed-card action hierarchy around quick flows</name>
  <files>src/components/modpacks/ModpackList.tsx, src/locales/en.json, src/locales/ru.json</files>
  <action>Refine the installed modpack cards so the common next action is obvious and reachable without detouring through details by default. Improve button hierarchy, selection or quick-open behavior, and menu contents as needed to reduce friction while keeping the existing selection and overlay foundations intact. Do not reopen overlay placement work; reuse the existing anchored menu seam and focus on action priority and workflow clarity.</action>
  <verify>npx eslint src/components/modpacks/ModpackList.tsx && npx tsc --noEmit</verify>
  <done>Installed modpack cards now reflect the likely next action instead of over-prioritizing a generic select step.</done>
</task>

<task type="auto">
  <name>Task 2: Lock installed-card quick actions with focused tests</name>
  <files>src/components/modpacks/__tests__/ModpackList.quick-actions.test.tsx, src/components/modpacks/__tests__/ModpackList.actions.test.tsx, src/components/modpacks/__tests__/ModpackList.keyboard.test.tsx</files>
  <action>Add or extend focused tests that prove the updated quick actions, menu hierarchy, and keyboard flows remain stable for installed modpacks. Reuse the existing actions or keyboard seams where possible, and keep the test scope on local-card behavior rather than whole-app route walkthroughs.</action>
  <verify>npx vitest run src/components/modpacks/__tests__/ModpackList.quick-actions.test.tsx src/components/modpacks/__tests__/ModpackList.actions.test.tsx src/components/modpacks/__tests__/ModpackList.keyboard.test.tsx</verify>
  <done>The local-card ergonomics improvements are regression-protected and no longer depend only on manual memory of the intended flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/modpacks/__tests__/ModpackList.quick-actions.test.tsx src/components/modpacks/__tests__/ModpackList.actions.test.tsx src/components/modpacks/__tests__/ModpackList.keyboard.test.tsx`
- [ ] `npx eslint src/components/modpacks/ModpackList.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Installed modpack cards expose stable, lower-friction quick actions without regressing existing overlay safety
</success_criteria>

<output>
After completion, create `.planning/phases/13-launch-trust-and-modpack-workflow-ergonomics/13-04-SUMMARY.md`
</output>
