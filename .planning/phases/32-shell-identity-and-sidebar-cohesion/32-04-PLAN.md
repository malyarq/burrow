---
phase: 32-shell-identity-and-sidebar-cohesion
plan: "04"
type: execute
wave: 2
depends_on:
  - 32-01
  - 32-02
  - 32-03
files_modified:
  - src/verification/manual/scenarios.tsx
  - src/verification/manual/views.ts
  - src/verification/manual/__tests__/appearanceProof.test.tsx
  - src/verification/manual/__tests__/views.test.ts
autonomous: true
requirements:
  - SHELL-09
  - SHELL-10
  - BRAND-01
user_setup: []
must_haves:
  truths:
    - The manual proof harness is part of the product-truth system because stale proof copy already helped hide open UX regressions in the previous milestone.
    - Phase 32 proof routes must talk about readable sidebar identity, native macOS shell behavior, and calm fallback surfaces instead of older brand-reset language.
    - This plan should reuse the existing verification/manual harness instead of inventing another proof layer.
    - The plan must not mask missing product work behind proof-only copy changes; it depends on the earlier execution plans being complete first.
  artifacts:
    - src/verification/manual/scenarios.tsx
    - src/verification/manual/views.ts
    - src/verification/manual/__tests__/appearanceProof.test.tsx
    - src/verification/manual/__tests__/views.test.ts
  key_links:
    - `32-RESEARCH.md` identifies stale manual-proof wording as one reason earlier closeouts could read green while the live shell still felt wrong.
    - `32-VALIDATION.md` explicitly requires updated manual readiness text and view descriptions before Phase 32 can close.
    - The earlier milestone closeout gap makes this plan a product safeguard, not optional documentation polish.
---

<objective>
Refresh the manual proof harness to the direct-feedback shell contract.

Purpose: ensure future closeout and walkthrough work verify the Phase 32 shell/sidebar/fallback criteria instead of stale milestone wording.
Output: manual verification routes, descriptions, and readiness tests that reflect the new shell contract and cannot pass on outdated copy alone.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ru/product-feedback-2026-04-20.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-shell-identity-and-sidebar-cohesion/32-RESEARCH.md
@.planning/phases/32-shell-identity-and-sidebar-cohesion/32-VALIDATION.md
@src/verification/manual/scenarios.tsx
@src/verification/manual/views.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Phase 32 manual view descriptions and readiness signals around the new shell contract</name>
  <files>src/verification/manual/scenarios.tsx, src/verification/manual/views.ts</files>
  <action>Update the Phase 32-relevant manual routes so their descriptions and readiness text explicitly reference readable sidebar identity, native macOS shell behavior, and calm fallback surfaces. Remove dependence on stale milestone-specific brand-reset wording or `FriendLauncher` text alone as proof of correctness.</action>
  <verify>npx eslint src/verification/manual/scenarios.tsx src/verification/manual/views.ts && npx tsc --noEmit</verify>
  <done>The manual proof hub now describes and signals the real shell criteria Phase 32 owns.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the refreshed proof harness with narrow regression tests</name>
  <files>src/verification/manual/__tests__/appearanceProof.test.tsx, src/verification/manual/__tests__/views.test.ts</files>
  <action>Update the manual proof tests so they fail when the app falls back to stale shell-brand wording or outdated proof descriptions. Keep the assertions narrow and product-facing so future closeouts cannot silently reuse old review criteria.</action>
  <verify>npx vitest run src/verification/manual/__tests__/appearanceProof.test.tsx src/verification/manual/__tests__/views.test.ts && npx tsc --noEmit</verify>
  <done>The manual verification harness is regression-protected against stale shell-proof language.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/verification/manual/__tests__/appearanceProof.test.tsx src/verification/manual/__tests__/views.test.ts`
- [ ] `npx eslint src/verification/manual/scenarios.tsx src/verification/manual/views.ts`
- [ ] `npx tsc --noEmit`
- [ ] Manual proof hub review completed: the Phase 32-owned routes clearly communicate sidebar readability, native macOS shell, and calm fallback expectations without reusing older shell-brand reset copy
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 32 closeout will depend on current product criteria instead of stale proof-route wording
</success_criteria>

<output>
After completion, create `.planning/phases/32-shell-identity-and-sidebar-cohesion/32-04-SUMMARY.md`
</output>
