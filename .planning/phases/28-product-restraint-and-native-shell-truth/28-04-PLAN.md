---
phase: 28-product-restraint-and-native-shell-truth
plan: "04"
type: execute
wave: 4
depends_on:
  - 28-01
  - 28-02
  - 28-03
files_modified:
  - src/contexts/ModpackContext.tsx
  - src/contexts/instances/hooks/useInstanceBootstrap.ts
  - src/contexts/instances/services/instancesService.ts
  - src/contexts/instances/services/legacySeed.ts
  - src/features/launch/hooks/useLaunchState.ts
  - src/App.tsx
  - electron/services/instances/indexStore.ts
  - electron/services/instances/configStore.ts
  - electron/services/instances/instanceService.ts
  - src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx
  - src/contexts/__tests__/ModpackContext.startup-truth.test.ts
  - electron/services/instances/__tests__/instanceMetadataCrud.test.ts
  - electron/services/instances/__tests__/legacySeed.startupTruth.test.ts
autonomous: true
requirements:
  - SHELL-08
user_setup: []
must_haves:
  truths:
    - Persisted truth already exists. The fix must hydrate and respect that truth instead of inventing another settings or runtime store.
    - Startup surfaces must not claim `1.12.2` or `vanilla` just because persisted config has not hydrated yet, and legacy migration must not keep reintroducing the same lie on older installs.
    - Keep Phase 28 tightly bounded to reopened shell truth: do only the minimum metadata-sync fallout needed for visible startup correctness, not the broader Phase 29 runtime-authority unification.
    - This final plan also owns the phase closeout gate: after startup truth lands, the targeted shell-truth suite and real quit/relaunch walkthrough must pass before Phase 28 is considered complete.
  artifacts:
    - src/contexts/ModpackContext.tsx
    - src/contexts/instances/hooks/useInstanceBootstrap.ts
    - src/contexts/instances/services/instancesService.ts
    - src/contexts/instances/services/legacySeed.ts
    - src/features/launch/hooks/useLaunchState.ts
    - src/App.tsx
    - electron/services/instances/indexStore.ts
    - electron/services/instances/configStore.ts
    - electron/services/instances/instanceService.ts
    - src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx
    - src/contexts/__tests__/ModpackContext.startup-truth.test.ts
    - electron/services/instances/__tests__/instanceMetadataCrud.test.ts
    - electron/services/instances/__tests__/legacySeed.startupTruth.test.ts
  key_links:
    - `28-RESEARCH.md` traces the reopen lie to startup fallbacks in `useLaunchState.ts`, `App.tsx`, and the legacy seed or store paths rather than to missing persistence.
    - `28-VALIDATION.md` requires new startup-truth and legacy-migration coverage plus a real quit-and-relaunch walkthrough, so Phase 28 cannot close on unit tests alone.
---

<objective>
Restore truthful selected profile and runtime state on reopen.

Purpose: stop shell surfaces from showing stale default runtime or selected-pack values during startup, and close Phase 28 on one focused shell-truth verification gate.
Output: startup hydration that reflects persisted truth before the shell claims a value, plus a green targeted suite and real reopen walkthrough.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-product-restraint-and-native-shell-truth/28-RESEARCH.md
@.planning/phases/28-product-restraint-and-native-shell-truth/28-VALIDATION.md
@src/contexts/ModpackContext.tsx
@src/contexts/instances/hooks/useInstanceBootstrap.ts
@src/contexts/instances/services/instancesService.ts
@src/contexts/instances/services/legacySeed.ts
@src/features/launch/hooks/useLaunchState.ts
@src/App.tsx
@electron/services/instances/indexStore.ts
@electron/services/instances/configStore.ts
@electron/services/instances/instanceService.ts
@electron/services/instances/__tests__/instanceMetadataCrud.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the Wave 0 startup-truth seam for visible shell hydration</name>
  <files>src/contexts/ModpackContext.tsx, src/contexts/instances/hooks/useInstanceBootstrap.ts, src/features/launch/hooks/useLaunchState.ts, src/App.tsx, src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx, src/contexts/__tests__/ModpackContext.startup-truth.test.ts</files>
  <action>Land the missing Wave 0 startup-truth seam for provider hydration and top-level shell labels, then use it to stop visible `1.12.2` or `vanilla` defaults from leaking before persisted selection or classic runtime truth is known. Keep this task focused on the shell-visible hydration path rather than on legacy migration or deeper persistence cleanup.</action>
  <verify>npx vitest run src/contexts/__tests__/ModpackContext.startup-truth.test.ts src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx && npx tsc --noEmit</verify>
  <done>The visible startup shell path is regression-protected against hydration-time default lies before later persistence work depends on it.</done>
</task>

<task type="auto">
  <name>Task 2: Lock persisted reload and legacy migration truth behind focused storage seams</name>
  <files>src/contexts/instances/services/instancesService.ts, src/contexts/instances/services/legacySeed.ts, electron/services/instances/indexStore.ts, electron/services/instances/configStore.ts, electron/services/instances/instanceService.ts, electron/services/instances/__tests__/instanceMetadataCrud.test.ts, electron/services/instances/__tests__/legacySeed.startupTruth.test.ts</files>
  <action>Harden the persistence-side half of `SHELL-08`: make store reload and legacy migration preserve the same selected pack, version, and loader truth the visible shell now waits for, and prevent older installs from silently reintroducing hardcoded `1.12.2` or `vanilla` fallbacks when better persisted truth exists or can be derived.</action>
  <verify>npx vitest run electron/services/instances/__tests__/instanceMetadataCrud.test.ts electron/services/instances/__tests__/legacySeed.startupTruth.test.ts && npx eslint src/contexts/instances/services/instancesService.ts src/contexts/instances/services/legacySeed.ts electron/services/instances/indexStore.ts electron/services/instances/configStore.ts electron/services/instances/instanceService.ts && npx tsc --noEmit</verify>
  <done>The persistence and migration seams no longer recreate stale startup defaults after reload or upgrade.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run electron/window/__tests__/windowManager.macos.test.ts src/components/__tests__/TitleBar.branding.test.tsx src/components/__tests__/UpdateNotification.layout.test.tsx src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx src/components/__tests__/SimplePlayHome.visualTruth.test.tsx src/components/sidebar/__tests__/SidebarHeader.compact-mode.test.tsx src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx src/components/modpacks/__tests__/ModpackDetails.actions.test.tsx src/components/modpacks/__tests__/ModpackUpdateModal.degraded-state.test.tsx src/components/settings/__tests__/AppearanceTab.branding.test.tsx src/contexts/__tests__/ModpackContext.startup-truth.test.ts electron/services/instances/__tests__/instanceMetadataCrud.test.ts electron/services/instances/__tests__/legacySeed.startupTruth.test.ts`
- [ ] `npx tsc --noEmit`
- [ ] `npx eslint src/ electron/`
- [ ] Real-app macOS shell checklist completed from Plan `28-01`
- [ ] Manual shell-restraint checklist completed from Plan `28-02`
- [ ] Manual modpack update-locality checklist completed from Plan `28-03`
- [ ] Real quit-and-relaunch walkthrough completed: switch to Classic mode, choose a non-default version and non-vanilla loader, select a non-default installed modpack, fully quit, relaunch, and confirm shell mode, selected pack, version, and loader all match persisted truth before corrective interaction
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Reopened shell state reflects the user’s actual persisted profile and runtime instead of stale default values, and the whole Phase 28 shell-truth matrix closes green
</success_criteria>

<output>
After completion, create `.planning/phases/28-product-restraint-and-native-shell-truth/28-04-SUMMARY.md`
</output>
