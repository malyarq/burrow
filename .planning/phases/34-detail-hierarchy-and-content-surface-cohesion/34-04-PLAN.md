---
phase: 34-detail-hierarchy-and-content-surface-cohesion
plan: "04"
type: execute
wave: 4
depends_on:
  - 34-01
  - 34-02
  - 34-03
files_modified:
  - src/verification/manual/views.ts
  - src/verification/manual/scenarios.tsx
  - src/verification/manual/__tests__/views.test.ts
autonomous: true
requirements:
  - MODPACK-11
  - MODPACK-12
  - CONTENT-07
user_setup: []
must_haves:
  truths:
    - Manual proof routes tell reviewers to check tab reachability on the real details surface.
    - Manual proof routes tell reviewers to check first-read runtime and dependency authority, not only settings truth.
    - Manual proof routes explicitly compare Mods, Resource Packs, Shaders, Worlds, and Screenshots as one workspace.
    - Retained legacy density views remain useful only as regression routes, not as the current success criteria.
  artifacts:
    - src/verification/manual/views.ts
    - src/verification/manual/scenarios.tsx
    - src/verification/manual/__tests__/views.test.ts
  key_links:
    - `src/verification/manual/views.ts` -> `src/verification/manual/scenarios.tsx` via route metadata and rendered proof surfaces
    - `src/verification/manual/__tests__/views.test.ts` -> `src/verification/manual/views.ts` via required description assertions
    - `modpack-details` view -> `phase-21-details-density` view via refreshed direct-feedback wording
    - `phase-21-secondary-density` view -> content tab scenarios via shared-workspace review instructions
---

<objective>
Refresh the manual proof harness to the direct-feedback details and content cohesion contract.

Purpose: make future walkthroughs review the actual Phase 34 closure criteria instead of the earlier density-only story.
Output: manual routes and proof tests that force reviewers to check tab reachability, authoritative runtime/dependency truth, and one shared content workspace across details tabs.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ru/product-feedback-2026-04-20.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-detail-hierarchy-and-content-surface-cohesion/34-RESEARCH.md
@.planning/phases/34-detail-hierarchy-and-content-surface-cohesion/34-VALIDATION.md
@src/verification/manual/views.ts
@src/verification/manual/scenarios.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the details proof routes around tab reachability, runtime authority, and shared content language</name>
  <files>src/verification/manual/views.ts, src/verification/manual/scenarios.tsx</files>
  <action>Update `modpack-details` and the retained density-era routes so they explicitly direct reviewers to validate tab readability above the fold, the first-read runtime/dependency summary, and one shared workspace language across Mods, Resource Packs, Shaders, Worlds, and Screenshots. Remove wording that still normalizes the older “better than before” density story.</action>
  <verify>npx eslint src/verification/manual/views.ts src/verification/manual/scenarios.tsx && npx tsc --noEmit</verify>
  <done>The proof hub describes the real Phase 34 contract instead of stale density-only expectations.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the refreshed proof copy with narrow route-facing assertions</name>
  <files>src/verification/manual/__tests__/views.test.ts</files>
  <action>Update the manual route tests so they fail when the details routes stop mentioning tab reachability, when runtime authority disappears from the descriptions, or when the cross-tab content cohesion contract regresses back to generic density wording.</action>
  <verify>npx vitest run src/verification/manual/__tests__/views.test.ts && npx tsc --noEmit</verify>
  <done>The proof harness is regression-protected against drifting back to the older details-density story.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/verification/manual/__tests__/views.test.ts`
- [ ] `npx eslint src/verification/manual/views.ts src/verification/manual/scenarios.tsx`
- [ ] `npx tsc --noEmit`
- [ ] Manual proof-hub review completed: relevant routes clearly communicate tab reachability, runtime/dependency authority, and shared content workspace language
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Future closeout work cannot claim Phase 34 complete while still reviewing the older density contract
</success_criteria>

<output>
After completion, create `.planning/phases/34-detail-hierarchy-and-content-surface-cohesion/34-04-SUMMARY.md`
</output>
