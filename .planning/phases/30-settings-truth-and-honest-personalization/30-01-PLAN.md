---
phase: 30-settings-truth-and-honest-personalization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contexts/SettingsContext.tsx
  - src/contexts/settings/theme.ts
  - src/contexts/settings/theme-presets.ts
  - src/contexts/settings/types.ts
  - src/components/settings/tabs/AppearanceTab.tsx
  - src/components/layout/BackgroundLayer.tsx
  - src/components/layout/__tests__/BackgroundLayer.motion.test.tsx
  - src/contexts/settings/__tests__/themeRuntimeContract.test.ts
  - src/contexts/settings/__tests__/themeDocument.test.ts
  - src/components/settings/__tests__/AppearanceTab.presets.test.tsx
  - src/components/settings/__tests__/AppearanceTab.state-fidelity.test.tsx
autonomous: true
requirements:
  - SETTINGS-01
user_setup: []
must_haves:
  truths:
    - Preset selection must produce one deterministic visual result without hidden dark-or-light mode flips, split background ownership, or ambiguous fallback to base defaults.
    - Preset identity, base theme mode, accent, custom overrides, and background settings must persist as one explicit runtime contract instead of relying on destructive edits or inference-only recovery.
    - Import and export must round-trip declared preset-versus-custom ownership directly; inference may remain only as migration fallback for older payloads or storage.
    - The resolved appearance contract must drive both document tokens and background rendering so the user sees one truthful appearance system rather than separate seams.
  artifacts:
    - src/contexts/SettingsContext.tsx
    - src/contexts/settings/theme.ts
    - src/contexts/settings/theme-presets.ts
    - src/contexts/settings/types.ts
    - src/components/settings/tabs/AppearanceTab.tsx
    - src/components/layout/BackgroundLayer.tsx
    - src/components/layout/__tests__/BackgroundLayer.motion.test.tsx
    - src/contexts/settings/__tests__/themeRuntimeContract.test.ts
    - src/contexts/settings/__tests__/themeDocument.test.ts
    - src/components/settings/__tests__/AppearanceTab.presets.test.tsx
    - src/components/settings/__tests__/AppearanceTab.state-fidelity.test.tsx
  key_links:
    - `30-RESEARCH.md` identifies `SettingsContext`, `theme.ts`, `theme-presets.ts`, `AppearanceTab.tsx`, and `BackgroundLayer.tsx` as the split ownership seam that currently breaks settings trust.
    - `30-RESEARCH.md` calls out preset loss on custom edits, hidden `defaultTheme` coupling, and import/export inference as the main blockers for truthful personalization.
    - `30-VALIDATION.md` requires the runtime-document, preset, and appearance-state seams to stay green after each task before downstream settings cleanup can proceed.
---

<objective>
Repair preset, theme, custom-override, and background ownership so appearance state becomes truthful and deterministic.

Purpose: eliminate the hidden state coupling that makes preset themes feel misleading by giving FMCL one explicit appearance runtime contract instead of several loosely related persistence seams.
Output: preset application, accent persistence, custom overrides, import-export behavior, and background rendering all consume one resolved appearance model with regression coverage that proves deterministic results.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-settings-truth-and-honest-personalization/30-RESEARCH.md
@.planning/phases/30-settings-truth-and-honest-personalization/30-VALIDATION.md
@src/contexts/SettingsContext.tsx
@src/contexts/settings/theme.ts
@src/contexts/settings/theme-presets.ts
@src/components/settings/tabs/AppearanceTab.tsx
@src/components/layout/BackgroundLayer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish one explicit appearance runtime contract and persistence owner</name>
  <files>src/contexts/SettingsContext.tsx, src/contexts/settings/theme.ts, src/contexts/settings/theme-presets.ts, src/contexts/settings/types.ts, src/contexts/settings/__tests__/themeRuntimeContract.test.ts, src/contexts/settings/__tests__/themeDocument.test.ts</files>
  <action>Refactor appearance state so preset identity, base theme mode, accent ownership, custom overrides, and background behavior resolve through one explicit contract with migration-safe persistence. Stop relying on preset inference as the normal ownership path, preserve legacy storage only as fallback migration, and ensure the resolved contract is the source of truth for document tokens.</action>
  <verify>npx vitest run src/contexts/settings/__tests__/themeDocument.test.ts src/contexts/settings/__tests__/themeRuntimeContract.test.ts && npx tsc --noEmit</verify>
  <done>The settings layer exposes one deterministic appearance contract that survives preset changes, accent updates, custom edits, and persisted reloads without silent ownership loss.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire appearance controls, import-export, and background rendering to the shared contract</name>
  <files>src/components/settings/tabs/AppearanceTab.tsx, src/components/layout/BackgroundLayer.tsx, src/components/layout/__tests__/BackgroundLayer.motion.test.tsx, src/components/settings/__tests__/AppearanceTab.presets.test.tsx, src/components/settings/__tests__/AppearanceTab.state-fidelity.test.tsx</files>
  <action>Update the appearance tab and background layer so preset selection, accent changes, explicit custom state, import-export payloads, and visible background behavior all consume the same resolved appearance contract. Preserve deterministic preset outcomes, stop silent preset loss on narrow edits, and make the current state observable enough for truthful UI follow-up work in later plans.</action>
  <verify>npx vitest run src/components/layout/__tests__/BackgroundLayer.motion.test.tsx src/components/settings/__tests__/AppearanceTab.presets.test.tsx src/components/settings/__tests__/AppearanceTab.state-fidelity.test.tsx src/contexts/settings/__tests__/themeRuntimeContract.test.ts && npx eslint src/contexts/SettingsContext.tsx src/contexts/settings/theme.ts src/contexts/settings/theme-presets.ts src/components/settings/tabs/AppearanceTab.tsx src/components/layout/BackgroundLayer.tsx && npx tsc --noEmit</verify>
  <done>Appearance controls and background rendering show the same truthful state the persistence layer stores, so preset and accent changes now have deterministic visible results.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/contexts/settings/__tests__/themeDocument.test.ts src/contexts/settings/__tests__/themeRuntimeContract.test.ts src/components/settings/__tests__/AppearanceTab.presets.test.tsx src/components/settings/__tests__/AppearanceTab.state-fidelity.test.tsx src/components/layout/__tests__/BackgroundLayer.motion.test.tsx`
- [ ] `npx eslint src/contexts/SettingsContext.tsx src/contexts/settings/theme.ts src/contexts/settings/theme-presets.ts src/components/settings/tabs/AppearanceTab.tsx src/components/layout/BackgroundLayer.tsx`
- [ ] `npx tsc --noEmit`
- [ ] Manual appearance walkthrough completed: switching between at least one dark preset and one light preset produces deterministic visible changes, accent updates stay attached to the resolved contract, preset identity survives bounded edits or is explicitly reclassified, and background behavior matches the resolved appearance state
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Preset themes, custom overrides, and background rendering become one deterministic appearance contract
</success_criteria>

<output>
After completion, create `.planning/phases/30-settings-truth-and-honest-personalization/30-01-SUMMARY.md`
</output>
