---
phase: 16-modpack-detail-integrity-and-discoverable-dense-navigation
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - src/components/modpacks/details/__tests__/SecondaryContentTabs.test.tsx
  - src/components/modpacks/__tests__/ModpackDetailsHeader.i18n.test.tsx
  - src/verification/manual/scenarios.tsx
  - src/verification/manual/mockEnvironment.ts
autonomous: true
requirements:
  - DETAIL-01
  - DETAIL-02
  - DETAIL-03
user_setup: []
must_haves:
  truths:
    - Phase 16 must reuse the existing `manual-verification.html?view=modpack-details` seam rather than inventing another browser harness for detail proof.
    - Focused automated proof should cover the repaired dependency truth, readable requirement copy, and dense-navigation behavior strongly enough that the final wave can stay fallout-only.
    - Manual fixtures should expose representative runtime-provided and incompatible dependency states instead of only a happy-path mod list with no phase-owned edge cases.
    - This plan owns proof and fixture shaping for the repaired detail surface only; it must not reopen unrelated modpack browser, export, or catalog work.
  artifacts:
    - src/components/modpacks/details/__tests__/SecondaryContentTabs.test.tsx
    - src/components/modpacks/__tests__/ModpackDetailsHeader.i18n.test.tsx
    - src/verification/manual/scenarios.tsx
    - src/verification/manual/mockEnvironment.ts
  key_links:
    - Research confirmed that `manual-verification.html?view=modpack-details` already exists, but its fixture currently only proves the overview surface rather than Phase 16's dependency-truth and dense-navigation edge cases.
    - The current manual mock environment already owns the Alpha Pack runtime and mod entries, so Phase 16 proof can stay on the established verification seam with targeted fixture expansion.
---

<objective>
Turn the repaired detail surface into a reviewable and regression-protected phase artifact.

Purpose: extend focused tests and the existing manual verification seam so runtime dependency truth, readable copy, and dense navigation can be proven together on the shipped modpack-details surface.
Output: automated coverage plus manual-fixture evidence for the exact detail-page behaviors Phase 16 changes.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-modpack-detail-integrity-and-discoverable-dense-navigation/16-CONTEXT.md
@.planning/phases/16-modpack-detail-integrity-and-discoverable-dense-navigation/16-RESEARCH.md
@.planning/phases/16-modpack-detail-integrity-and-discoverable-dense-navigation/16-VALIDATION.md
@.planning/phases/16-modpack-detail-integrity-and-discoverable-dense-navigation/16-01-PLAN.md
@.planning/phases/16-modpack-detail-integrity-and-discoverable-dense-navigation/16-02-PLAN.md
@src/components/modpacks/details/__tests__/SecondaryContentTabs.test.tsx
@src/components/modpacks/__tests__/ModpackDetailsHeader.i18n.test.tsx
@src/verification/manual/scenarios.tsx
@src/verification/manual/mockEnvironment.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend focused detail regressions to prove phase-owned behavior end to end</name>
  <files>src/components/modpacks/details/__tests__/SecondaryContentTabs.test.tsx, src/components/modpacks/__tests__/ModpackDetailsHeader.i18n.test.tsx</files>
  <action>Update the focused detail-surface tests so they prove runtime-provided and incompatible dependency states, readable requirement copy, and the refreshed header discoverability contract together. Keep the proof close to the shipped detail seam instead of dropping to isolated helper-only assertions, so future regressions in the rendered product surface are caught early.</action>
  <verify>npx vitest run src/components/modpacks/details/__tests__/SecondaryContentTabs.test.tsx src/components/modpacks/__tests__/ModpackDetailsHeader.i18n.test.tsx</verify>
  <done>The repaired detail behaviors are regression-protected at the same component seams the user actually sees.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the manual detail scenario with phase-owned fixture states</name>
  <files>src/verification/manual/scenarios.tsx, src/verification/manual/mockEnvironment.ts</files>
  <action>Extend the existing `modpack-details` manual verification surface so it explicitly demonstrates Phase 16's repaired states: runtime-provided dependencies, incompatible runtime versions, readable requirement copy, and the discoverable dense-navigation layout at the audited desktop width. Reuse the existing manual verification entry and fixture model rather than introducing a phase-specific page or duplicating mock state elsewhere.</action>
  <verify>npx eslint src/verification/manual/scenarios.tsx src/verification/manual/mockEnvironment.ts && npx tsc --noEmit</verify>
  <done>Phase 16 has a reviewable browser-backed proof seam that shows the repaired detail surface instead of relying only on isolated tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/modpacks/details/__tests__/SecondaryContentTabs.test.tsx src/components/modpacks/__tests__/ModpackDetailsHeader.i18n.test.tsx`
- [ ] `npx eslint src/verification/manual/scenarios.tsx src/verification/manual/mockEnvironment.ts`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Runtime dependency truth, readable requirement copy, and dense navigation are provable on both focused tests and the existing manual detail seam
</success_criteria>

<output>
After completion, create `.planning/phases/16-modpack-detail-integrity-and-discoverable-dense-navigation/16-03-SUMMARY.md`
</output>
