---
phase: 28-product-restraint-and-native-shell-truth
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/window/windowManager.ts
  - src/components/TitleBar.tsx
  - src/components/AppLayout.tsx
  - src/services/ipc/windowControlsIPC.ts
  - electron/window/__tests__/windowManager.macos.test.ts
  - src/components/__tests__/TitleBar.branding.test.tsx
  - src/components/__tests__/UpdateNotification.layout.test.tsx
autonomous: true
requirements:
  - SHELL-05
user_setup: []
must_haves:
  truths:
    - macOS must feel native-first. FMCL cannot expose native traffic lights while still rendering a competing renderer-owned chrome contract.
    - The repair must live in the main-window and shared shell seams, not in route-local padding or notification-specific hacks.
    - Notification and shell-safe-area behavior still need to clear the top edge after the chrome change, but this plan must not absorb brand restraint, modpack update locality, or startup persistence work.
    - Windows and Linux behavior should stay stable unless a shared shell cleanup is required to keep the cross-platform contract coherent.
  artifacts:
    - electron/window/windowManager.ts
    - src/components/TitleBar.tsx
    - src/components/AppLayout.tsx
    - src/services/ipc/windowControlsIPC.ts
    - electron/window/__tests__/windowManager.macos.test.ts
    - src/components/__tests__/TitleBar.branding.test.tsx
    - src/components/__tests__/UpdateNotification.layout.test.tsx
  key_links:
    - `28-RESEARCH.md` identifies `windowManager.ts`, `TitleBar.tsx`, and `AppLayout.tsx` as the hybrid macOS shell contract causing duplicate or competing chrome.
    - `28-VALIDATION.md` requires both a main-process macOS window test and renderer shell assertions because jsdom alone cannot prove the real `BrowserWindow` branch.
---

<objective>
Make the macOS window contract truthful and native-first.

Purpose: remove competing custom chrome on macOS while preserving one shared shell-safe top edge for notifications, dragging, and cross-platform layout.
Output: a native-first macOS shell contract in the real Electron window plus focused regression coverage for the main-process branch and renderer shell seam.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-product-restraint-and-native-shell-truth/28-RESEARCH.md
@.planning/phases/28-product-restraint-and-native-shell-truth/28-VALIDATION.md
@electron/window/windowManager.ts
@src/components/TitleBar.tsx
@src/components/AppLayout.tsx
@src/services/ipc/windowControlsIPC.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the Wave 0 main-process macOS shell seam</name>
  <files>electron/window/windowManager.ts, electron/window/__tests__/windowManager.macos.test.ts</files>
  <action>Land the missing Wave 0 proof seam for the real macOS `BrowserWindow` branch and tighten `createMainWindow()` just enough for that seam to encode the native-first chrome contract. This task exists so execution does not depend on an untested main-process path while the renderer shell is still being cleaned up.</action>
  <verify>npx vitest run electron/window/__tests__/windowManager.macos.test.ts && npx tsc --noEmit</verify>
  <done>The main-process macOS chrome branch is regression-protected before later renderer shell cleanup depends on it.</done>
</task>

<task type="auto">
  <name>Task 2: Finish the native-first renderer shell contract and lock its regressions</name>
  <files>electron/window/windowManager.ts, src/components/TitleBar.tsx, src/components/AppLayout.tsx, src/services/ipc/windowControlsIPC.ts, src/components/__tests__/TitleBar.branding.test.tsx, src/components/__tests__/UpdateNotification.layout.test.tsx</files>
  <action>Use the new main-process seam to finish the renderer-side cleanup: remove competing custom controls on macOS, preserve one shared shell-safe clearance seam for notifications and drag behavior, and avoid compensating with route-local offsets or notification-specific hacks. Update renderer tests so they prove macOS clears custom controls while keeping title-bar-safe notification placement and shell ordering intact.</action>
  <verify>npx vitest run src/components/__tests__/TitleBar.branding.test.tsx src/components/__tests__/UpdateNotification.layout.test.tsx && npx eslint src/components/TitleBar.tsx src/components/AppLayout.tsx electron/window/windowManager.ts src/services/ipc/windowControlsIPC.ts && npx tsc --noEmit</verify>
  <done>The renderer shell now follows the native-first macOS contract without regressing notification clearance or drag behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run electron/window/__tests__/windowManager.macos.test.ts src/components/__tests__/TitleBar.branding.test.tsx src/components/__tests__/UpdateNotification.layout.test.tsx`
- [ ] `npx eslint electron/window/windowManager.ts src/components/TitleBar.tsx src/components/AppLayout.tsx src/services/ipc/windowControlsIPC.ts`
- [ ] `npx tsc --noEmit`
- [ ] Real-app macOS check completed: traffic lights visible and unobstructed, no competing right-side control group, update banner clears shell chrome, and the window still drags as expected
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- macOS users see one native-first window contract instead of native traffic lights fighting with renderer-owned chrome
</success_criteria>

<output>
After completion, create `.planning/phases/28-product-restraint-and-native-shell-truth/28-01-SUMMARY.md`
</output>
