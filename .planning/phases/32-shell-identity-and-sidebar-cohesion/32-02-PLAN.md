---
phase: 32-shell-identity-and-sidebar-cohesion
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/window/windowManager.ts
  - electron/app/bootstrap.ts
  - src/components/TitleBar.tsx
  - src/components/AppLayout.tsx
  - src/services/ipc/windowControlsIPC.ts
  - electron/window/__tests__/windowManager.macos.test.ts
  - src/components/__tests__/TitleBar.branding.test.tsx
  - src/components/__tests__/UpdateNotification.layout.test.tsx
autonomous: true
requirements:
  - SHELL-10
user_setup: []
must_haves:
  truths:
    - macOS must behave as a native-first window. Renderer chrome cannot compete with traffic lights, title-bar clearance, or icon identity.
    - The shared top edge still needs one shell-owned drag and notification seam, but it must not solve problems by reintroducing custom chrome.
    - Native icon resolution should be intentional and aligned with the canonical launcher identity instead of platform-specific drift.
    - This plan owns the main app window only; it must not absorb a broader cross-platform shell redesign.
  artifacts:
    - electron/window/windowManager.ts
    - electron/app/bootstrap.ts
    - src/components/TitleBar.tsx
    - src/components/AppLayout.tsx
    - src/services/ipc/windowControlsIPC.ts
    - electron/window/__tests__/windowManager.macos.test.ts
    - src/components/__tests__/TitleBar.branding.test.tsx
    - src/components/__tests__/UpdateNotification.layout.test.tsx
  key_links:
    - `docs/ru/product-feedback-2026-04-20.md` explicitly rejects macOS chrome that argues with native traffic lights or duplicates control groups.
    - `32-RESEARCH.md` identifies the remaining risk as a hybrid shell contract plus divergent native icon resolution, not just button presence.
    - `32-VALIDATION.md` requires both mocked `BrowserWindow` proof and renderer shell-order proof before closeout can claim `SHELL-10`.
---

<objective>
Finish the native macOS shell contract.

Purpose: keep the real window native-first on macOS while preserving one shared shell-safe top edge for notifications and drag behavior.
Output: a main-window and renderer-shell contract that clears native chrome, uses intentional app identity, and remains regression-protected.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ru/product-feedback-2026-04-20.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-shell-identity-and-sidebar-cohesion/32-RESEARCH.md
@.planning/phases/32-shell-identity-and-sidebar-cohesion/32-VALIDATION.md
@electron/window/windowManager.ts
@electron/app/bootstrap.ts
@src/components/TitleBar.tsx
@src/components/AppLayout.tsx
@src/services/ipc/windowControlsIPC.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten the main-process macOS window and native icon contract</name>
  <files>electron/window/windowManager.ts, electron/app/bootstrap.ts, electron/window/__tests__/windowManager.macos.test.ts</files>
  <action>Audit and tighten the macOS `BrowserWindow` configuration and native icon-resolution path so the main app window stays native-first and uses intentional launcher identity. Extend the macOS window test seam beyond `hiddenInset` to prove the owned chrome and icon contract directly.</action>
  <verify>npx vitest run electron/window/__tests__/windowManager.macos.test.ts && npx tsc --noEmit</verify>
  <done>The main-process macOS branch is explicitly protected against chrome and icon-regression drift.</done>
</task>

<task type="auto">
  <name>Task 2: Align the renderer top-edge seam with the native-first shell contract</name>
  <files>src/components/TitleBar.tsx, src/components/AppLayout.tsx, src/services/ipc/windowControlsIPC.ts, src/components/__tests__/TitleBar.branding.test.tsx, src/components/__tests__/UpdateNotification.layout.test.tsx</files>
  <action>Use the tightened main-process contract to keep the renderer title bar minimal and non-competitive on macOS while preserving a safe shared top edge for notifications and drag behavior. Rewrite the relevant renderer tests so they prove shell ordering and title-bar restraint instead of older brand-heavy assumptions.</action>
  <verify>npx vitest run src/components/__tests__/TitleBar.branding.test.tsx src/components/__tests__/UpdateNotification.layout.test.tsx && npx eslint electron/window/windowManager.ts electron/app/bootstrap.ts src/components/TitleBar.tsx src/components/AppLayout.tsx src/services/ipc/windowControlsIPC.ts && npx tsc --noEmit</verify>
  <done>The renderer top edge now supports the native macOS shell contract without reintroducing duplicate controls or unsafe spacing.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run electron/window/__tests__/windowManager.macos.test.ts src/components/__tests__/TitleBar.branding.test.tsx src/components/__tests__/UpdateNotification.layout.test.tsx`
- [ ] `npx eslint electron/window/windowManager.ts electron/app/bootstrap.ts src/components/TitleBar.tsx src/components/AppLayout.tsx src/services/ipc/windowControlsIPC.ts`
- [ ] `npx tsc --noEmit`
- [ ] Real-app macOS check completed: traffic lights visible and unobstructed, no competing renderer control group, update banner clears the shell chrome, drag behavior still works, and the dock/window icon matches the intended launcher identity
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- macOS users see one native-first shell contract instead of a native window with leftover renderer chrome behavior
</success_criteria>

<output>
After completion, create `.planning/phases/32-shell-identity-and-sidebar-cohesion/32-02-SUMMARY.md`
</output>
