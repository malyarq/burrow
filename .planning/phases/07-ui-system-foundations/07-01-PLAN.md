---
phase: 07-ui-system-foundations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/components/ui/Button.tsx
  - src/components/ui/Input.tsx
  - src/components/ui/Select.tsx
  - src/components/ui/Modal.tsx
  - src/components/ui/ConfirmDialog.tsx
  - src/components/ui/Tooltip.tsx
  - src/components/ui/Toast.tsx
  - src/components/ui/ProgressBar.tsx
  - src/components/ui/CollapsibleSection.tsx
  - src/components/ui/__tests__/Modal.a11y.test.tsx
autonomous: true
requirements:
  - DSYS-01
  - DSYS-02
user_setup: []
must_haves:
  truths:
    - Shared FMCL controls expose one consistent surface, spacing, focus, and motion contract instead of each primitive carrying its own styling dialect.
    - The shared visual language is expressed through token-aware primitives and global surface classes, not reintroduced as feature-local hardcoded values.
    - Foundational dialog and menu-like affordances use one shared action grammar for buttons, confirms, tooltip-triggered controls, and selectable openers before shell rollout begins.
    - Foundation changes preserve or improve the existing dialog and interactive accessibility seams rather than forcing later phases to patch around them.
  artifacts:
    - src/index.css
    - src/components/ui/Button.tsx
    - src/components/ui/Input.tsx
    - src/components/ui/Select.tsx
    - src/components/ui/Modal.tsx
    - src/components/ui/ConfirmDialog.tsx
    - src/components/ui/Tooltip.tsx
    - src/components/ui/Toast.tsx
    - src/components/ui/ProgressBar.tsx
    - src/components/ui/CollapsibleSection.tsx
    - src/components/ui/__tests__/Modal.a11y.test.tsx
  key_links:
    - Do not widen this slice into route-level redesign; Phase 7 starts by stabilizing the reusable contract later surfaces must consume.
    - Reuse the existing CSS variable and surface-class approach in `src/index.css` instead of inventing a parallel component-library abstraction.
---

<objective>
Normalize FMCL's shared UI foundation.

Purpose: establish a real visual contract across the primitive layer before shell or route rollout builds on it.
Output: token-aware shared primitives and surface classes that define the baseline look and behavior for refreshed launcher surfaces.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-ui-system-foundations/07-RESEARCH.md
@.planning/phases/07-ui-system-foundations/07-VALIDATION.md
@src/index.css
@src/components/ui/Button.tsx
@src/components/ui/Input.tsx
@src/components/ui/Select.tsx
@src/components/ui/Modal.tsx
@src/components/ui/ConfirmDialog.tsx
@src/components/ui/Tooltip.tsx
@src/components/ui/Toast.tsx
@src/components/ui/ProgressBar.tsx
@src/components/ui/CollapsibleSection.tsx
@src/components/ui/__tests__/Modal.a11y.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Formalize the token, surface, and foundational action contract for shared primitives</name>
  <files>src/index.css, src/components/ui/Button.tsx, src/components/ui/Input.tsx, src/components/ui/Select.tsx, src/components/ui/ConfirmDialog.tsx, src/components/ui/Tooltip.tsx, src/components/ui/Toast.tsx, src/components/ui/ProgressBar.tsx, src/components/ui/CollapsibleSection.tsx</files>
  <action>Align the shared UI layer around one foundation contract for surfaces, spacing, focus treatment, motion behavior, accent-aware states, and foundational action semantics. Consolidate the visual and affordance rules in `src/index.css` and the shared primitives so later phases inherit consistent card, dialog, confirm, tooltip-trigger, and selectable-control behavior instead of inventing new local patterns. Keep the work brownfield-safe: refine the existing primitives rather than introducing a replacement component system.</action>
  <verify>npx eslint src/components/ui/Button.tsx src/components/ui/Input.tsx src/components/ui/Select.tsx src/components/ui/ConfirmDialog.tsx src/components/ui/Tooltip.tsx src/components/ui/Toast.tsx src/components/ui/ProgressBar.tsx src/components/ui/CollapsibleSection.tsx && npx tsc --noEmit</verify>
  <done>FMCL has one shared primitive, surface, and foundational action baseline that later shell and route work can rely on without re-specifying core visual or affordance rules.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve accessibility while the primitive contract changes</name>
  <files>src/components/ui/Modal.tsx, src/components/ui/__tests__/Modal.a11y.test.tsx</files>
  <action>Update the shared modal and any related primitive semantics needed by the new foundation contract while preserving accessible roles, focus handling, and close behavior. Extend the focused modal regression test only as far as necessary to prove the foundation changes did not trade visual consistency for weaker semantics.</action>
  <verify>npx vitest run src/components/ui/__tests__/Modal.a11y.test.tsx</verify>
  <done>The primitive-level visual cleanup remains compatible with the a11y guarantees already established in the shared modal seam.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/ui/__tests__/Modal.a11y.test.tsx`
- [ ] `npx eslint src/components/ui/Button.tsx src/components/ui/Input.tsx src/components/ui/Select.tsx src/components/ui/Modal.tsx src/components/ui/ConfirmDialog.tsx src/components/ui/Tooltip.tsx src/components/ui/Toast.tsx src/components/ui/ProgressBar.tsx src/components/ui/CollapsibleSection.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- FMCL has a stable primitive and surface contract that later Phase 7 and Phase 8 plans can consume instead of working around
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-system-foundations/07-01-SUMMARY.md`
</output>
