---
phase: 24-verification-locale-and-release-truth
plan: "02"
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - src/verification/manual/views.ts
  - src/verification/manual/scenarios.tsx
  - src/verification/manual/mockEnvironment.ts
autonomous: true
requirements:
  - VER-01
  - VER-03
user_setup: []
must_haves:
  truths:
    - Degraded-state closeout proof must be representative and shell-integrated; it cannot collapse back into synthetic token pages or isolated component demos.
    - Dark/light and EN/RU evidence must become explicit Phase 24 closeout pairs on real milestone-owned content instead of staying hidden behind the older Phase 22 view IDs.
    - Theme and locale pairs should keep data and shell composition as stable as possible so reviewers can compare theme or language differences instead of unrelated fixture churn.
    - This plan owns representative closeout views only; it must not add the screenshot lane or broaden fallback implementation beyond bounded view-fixture fallout required to keep proof truthful.
  artifacts:
    - src/verification/manual/views.ts
    - src/verification/manual/scenarios.tsx
    - src/verification/manual/mockEnvironment.ts
  key_links:
    - Research recommended a small final closeout family built around `phase-24-home-closeout`, `phase-24-modpacks-closeout`, `phase-24-degraded-closeout`, and explicit dark/light plus EN/RU pairs.
    - The current manual seam still underrepresents degraded-state proof even though Phase 23 landed strong degraded-state behavior and tests.
    - Phase 22 already proved the value of dedicated theme and locale views; Phase 24 should reuse that explicit-review pattern under closeout-owned IDs instead of leaving the phase-local names as the final milestone story.
---

<objective>
Expand the closeout proof matrix with representative degraded-state views and explicit theme or locale pairs.

Purpose: make the final `v0.5.0` proof set reviewable as a shipped product story instead of a success-only or historically mixed walkthrough.
Output: named Phase 24 closeout views for representative degraded states plus explicit dark/light and EN/RU comparison pairs on real milestone-owned content.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-verification-locale-and-release-truth/24-CONTEXT.md
@.planning/phases/24-verification-locale-and-release-truth/24-RESEARCH.md
@.planning/phases/24-verification-locale-and-release-truth/24-VALIDATION.md
@.planning/phases/24-verification-locale-and-release-truth/24-01-PLAN.md
@src/verification/manual/views.ts
@src/verification/manual/scenarios.tsx
@src/verification/manual/mockEnvironment.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add representative shell-integrated degraded closeout proof views</name>
  <files>src/verification/manual/views.ts, src/verification/manual/scenarios.tsx, src/verification/manual/mockEnvironment.ts</files>
  <action>Implement the representative Phase 24 closeout views that prove real milestone-owned shell composition under both success and degraded conditions. Land `phase-24-home-closeout`, `phase-24-modpacks-closeout`, and `phase-24-degraded-closeout` as direct, deterministic proof states, and make the degraded view deliberately exercise high-signal failed-load or unavailable-state surfaces instead of reusing success-path data or decorative placeholder pages.</action>
  <verify>npx vitest run src/components/modpacks/__tests__/AddModPlaceholderTruth.test.tsx src/components/modpacks/__tests__/ModpackUpdateModal.degraded-state.test.tsx src/features/screenshots/components/__tests__/ScreenshotsExperience.test.tsx src/features/settings/statistics/__tests__/StatisticsTab.test.tsx src/features/share/__tests__/ShareFlows.test.tsx src/components/__tests__/ErrorBoundary.recovery.test.tsx && npx tsc --noEmit</verify>
  <done>The Phase 24 closeout set proves milestone-owned shell content under representative degraded conditions instead of relying on success-only manual states.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit dark/light and EN/RU closeout pairs with stable comparison fixtures</name>
  <files>src/verification/manual/views.ts, src/verification/manual/scenarios.tsx, src/verification/manual/mockEnvironment.ts</files>
  <action>Land `phase-24-theme-dark`, `phase-24-theme-light`, `phase-24-locale-en`, and `phase-24-locale-ru` as explicit Phase 24 closeout pairs. Reuse real milestone-owned content with visible translated copy, dates, counts, and stateful controls, but keep the underlying fixture data stable enough that the only intended differences are theme or locale. Preserve shell-integrated composition, and keep the new closeout IDs as the obvious review targets rather than routing reviewers back through the older Phase 22 names.</action>
  <verify>git diff --check -- src/verification/manual/views.ts src/verification/manual/scenarios.tsx src/verification/manual/mockEnvironment.ts && npx eslint src/verification/manual/views.ts src/verification/manual/scenarios.tsx src/verification/manual/mockEnvironment.ts && npx vitest run src/contexts/settings/__tests__/themeRuntimeContract.test.ts src/utils/__tests__/format.test.ts src/components/settings/__tests__/AppearanceTab.state-fidelity.test.tsx src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx src/components/modpacks/details/__tests__/SecondaryContentTabs.test.tsx && npx tsc --noEmit</verify>
  <done>The final proof matrix exposes direct dark/light and EN/RU comparisons on real shell-integrated milestone surfaces.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `git diff --check -- src/verification/manual/views.ts src/verification/manual/scenarios.tsx src/verification/manual/mockEnvironment.ts`
- [ ] `npx eslint src/verification/manual/views.ts src/verification/manual/scenarios.tsx src/verification/manual/mockEnvironment.ts`
- [ ] `npx vitest run src/components/modpacks/__tests__/AddModPlaceholderTruth.test.tsx src/components/modpacks/__tests__/ModpackUpdateModal.degraded-state.test.tsx src/features/screenshots/components/__tests__/ScreenshotsExperience.test.tsx src/features/settings/statistics/__tests__/StatisticsTab.test.tsx src/features/share/__tests__/ShareFlows.test.tsx src/components/__tests__/ErrorBoundary.recovery.test.tsx`
- [ ] `npx vitest run src/contexts/settings/__tests__/themeRuntimeContract.test.ts src/utils/__tests__/format.test.ts src/components/settings/__tests__/AppearanceTab.state-fidelity.test.tsx src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx src/components/modpacks/details/__tests__/SecondaryContentTabs.test.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 24 exposes representative degraded-state proof plus explicit dark/light and EN/RU closeout pairs on the manual seam
</success_criteria>

<output>
After completion, create `.planning/phases/24-verification-locale-and-release-truth/24-02-SUMMARY.md`
</output>
