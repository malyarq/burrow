---
phase: 20-brand-system-shared-tokens-and-surface-migration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/assets/branding.ts
  - src/app/hooks/useAppIcon.ts
  - src/components/branding/BrandMark.tsx
  - src/components/branding/BrandWordmark.tsx
  - src/components/branding/BrandLockup.tsx
  - src/index.css
  - src/contexts/settings/theme.ts
  - src/contexts/settings/accent.ts
  - src/contexts/settings/theme-presets.ts
  - src/contexts/settings/types.ts
  - src/contexts/SettingsContext.tsx
  - src/components/__tests__/TitleBar.branding.test.tsx
  - src/components/__tests__/SimplePlayHome.visualTruth.test.tsx
  - src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx
autonomous: true
requirements:
  - BRAND-01
  - BRAND-02
user_setup: []
must_haves:
  truths:
    - Phase 20 must define canonical ownership for the app icon, in-product mark, wordmark, and neutral artwork fallback before broad surface migration starts.
    - Product brand identity must stop collapsing into user accent choice; user accent remains personalization, not the only visual language.
    - Shared brand tokens should extend the existing document/theme seams instead of inventing a second parallel styling system beside `SettingsContext` and `index.css`.
    - This plan owns primitives and token contract only; it must not absorb shell-surface adoption, fallback-route migration, or later interaction-state fidelity work.
  artifacts:
    - src/app/assets/branding.ts
    - src/app/hooks/useAppIcon.ts
    - src/components/branding/BrandMark.tsx
    - src/components/branding/BrandWordmark.tsx
    - src/components/branding/BrandLockup.tsx
    - src/index.css
    - src/contexts/settings/theme.ts
    - src/contexts/settings/accent.ts
    - src/contexts/settings/theme-presets.ts
    - src/contexts/settings/types.ts
    - src/contexts/SettingsContext.tsx
    - src/components/__tests__/TitleBar.branding.test.tsx
    - src/components/__tests__/SimplePlayHome.visualTruth.test.tsx
    - src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx
  key_links:
    - Phase 20 research found that `branding.ts` only exposes `APP_ICON_PATH` and `LAUNCHER_MARK_PATH`, while actual wordmark usage is duplicated as raw `FriendLauncher` text across shell surfaces.
    - The same research showed brand identity currently lives partly in `index.css`, partly in preset palettes, and partly in user accent logic, which makes FMCL drift toward arbitrary accent-led styling instead of one product-owned language.
---

<objective>
Define the canonical Phase 20 brand contract before any surface-level migration begins.

Purpose: establish one shared source of truth for FMCL brand assets, wordmark usage, and brand-owned tokens so later plans migrate surfaces onto a stable visual contract instead of inventing more ad hoc styling.
Output: reusable brand primitives plus a token contract that distinguishes product-owned identity from user-owned accent customization.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-brand-system-shared-tokens-and-surface-migration/20-RESEARCH.md
@.planning/phases/20-brand-system-shared-tokens-and-surface-migration/20-VALIDATION.md
@src/app/assets/branding.ts
@src/app/hooks/useAppIcon.ts
@src/index.css
@src/contexts/settings/theme.ts
@src/contexts/settings/accent.ts
@src/contexts/settings/theme-presets.ts
@src/contexts/settings/types.ts
@src/contexts/SettingsContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce one canonical FMCL brand contract for mark, wordmark, icon, and media-fallback roles</name>
  <files>src/app/assets/branding.ts, src/app/hooks/useAppIcon.ts, src/components/branding/BrandMark.tsx, src/components/branding/BrandWordmark.tsx, src/components/branding/BrandLockup.tsx</files>
  <action>Expand the shared branding seam so FMCL has explicit roles for OS/window icon, in-product mark, shared wordmark, and neutral media-fallback art. Add reusable brand primitives or helpers that make those roles consumable without raw asset-path branching or repeated `FriendLauncher` text strings. Keep the contract simple and Minecraft-readable rather than abstract or novelty-led.</action>
  <verify>npx eslint src/app/assets/branding.ts src/app/hooks/useAppIcon.ts src/components/branding/BrandMark.tsx src/components/branding/BrandWordmark.tsx src/components/branding/BrandLockup.tsx && npx tsc --noEmit</verify>
  <done>FMCL has one reusable brand source of truth that clearly separates icon, mark, wordmark, and content-art fallback ownership.</done>
</task>

<task type="auto">
  <name>Task 2: Add a product-owned token layer above raw palette and accent seams, then lock it with focused tests</name>
  <files>src/index.css, src/contexts/settings/theme.ts, src/contexts/settings/accent.ts, src/contexts/settings/theme-presets.ts, src/contexts/settings/types.ts, src/contexts/SettingsContext.tsx, src/components/__tests__/TitleBar.branding.test.tsx, src/components/__tests__/SimplePlayHome.visualTruth.test.tsx, src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx</files>
  <action>Extend the existing theme/document seams with product-owned brand tokens for typography, shell depth, mark treatment, and neutral placeholder framing so FMCL no longer derives its whole identity from user accent. Keep the contract phase-bounded: do not solve interaction-state fidelity or preset distinctness beyond what is necessary to make the shared brand system authoritative. Add focused coverage proving the new primitives can drive core shell and home surfaces without falling back to raw duplicated branding.</action>
  <verify>npx vitest run src/components/__tests__/SimplePlayHome.visualTruth.test.tsx src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx src/components/__tests__/TitleBar.branding.test.tsx && npx tsc --noEmit</verify>
  <done>Brand primitives and token ownership are regression-protected, and Phase 20 has a reusable contract that later shell and route migrations can consume without re-deciding branding rules.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/__tests__/SimplePlayHome.visualTruth.test.tsx src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx src/components/__tests__/TitleBar.branding.test.tsx`
- [ ] `npx eslint src/app/assets/branding.ts src/app/hooks/useAppIcon.ts src/components/branding/BrandMark.tsx src/components/branding/BrandWordmark.tsx src/components/branding/BrandLockup.tsx src/index.css src/contexts/settings/theme.ts src/contexts/settings/accent.ts src/contexts/settings/theme-presets.ts src/contexts/settings/types.ts`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- FMCL has one explicit brand contract and token ownership layer instead of duplicated asset decisions or accent-only identity
</success_criteria>

<output>
After completion, create `.planning/phases/20-brand-system-shared-tokens-and-surface-migration/20-01-SUMMARY.md`
</output>
