---
phase: 16-modpack-detail-integrity-and-discoverable-dense-navigation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/modpacks/ModpackDetails.tsx
  - src/components/modpacks/details/ModpackDetailsModsTab.tsx
  - src/utils/versionCheck.ts
  - src/locales/en.json
  - src/locales/ru.json
  - src/components/modpacks/details/__tests__/SecondaryContentTabs.test.tsx
autonomous: true
requirements:
  - DETAIL-01
  - DETAIL-02
user_setup: []
must_haves:
  truths:
    - The installed pack `effectiveConfig` must become the authoritative source for runtime-provided dependencies on the detail page before falling back to installed-mod lookup.
    - Dependencies provided by the pack runtime must remain in the ordinary dependency UI, and wrong runtime versions must surface as incompatible rather than missing.
    - Readable requirement copy must be derived from the same accepted range grammar used for compatibility checks so formatting and status logic cannot drift apart.
    - This plan owns dependency truth and requirement readability only; it must not absorb the dense-navigation redesign that belongs to the sibling header plan.
  artifacts:
    - src/components/modpacks/ModpackDetails.tsx
    - src/components/modpacks/details/ModpackDetailsModsTab.tsx
    - src/utils/versionCheck.ts
    - src/locales/en.json
    - src/locales/ru.json
  key_links:
    - Phase 16 context locked `effectiveConfig` as the source of truth for runtime-provided dependencies and explicitly rejected treating wrong runtime versions as ordinary missing dependencies.
    - Research found the current false-negative behavior and raw range leak are both concentrated in `ModpackDetailsModsTab.tsx`, with `versionCheck.ts` already owning the accepted range grammar.
---

<objective>
Repair the dependency truth seam on modpack details.

Purpose: stop the detail page from falsely marking pack-provided runtime dependencies as missing and replace raw version-range syntax with readable requirement copy grounded in the same compatibility rules.
Output: one truthful dependency-state resolver and readable requirement rendering on the shipped modpack detail surface.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-modpack-detail-integrity-and-discoverable-dense-navigation/16-CONTEXT.md
@.planning/phases/16-modpack-detail-integrity-and-discoverable-dense-navigation/16-RESEARCH.md
@.planning/phases/16-modpack-detail-integrity-and-discoverable-dense-navigation/16-VALIDATION.md
@src/components/modpacks/ModpackDetails.tsx
@src/components/modpacks/details/ModpackDetailsModsTab.tsx
@src/utils/versionCheck.ts
@src/locales/en.json
@src/locales/ru.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve dependency truth against pack runtime before installed-mod fallback</name>
  <files>src/components/modpacks/ModpackDetails.tsx, src/components/modpacks/details/ModpackDetailsModsTab.tsx</files>
  <action>Pass the installed runtime context from modpack details into the dependency UI and replace the current id-only dependency check with a resolver that can recognize pack-provided runtime dependencies first. Keep the logic conservative: only explicit runtime values from the installed configuration should satisfy `minecraft` or loader-family dependencies, and wrong runtime versions must surface as incompatible rather than missing. Preserve the existing detail-page ownership and avoid turning this into a broader dependency-management feature.</action>
  <verify>npx eslint src/components/modpacks/ModpackDetails.tsx src/components/modpacks/details/ModpackDetailsModsTab.tsx && npx tsc --noEmit</verify>
  <done>The detail page stops lying about pack-provided runtime dependencies and can distinguish satisfied, incompatible, and missing states coherently.</done>
</task>

<task type="auto">
  <name>Task 2: Replace raw range syntax with readable requirement copy and regression coverage</name>
  <files>src/components/modpacks/details/ModpackDetailsModsTab.tsx, src/utils/versionCheck.ts, src/locales/en.json, src/locales/ru.json, src/components/modpacks/details/__tests__/SecondaryContentTabs.test.tsx</files>
  <action>Add a product-facing range formatter or companion helper that reuses the accepted compatibility grammar from `versionCheck.ts`, then route dependency-row rendering through localized readable copy instead of raw `String(dep.versionRange)`. Cover supported range shapes and safe fallback behavior in the focused detail-surface regression suite so readable copy and dependency status remain aligned over time.</action>
  <verify>npx vitest run src/components/modpacks/details/__tests__/SecondaryContentTabs.test.tsx && npx eslint src/components/modpacks/details/ModpackDetailsModsTab.tsx src/utils/versionCheck.ts src/locales/en.json src/locales/ru.json && npx tsc --noEmit</verify>
  <done>Supported dependency requirements render in understandable product copy, and the mods tab has focused proof for runtime truth plus readable requirement rendering.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/modpacks/details/__tests__/SecondaryContentTabs.test.tsx`
- [ ] `npx eslint src/components/modpacks/ModpackDetails.tsx src/components/modpacks/details/ModpackDetailsModsTab.tsx src/utils/versionCheck.ts src/locales/en.json src/locales/ru.json`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Modpack details now treats runtime-provided dependencies truthfully and renders readable requirement copy instead of raw syntax
</success_criteria>

<output>
After completion, create `.planning/phases/16-modpack-detail-integrity-and-discoverable-dense-navigation/16-01-SUMMARY.md`
</output>
