---
phase: 12-theme-truth-and-settings-ia-simplification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contexts/settings/theme-presets.ts
  - src/contexts/settings/types.ts
  - src/contexts/settings/theme.ts
  - src/contexts/SettingsContext.tsx
  - src/components/settings/tabs/AppearanceTab.tsx
  - src/contexts/settings/__tests__/themeDocument.test.ts
  - src/components/settings/__tests__/AppearanceTab.presets.test.tsx
autonomous: true
requirements:
  - THEME-01
user_setup: []
must_haves:
  truths:
    - FMCL presets must become truthful launcher presets, not one-off writes to `theme` plus `customTheme` that drift after mode changes.
    - Preset application must own the whole runtime contract that the renderer actually consumes, so applying a preset updates the launcher immediately and predictably.
    - The fix must preserve the existing brownfield settings model and storage seam instead of redesigning the whole theming system.
    - This plan establishes preset source of truth only; it must not absorb the broader contrast sweep or settings IA redesign.
  artifacts:
    - src/contexts/settings/theme-presets.ts
    - src/contexts/settings/types.ts
    - src/contexts/settings/theme.ts
    - src/contexts/SettingsContext.tsx
    - src/components/settings/tabs/AppearanceTab.tsx
  key_links:
    - Phase 12 Research requires `theme-presets.ts`, `AppearanceTab.tsx`, `SettingsContext.tsx`, and `theme.ts` to be treated as one runtime seam.
    - Phase 11 already stabilized the shell and layout contract, so this plan should stay focused on preset truth rather than reopening responsive layout work.
---

<objective>
Repair FMCL's preset theme application contract.

Purpose: make shipped presets apply immediately and truthfully across the live launcher instead of behaving like partial custom overrides that depend on a lucky light or dark state.
Output: a stable preset source of truth, predictable mode behavior, and focused regression coverage for preset application.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-theme-truth-and-settings-ia-simplification/12-RESEARCH.md
@.planning/phases/12-theme-truth-and-settings-ia-simplification/12-VALIDATION.md
@src/contexts/settings/theme-presets.ts
@src/contexts/settings/types.ts
@src/contexts/settings/theme.ts
@src/contexts/SettingsContext.tsx
@src/components/settings/tabs/AppearanceTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish a truthful preset source of truth</name>
  <files>src/contexts/settings/theme-presets.ts, src/contexts/settings/types.ts, src/contexts/settings/theme.ts, src/contexts/SettingsContext.tsx</files>
  <action>Refine the preset model and document-application seam so shipped presets have a stable identity and mode behavior instead of acting like loosely coupled `theme` plus `customTheme` writes. Close the gaps between the preset model, persisted settings, and the document variables that the live launcher actually consumes. Keep the work brownfield-safe: preserve persisted settings compatibility where possible, and do not turn this slice into a full theme-system rewrite.</action>
  <verify>npx eslint src/contexts/SettingsContext.tsx src/contexts/settings/theme.ts src/contexts/settings/theme-presets.ts && npx tsc --noEmit</verify>
  <done>FMCL has one truthful preset runtime contract that later surface fixes can rely on.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire the appearance preset controls and lock the contract with tests</name>
  <files>src/components/settings/tabs/AppearanceTab.tsx, src/contexts/settings/__tests__/themeDocument.test.ts, src/components/settings/__tests__/AppearanceTab.presets.test.tsx</files>
  <action>Update the appearance surface so preset application uses the repaired contract and exposes honest mode behavior to the user. Add focused regression coverage for preset selection, preset-driven mode changes, and the immediate launcher update contract. Keep tests seam-level and deterministic instead of trying to prove every themed route here.</action>
  <verify>npx vitest run src/contexts/settings/__tests__/themeDocument.test.ts src/components/settings/__tests__/AppearanceTab.presets.test.tsx</verify>
  <done>Preset selection is regression-protected and no longer depends on manual memory about how presets should behave.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/contexts/settings/__tests__/themeDocument.test.ts src/components/settings/__tests__/AppearanceTab.presets.test.tsx`
- [ ] `npx eslint src/contexts/SettingsContext.tsx src/contexts/settings/theme.ts src/contexts/settings/theme-presets.ts src/components/settings/tabs/AppearanceTab.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Shipped presets now have one truthful application contract instead of partial theme writes
</success_criteria>

<output>
After completion, create `.planning/phases/12-theme-truth-and-settings-ia-simplification/12-01-SUMMARY.md`
</output>
