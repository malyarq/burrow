---
phase: 01-release-baseline-and-trust-boundaries
plan: "09"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-03"
  - "01-08"
files_modified:
  - electron/services/account/accountService.ts
  - electron/services/mirrors/mirrorsService.ts
  - shared/types/account.ts
  - shared/types/mirrors.ts
  - shared/contracts/account.ts
  - shared/contracts/mirrors.ts
  - src/services/ipc/accountIPC.ts
  - src/services/ipc/mirrorsIPC.ts
  - src/features/accounts/AccountsPage.tsx
  - src/features/settings/mirrors/MirrorsSettings.tsx
  - src/locales/en.json
  - src/locales/ru.json
autonomous: true
requirements:
  - SEC-01
  - SEC-03
user_setup: []
must_haves:
  truths:
    - Previously saved insecure third-party accounts or mirrors are disabled visibly on load instead of continuing to operate silently.
    - The same trust policy is applied when users add a new account or mirror and when FMCL reloads persisted state on startup.
    - Disabled persisted configs surface through the accounts and mirrors UI with a corrective path instead of disappearing or remaining active without explanation.
  artifacts:
    - electron/services/account/accountService.ts
    - electron/services/mirrors/mirrorsService.ts
    - shared/types/account.ts
    - shared/types/mirrors.ts
    - shared/contracts/account.ts
    - shared/contracts/mirrors.ts
    - src/features/accounts/AccountsPage.tsx
    - src/features/settings/mirrors/MirrorsSettings.tsx
  key_links:
    - Persisted-state revalidation reuses the ingress trust policy from `01-03` instead of introducing a second set of URL rules.
    - The UI reuses the retry and confirmation patterns established in `01-01` so disabled configs are actionable in place.
---

<objective>
Disable insecure persisted auth and mirror configurations visibly instead of grandfathering them.

Purpose: Phase 1 cannot claim safer defaults if FMCL still reloads insecure remote HTTP endpoints from saved state after ingress validation is added for new submissions.
Output: Persisted account and mirror revalidation, typed disabled-state contracts, and visible corrective UI in accounts and mirrors settings.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-CONTEXT.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-RESEARCH.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-VALIDATION.md
@electron/services/account/accountService.ts
@electron/services/mirrors/mirrorsService.ts
@shared/types/account.ts
@shared/types/mirrors.ts
@shared/contracts/account.ts
@shared/contracts/mirrors.ts
@src/services/ipc/accountIPC.ts
@src/services/ipc/mirrorsIPC.ts
@src/features/accounts/AccountsPage.tsx
@src/features/settings/mirrors/MirrorsSettings.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revalidate persisted accounts and mirrors on load with typed disabled-state results</name>
  <files>electron/services/account/accountService.ts, electron/services/mirrors/mirrorsService.ts, shared/types/account.ts, shared/types/mirrors.ts, shared/contracts/account.ts, shared/contracts/mirrors.ts, src/services/ipc/accountIPC.ts, src/services/ipc/mirrorsIPC.ts</files>
  <action>Apply the Phase 1 trust policy to persisted third-party accounts and custom mirrors during load, mark insecure remote HTTP entries disabled by default instead of silently using or deleting them, and expose the disabled state through typed contracts the renderer can consume. Preserve explicit local exceptions and leave room for a future override path, but do not keep unsafe remote configs active by default.</action>
  <verify>npx eslint electron/services/account/accountService.ts electron/services/mirrors/mirrorsService.ts shared/types/account.ts shared/types/mirrors.ts shared/contracts/account.ts shared/contracts/mirrors.ts src/services/ipc/accountIPC.ts src/services/ipc/mirrorsIPC.ts && npx tsc --noEmit</verify>
  <done>Seeded insecure saved auth or mirror configs reappear as disabled and typed, rather than being used silently or disappearing without explanation.</done>
</task>

<task type="auto">
  <name>Task 2: Surface disabled persisted configs with in-flow recovery in accounts and mirrors UI</name>
  <files>src/features/accounts/AccountsPage.tsx, src/features/settings/mirrors/MirrorsSettings.tsx, src/locales/en.json, src/locales/ru.json</files>
  <action>Render disabled persisted accounts and mirrors with localized explanations plus an immediate correction path that reuses the retryable FMCL UX established in Wave 1. Keep users in the current flow; do not require them to infer why an entry disappeared or stopped working.</action>
  <verify>npx eslint src/features/accounts/AccountsPage.tsx src/features/settings/mirrors/MirrorsSettings.tsx && npx tsc --noEmit</verify>
  <done>Users can see which saved configs were disabled, why they were disabled, and what to change next without leaving the current screen.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx eslint electron/services/account/accountService.ts electron/services/mirrors/mirrorsService.ts shared/types/account.ts shared/types/mirrors.ts shared/contracts/account.ts shared/contracts/mirrors.ts src/services/ipc/accountIPC.ts src/services/ipc/mirrorsIPC.ts src/features/accounts/AccountsPage.tsx src/features/settings/mirrors/MirrorsSettings.tsx`
- [ ] `npx tsc --noEmit`
- [ ] Manual smoke seeds an insecure saved auth or mirror config, restarts FMCL, and confirms the entry is disabled visibly with a retryable correction path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- FMCL stops silently grandfathering insecure saved auth and mirror endpoints after the new trust policy is in place
</success_criteria>

<output>
After completion, create `.planning/phases/01-release-baseline-and-trust-boundaries/01-09-SUMMARY.md`
</output>
