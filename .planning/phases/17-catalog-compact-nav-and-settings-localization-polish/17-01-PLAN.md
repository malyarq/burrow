---
phase: 17-catalog-compact-nav-and-settings-localization-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/modpacks/ModpackList.tsx
  - src/components/modpacks/ModpackBrowser.tsx
  - src/components/ui/LazyImage.tsx
  - src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx
  - src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx
autonomous: true
requirements:
  - CATALOG-01
  - CATALOG-02
user_setup: []
must_haves:
  truths:
    - Catalog scanability must be repaired on the shipped catalog surfaces users actually browse, not only on one isolated list layout.
    - No-art card states must become a deliberate branded fallback contract instead of continuing to depend on `"/icon.png"` or generic gray placeholders.
    - This plan owns catalog responsiveness and fallback presentation only; it must not absorb compact-sidebar or settings-localization work from sibling plans.
    - Existing catalog browsing behavior, search/filter semantics, and current metadata resolution rules should stay intact while the layout and empty-art presentation improve.
  artifacts:
    - src/components/modpacks/ModpackList.tsx
    - src/components/modpacks/ModpackBrowser.tsx
    - src/components/ui/LazyImage.tsx
    - src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx
    - src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx
  key_links:
    - Phase 17 research identified the installed catalog filter bar in `ModpackList.tsx` as the likeliest source of the sidebar-open truncation bug because it still uses three fixed-width selects in a narrow horizontal strip.
    - The same research confirmed both installed and remote catalog cards bypass the branded launcher-mark default and hardcode `"/icon.png"` instead of routing missing-art states through one shared fallback contract.
---

<objective>
Repair the shipped catalog surfaces so they stay readable and look intentionally branded.

Purpose: make the installed and remote catalog controls remain legible with the sidebar open and replace generic no-art placeholders with one deliberate FMCL fallback contract.
Output: resilient catalog filter layout plus shared branded no-art card behavior across the catalog surfaces Phase 17 owns.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-catalog-compact-nav-and-settings-localization-polish/17-RESEARCH.md
@.planning/phases/17-catalog-compact-nav-and-settings-localization-polish/17-VALIDATION.md
@src/components/modpacks/ModpackList.tsx
@src/components/modpacks/ModpackBrowser.tsx
@src/components/ui/LazyImage.tsx
@src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the installed and remote catalog controls stay readable at the shipped sidebar-constrained width</name>
  <files>src/components/modpacks/ModpackList.tsx, src/components/modpacks/ModpackBrowser.tsx</files>
  <action>Refactor the catalog header and filter composition so the installed modpack list no longer depends on three fixed-width selects sitting in one cramped row when the launcher sidebar is open. Verify whether the remote browser needs matching composition or token tuning for parity, and keep the current search/filter behavior, control hierarchy, and keyboard semantics intact instead of inventing a new catalog model.</action>
  <verify>npx eslint src/components/modpacks/ModpackList.tsx src/components/modpacks/ModpackBrowser.tsx && npx tsc --noEmit</verify>
  <done>The shipped catalog controls remain readable and scannable at the audited desktop width with the sidebar open, without changing what the catalog does.</done>
</task>

<task type="auto">
  <name>Task 2: Route missing-art cards through one branded fallback contract and lock it with focused catalog tests</name>
  <files>src/components/modpacks/ModpackList.tsx, src/components/modpacks/ModpackBrowser.tsx, src/components/ui/LazyImage.tsx, src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx, src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx</files>
  <action>Replace the current `"/icon.png"` fallback behavior with a shared branded catalog fallback pattern that both installed and remote modpack cards can reuse. Prefer the narrowest shared contract that works with the existing `LazyImage` and launcher branding seams, then add focused regression coverage so sidebar-width readability and no-art fallback behavior are protected together on the catalog surfaces users actually see.</action>
  <verify>npx vitest run src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx && npx eslint src/components/modpacks/ModpackList.tsx src/components/modpacks/ModpackBrowser.tsx src/components/ui/LazyImage.tsx && npx tsc --noEmit</verify>
  <done>Installed and remote catalog cards both show an intentional branded empty-art state, and the catalog surfaces have focused proof for the Phase 17 responsiveness and fallback contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx`
- [ ] `npx eslint src/components/modpacks/ModpackList.tsx src/components/modpacks/ModpackBrowser.tsx src/components/ui/LazyImage.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Catalog controls stay readable with the sidebar open, and missing-art cards render through one deliberate branded fallback instead of a broken-looking placeholder
</success_criteria>

<output>
After completion, create `.planning/phases/17-catalog-compact-nav-and-settings-localization-polish/17-01-SUMMARY.md`
</output>
