---
phase: 32-shell-identity-and-sidebar-cohesion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sidebar/SidebarHeader.tsx
  - src/components/Sidebar.tsx
  - src/components/sidebar/__tests__/SidebarHeader.compact-mode.test.tsx
autonomous: true
requirements:
  - SHELL-09
user_setup: []
must_haves:
  truths:
    - The sidebar header is an orientation seam, not a place for a redundant logo billboard.
    - The launcher title must be readable at common desktop widths without relying on truncation as the normal state.
    - Collapsed and compact-mode affordances must look centered and visually coherent instead of like differently rounded leftovers inside one container.
    - This plan owns sidebar-shell readability only; it must not absorb catalog density or settings control cleanup.
  artifacts:
    - src/components/sidebar/SidebarHeader.tsx
    - src/components/Sidebar.tsx
    - src/components/sidebar/__tests__/SidebarHeader.compact-mode.test.tsx
  key_links:
    - `docs/ru/product-feedback-2026-04-20.md` explicitly calls out the square logo block, clipped `Friend...` title, off-center burger, and uneven compact switch geometry.
    - `32-RESEARCH.md` identifies `SidebarHeader.tsx` and its current test seam as the direct Phase 32 hotspot for `SHELL-09`.
    - `32-VALIDATION.md` requires the sidebar test seam to flip from “icon present” to readability and alignment assertions before execution can be trusted.
---

<objective>
Make the sidebar header readable and visually even.

Purpose: remove the redundant branded header block and normalize compact affordances so the sidebar helps orientation instead of introducing shell noise.
Output: a sidebar header that fits the launcher name naturally, keeps contextual metadata calm, and uses centered compact controls with regression coverage.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ru/product-feedback-2026-04-20.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-shell-identity-and-sidebar-cohesion/32-RESEARCH.md
@.planning/phases/32-shell-identity-and-sidebar-cohesion/32-VALIDATION.md
@src/components/sidebar/SidebarHeader.tsx
@src/components/Sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify the expanded sidebar header into one readable orientation row</name>
  <files>src/components/sidebar/SidebarHeader.tsx, src/components/Sidebar.tsx</files>
  <action>Remove the redundant square logo treatment, rebalance the title and secondary metadata, and ensure the launcher header reads as one calm orientation row instead of a clipped brand block. Keep the contextual mode/version text, but do not let it reintroduce truncation or visual noise.</action>
  <verify>npx eslint src/components/sidebar/SidebarHeader.tsx src/components/Sidebar.tsx && npx tsc --noEmit</verify>
  <done>The expanded sidebar header reads naturally without a redundant logo block or clipped launcher name.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize compact-mode affordances and flip the sidebar regression seam</name>
  <files>src/components/sidebar/SidebarHeader.tsx, src/components/sidebar/__tests__/SidebarHeader.compact-mode.test.tsx</files>
  <action>Adjust the collapsed burger and mode-switch geometry so the controls sit centered and visually consistent inside their container, then rewrite the sidebar test seam to prove readable title behavior and aligned compact affordances rather than defending the old icon-heavy contract.</action>
  <verify>npx vitest run src/components/sidebar/__tests__/SidebarHeader.compact-mode.test.tsx && npx tsc --noEmit</verify>
  <done>The compact sidebar no longer looks offset or uneven, and the regression seam encodes the new readability-first contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/sidebar/__tests__/SidebarHeader.compact-mode.test.tsx`
- [ ] `npx eslint src/components/sidebar/SidebarHeader.tsx src/components/Sidebar.tsx`
- [ ] `npx tsc --noEmit`
- [ ] Manual sidebar check completed: full launcher name is readable, no redundant square logo block remains, and compact-mode controls look centered in a real app window
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The sidebar header feels like a navigation aid instead of a clipped branded widget
</success_criteria>

<output>
After completion, create `.planning/phases/32-shell-identity-and-sidebar-cohesion/32-01-SUMMARY.md`
</output>
