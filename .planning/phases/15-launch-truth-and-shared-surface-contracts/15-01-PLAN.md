---
phase: 15-launch-truth-and-shared-surface-contracts
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/launcher/services/launcherService.ts
  - src/features/launcher/hooks/useLauncherState.ts
  - src/features/launcher/hooks/useLauncherIPC.ts
  - src/features/launcher/hooks/useLauncher.ts
  - src/features/launcher/services/__tests__/launcherService.test.ts
  - src/components/sidebar/__tests__/LaunchControls.status.test.tsx
autonomous: true
requirements:
  - LAUNCH-03
  - LAUNCH-04
user_setup: []
must_haves:
  truths:
    - Structured launch stage and progress must become the authoritative source for visible launch state on the main play surface.
    - Parsed logs may enrich the short human detail line, but they must not override a more reliable structured stage or revive stale percentages.
    - Failure, waiting, and running states must remain durable long enough for the user to understand the outcome instead of collapsing instantly back to idle.
    - This plan owns launch-state authority only; it must not absorb artwork fallback styling or broader settings cleanup that belongs to sibling plans.
  artifacts:
    - src/features/launcher/services/launcherService.ts
    - src/features/launcher/hooks/useLauncherState.ts
    - src/features/launcher/hooks/useLauncherIPC.ts
    - src/features/launcher/hooks/useLauncher.ts
  key_links:
    - Phase 15 research identified `useLauncherIPC.ts` as the current mixing point where progress events and log parsing both mutate visible state, creating contradictory UI.
    - The audited `BUG-04` and `BUG-05` both depend on getting stage precedence, progress semantics, and localized detail generation under one renderer truth contract.
---

<objective>
Make launch status truth come from one authoritative state model.

Purpose: stop the classic launch surface from showing contradictory progress, CTA, and runtime detail by defining clear precedence between structured progress, lifecycle events, and parsed logs.
Output: a durable launch-state contract that the dashboard and shared launch controls can trust.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-launch-truth-and-shared-surface-contracts/15-CONTEXT.md
@.planning/phases/15-launch-truth-and-shared-surface-contracts/15-RESEARCH.md
@.planning/phases/15-launch-truth-and-shared-surface-contracts/15-VALIDATION.md
@src/features/launcher/services/launcherService.ts
@src/features/launcher/hooks/useLauncherState.ts
@src/features/launcher/hooks/useLauncherIPC.ts
@src/features/launcher/hooks/useLauncher.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make structured launcher progress and lifecycle events authoritative</name>
  <files>src/features/launcher/services/launcherService.ts, src/features/launcher/hooks/useLauncherState.ts, src/features/launcher/hooks/useLauncherIPC.ts, src/features/launcher/hooks/useLauncher.ts</files>
  <action>Refine the renderer launch model so stage, progress, and visible status derive from one authoritative state flow. Structured progress and lifecycle signals must win over parsed logs, unknown percentages must remain indeterminate instead of snapping to fake `0%`, and success or failure states must remain visible until the user can understand them. Preserve the existing IPC seam and stage vocabulary instead of inventing a second state machine.</action>
  <verify>npx eslint src/features/launcher/services/launcherService.ts src/features/launcher/hooks/useLauncherState.ts src/features/launcher/hooks/useLauncherIPC.ts src/features/launcher/hooks/useLauncher.ts && npx tsc --noEmit</verify>
  <done>The classic launch flow exposes one reliable state contract that later UI rendering can trust without inferring truth from competing strings.</done>
</task>

<task type="auto">
  <name>Task 2: Lock state precedence with focused launch-surface regression coverage</name>
  <files>src/features/launcher/services/__tests__/launcherService.test.ts, src/components/sidebar/__tests__/LaunchControls.status.test.tsx</files>
  <action>Add focused regression coverage at the authority seam so stage precedence, indeterminate-vs-percent behavior, and failure persistence are proven without relying only on dashboard rendering. Keep a shared-controls assertion where helpful, but make the primary proof a launcher-service or hook-level test that fails if logs are allowed to override authoritative structured state.</action>
  <verify>npx vitest run src/features/launcher/services/__tests__/launcherService.test.ts src/components/sidebar/__tests__/LaunchControls.status.test.tsx</verify>
  <done>Launch-state authority is regression-protected at the same renderer seams that the audited classic screen uses.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/features/launcher/services/__tests__/launcherService.test.ts src/components/sidebar/__tests__/LaunchControls.status.test.tsx`
- [ ] `npx eslint src/features/launcher/services/launcherService.ts src/features/launcher/hooks/useLauncherState.ts src/features/launcher/hooks/useLauncherIPC.ts src/features/launcher/hooks/useLauncher.ts`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Launch progress, stage messaging, and CTA state advance from one authoritative flow instead of competing signal sources
</success_criteria>

<output>
After completion, create `.planning/phases/15-launch-truth-and-shared-surface-contracts/15-01-SUMMARY.md`
</output>
