---
phase: 01-release-baseline-and-trust-boundaries
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/ipc/handlers/modpacksHandlers.ts
  - shared/contracts/index.ts
  - shared/contracts/worlds.ts
  - src/services/ipc/worldsIPC.ts
autonomous: true
requirements:
  - SEC-01
user_setup:
  - Install workspace dependencies with `npm ci` before running verification if `node_modules/` is absent.
  - Prepare the Phase 1 manual abuse fixtures before automated verification: insecure auth URL, insecure mirror URL, traversal archive, unsafe settings path, unfamiliar external domain, and the warning-only renderer hotspots tracked in `01-VALIDATION.md`.
must_haves:
  truths:
    - Modpack import, export, and info actions resolve through one canonical handler path, so the same user action no longer depends on duplicate registration order.
    - The worlds folder-opening contract and wrapper agree about behavior and return shape, so later hardening does not build on a knowingly false seam.
    - Later validator work only has to secure one real modpacks boundary and one correct worlds contract instead of chasing duplicate or misleading paths.
  artifacts:
    - electron/ipc/handlers/modpacksHandlers.ts
    - shared/contracts/worlds.ts
    - shared/contracts/index.ts
    - src/services/ipc/worldsIPC.ts
  key_links:
    - Duplicate modpack registrations are removed before validation logic lands, so later trust checks only need to secure one real execution path.
    - The shared worlds contract matches the renderer wrapper behavior before any additional hardening or UI work depends on it.
---

<objective>
Make the modpacks and worlds seams objectively correct before broader Phase 1 hardening lands on top of them.

Purpose: Security fixes are brittle if the active handler path is duplicated or a shared contract already lies about what the renderer receives.
Output: A deduplicated modpacks handler surface plus a corrected shared worlds contract and renderer wrapper seam.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-CONTEXT.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-RESEARCH.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-VALIDATION.md
@electron/ipc/handlers/modpacksHandlers.ts
@shared/contracts/worlds.ts
@shared/contracts/index.ts
@src/services/ipc/worldsIPC.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize duplicate modpacks handler registrations into one supported execution path</name>
  <files>electron/ipc/handlers/modpacksHandlers.ts</files>
  <action>Collapse duplicate `modpacks:export`, `modpacks:getModpackInfoFromFile`, and `modpacks:import` registrations into one supported path without renaming channels that are already in use. Preserve the current feature surface, but remove order-dependent behavior so later validators land on a single real boundary.</action>
  <verify>npx eslint electron/ipc/handlers/modpacksHandlers.ts && npm run ipc:check</verify>
  <done>The active modpacks handler surface is singular and predictable, and later hardening only has to secure one registration path per channel.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the worlds contract and renderer wrapper seam without changing channel intent</name>
  <files>shared/contracts/worlds.ts, shared/contracts/index.ts, src/services/ipc/worldsIPC.ts</files>
  <action>Fix the worlds contract or wrapper so folder-opening behavior and return values agree across shared contracts and renderer code. Keep the existing channel intent intact; this slice is about making the seam truthful before later typed-boundary cleanup and handler hardening depend on it.</action>
  <verify>npm run contracts:check && npm run ipc:check && npx tsc --noEmit</verify>
  <done>The worlds contract and wrapper no longer lie about behavior or return shape, and the shared contract index exports the corrected seam.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx eslint electron/ipc/handlers/modpacksHandlers.ts`
- [ ] `npm run ipc:check`
- [ ] `npm run contracts:check`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Later security work only has to harden one real modpacks handler path and one corrected worlds contract seam
</success_criteria>

<output>
After completion, create `.planning/phases/01-release-baseline-and-trust-boundaries/01-02-SUMMARY.md`
</output>
