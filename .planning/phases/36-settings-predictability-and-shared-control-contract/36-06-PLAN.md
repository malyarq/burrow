---
phase: 36-settings-predictability-and-shared-control-contract
plan: "06"
type: execute
wave: 6
depends_on:
  - 36-05
files_modified:
  - src/contexts/SettingsContext.tsx
  - src/contexts/settings/theme.ts
  - src/contexts/settings/theme-presets.ts
  - src/components/settings/tabs/AppearanceTab.tsx
  - src/components/SimplePlayDashboard.tsx
  - src/components/sidebar/ModloaderSection.tsx
  - src/components/sidebar/ModpackSection.tsx
  - src/components/settings/__tests__/AppearanceTab.presets.test.tsx
  - src/contexts/settings/__tests__/themeRuntimeContract.test.ts
autonomous: true
requirements:
  - SETTINGS-06
  - DESIGN-01
user_setup: []
must_haves:
  truths:
    - Dark preset families visibly repaint the same shell surfaces that light/dark mode already affects.
    - Presets can own palette-coupled accent defaults, while explicit user overrides remain respected.
    - Appearance no longer needs runtime-heavy chrome to make preset changes feel visible.
    - Preset switching reads as a real launcher repaint, not a saved state mutation with no on-screen evidence.
  artifacts:
    - src/contexts/SettingsContext.tsx
    - src/contexts/settings/theme-presets.ts
    - src/components/SimplePlayDashboard.tsx
    - src/components/sidebar/ModloaderSection.tsx
  key_links:
    - `36-UAT.md` -> `src/contexts/settings/theme-presets.ts` via missing preset-owned accent defaults
    - `src/components/sidebar/*` and `SimplePlayDashboard.tsx` -> live preset visibility via tokenized shell surfaces
---

<objective>
Make appearance presets and advanced controls produce visible, trustworthy results on real product surfaces.

Purpose: fix the gap where settings technically change state but users still see almost nothing happen.
Output: preset families with owned palette behavior and tokenized high-visibility shell surfaces that visibly repaint under preset changes.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ru/product-feedback-2026-04-20.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-settings-predictability-and-shared-control-contract/36-UAT.md
@src/contexts/SettingsContext.tsx
@src/contexts/settings/theme.ts
@src/contexts/settings/theme-presets.ts
@src/components/settings/tabs/AppearanceTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Give presets owned palette defaults instead of preserving one global accent forever</name>
  <files>src/contexts/SettingsContext.tsx, src/contexts/settings/theme.ts, src/contexts/settings/theme-presets.ts, src/contexts/settings/__tests__/themeRuntimeContract.test.ts</files>
  <action>Introduce preset-owned accent defaults and make effective accent depend on preset+mode unless the user explicitly overrides it. At the same time, move high-visibility shell/dashboard/sidebar surfaces off hard-coded zinc or dark Tailwind classes and onto tokenized theme surfaces so dark presets visibly differentiate in the actual launcher.</action>
  <verify>npx vitest run src/contexts/settings/__tests__/themeRuntimeContract.test.ts src/components/settings/__tests__/AppearanceTab.presets.test.tsx && npx eslint src/contexts/SettingsContext.tsx src/contexts/settings/theme.ts src/contexts/settings/theme-presets.ts && npx tsc --noEmit</verify>
  <done>Preset family changes can carry palette-appropriate accent defaults without clobbering explicit user overrides.</done>
</task>

<task type="auto">
  <name>Task 2: Repaint the real launcher shell so preset switching is visible without extra runtime chrome</name>
  <files>src/components/settings/tabs/AppearanceTab.tsx, src/components/SimplePlayDashboard.tsx, src/components/sidebar/ModloaderSection.tsx, src/components/sidebar/ModpackSection.tsx, src/components/settings/__tests__/AppearanceTab.presets.test.tsx</files>
  <action>Move the high-visibility launcher shell surfaces that users actually stare at onto preset-aware tokens so dark presets visibly repaint the dashboard and sidebar without relying on an extra runtime card to explain what changed.</action>
  <verify>npx vitest run src/components/settings/__tests__/AppearanceTab.presets.test.tsx src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx && npx eslint src/components/settings/tabs/AppearanceTab.tsx src/components/SimplePlayDashboard.tsx src/components/sidebar/ModloaderSection.tsx src/components/sidebar/ModpackSection.tsx && npx tsc --noEmit</verify>
  <done>Preset switching now reads as a real launcher repaint instead of a mostly hidden state change.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/contexts/settings/__tests__/themeRuntimeContract.test.ts src/components/settings/__tests__/AppearanceTab.presets.test.tsx src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx`
- [ ] `npx eslint src/contexts/SettingsContext.tsx src/contexts/settings/theme.ts src/contexts/settings/theme-presets.ts src/components/settings/tabs/AppearanceTab.tsx src/components/SimplePlayDashboard.tsx src/components/sidebar/ModloaderSection.tsx src/components/sidebar/ModpackSection.tsx`
- [ ] `npx tsc --noEmit`
- [ ] Manual appearance review confirms dark presets visibly repaint the launcher and preset changes no longer need runtime-heavy helper chrome
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Preset switching visibly affects real launcher surfaces instead of only internal tokens
- Preset switching no longer depends on an extra runtime card to compensate for weak visible repaint
</success_criteria>

<output>
After completion, create `.planning/phases/36-settings-predictability-and-shared-control-contract/36-06-SUMMARY.md`
</output>
