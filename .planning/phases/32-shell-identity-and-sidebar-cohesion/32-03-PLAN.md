---
phase: 32-shell-identity-and-sidebar-cohesion
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/assets/branding.ts
  - src/components/ui/ArtworkFallback.tsx
  - src/components/ui/LazyImage.tsx
  - src/components/layout/EmptyStateView.tsx
  - src/components/modpacks/ModpackList.tsx
  - src/components/modpacks/details/ModpackDetailsHeader.tsx
  - src/components/ui/__tests__/ArtworkFallback.policy.test.tsx
  - src/components/ui/__tests__/LazyImage.cache.test.tsx
  - src/components/layout/__tests__/EmptyStateView.branding.test.tsx
  - src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx
  - src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx
  - src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx
autonomous: true
requirements:
  - BRAND-01
user_setup: []
must_haves:
  truths:
    - Branding is appropriate where app identity is needed, but missing-content and empty/fallback surfaces must stay calm and content-first.
    - The existing `app-icon`, `product-mark`, and `media-fallback` role split should be reused rather than replaced with another brand-system rewrite.
    - Neutral fallback art must not regress into a launcher mark or other loud identity filler when remote content fails.
    - This plan owns fallback and empty-state restraint only; it must not absorb content-tab hierarchy or broader catalog work.
  artifacts:
    - src/app/assets/branding.ts
    - src/components/ui/ArtworkFallback.tsx
    - src/components/ui/LazyImage.tsx
    - src/components/layout/EmptyStateView.tsx
    - src/components/modpacks/ModpackList.tsx
    - src/components/modpacks/details/ModpackDetailsHeader.tsx
    - src/components/ui/__tests__/ArtworkFallback.policy.test.tsx
    - src/components/ui/__tests__/LazyImage.cache.test.tsx
    - src/components/layout/__tests__/EmptyStateView.branding.test.tsx
    - src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx
    - src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx
    - src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx
  key_links:
    - `docs/ru/product-feedback-2026-04-20.md` rejects loud branded placeholders and specifically calls out the cube-like fallback art as the wrong answer when real artwork is missing.
    - `32-RESEARCH.md` identifies `MEDIA_FALLBACK_SVG`, `EmptyStateView`, and the installed/local modpack icon override as the remaining repo-owned hotspots for `BRAND-01`.
    - `32-VALIDATION.md` requires both fallback-policy tests and empty-state tests to flip to the new restraint contract before Phase 32 can close.
---

<objective>
Make fallback surfaces content-first instead of brand-first.

Purpose: keep app identity deliberate while making missing-art and empty/fallback surfaces calm, neutral, and truthful.
Output: neutral fallback artwork, restrained empty-state composition, and regression coverage that prevents loud branded filler from returning.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ru/product-feedback-2026-04-20.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-shell-identity-and-sidebar-cohesion/32-RESEARCH.md
@.planning/phases/32-shell-identity-and-sidebar-cohesion/32-VALIDATION.md
@src/app/assets/branding.ts
@src/components/ui/ArtworkFallback.tsx
@src/components/ui/LazyImage.tsx
@src/components/layout/EmptyStateView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace loud fallback art and unify the missing-art rule across shell and modpack surfaces</name>
  <files>src/app/assets/branding.ts, src/components/ui/ArtworkFallback.tsx, src/components/ui/LazyImage.tsx, src/components/layout/EmptyStateView.tsx, src/components/modpacks/ModpackList.tsx, src/components/modpacks/details/ModpackDetailsHeader.tsx</files>
  <action>Revise the neutral fallback artwork, the empty-state composition, and the installed/local modpack icon override so missing content no longer collapses into launcher-branded filler or contradictory per-surface rules. Preserve the canonical app icon where explicit app identity is needed, but keep missing-art surfaces calm and consistent everywhere the user encounters them.</action>
  <verify>npx eslint src/app/assets/branding.ts src/components/ui/ArtworkFallback.tsx src/components/ui/LazyImage.tsx src/components/layout/EmptyStateView.tsx src/components/modpacks/ModpackList.tsx src/components/modpacks/details/ModpackDetailsHeader.tsx && npx tsc --noEmit</verify>
  <done>Fallback surfaces, empty states, and modpack artwork gaps now follow one content-first rule instead of mixing neutral and brand-heavy placeholders.</done>
</task>

<task type="auto">
  <name>Task 2: Flip fallback, modpack-artwork, and empty-state regression tests to the new restraint posture</name>
  <files>src/components/ui/__tests__/ArtworkFallback.policy.test.tsx, src/components/ui/__tests__/LazyImage.cache.test.tsx, src/components/layout/__tests__/EmptyStateView.branding.test.tsx, src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx, src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx, src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx</files>
  <action>Update the fallback proof seams so they assert one neutral placeholder rule across shared artwork, installed/local modpack art, remote browser cards, and empty-state composition rather than the older mixed contract. Keep one shell-facing sanity check so classic surfaces still use the app icon only where explicit app identity is appropriate.</action>
  <verify>npx vitest run src/components/ui/__tests__/ArtworkFallback.policy.test.tsx src/components/ui/__tests__/LazyImage.cache.test.tsx src/components/layout/__tests__/EmptyStateView.branding.test.tsx src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx && npx tsc --noEmit</verify>
  <done>The fallback contract is regression-protected across neutral placeholder routing, cache retry behavior, installed/local modpack artwork, remote browser artwork, and restrained shell-owned identity usage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/ui/__tests__/ArtworkFallback.policy.test.tsx src/components/ui/__tests__/LazyImage.cache.test.tsx src/components/layout/__tests__/EmptyStateView.branding.test.tsx src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx`
- [ ] `npx eslint src/app/assets/branding.ts src/components/ui/ArtworkFallback.tsx src/components/ui/LazyImage.tsx src/components/layout/EmptyStateView.tsx src/components/modpacks/ModpackList.tsx src/components/modpacks/details/ModpackDetailsHeader.tsx`
- [ ] `npx tsc --noEmit`
- [ ] Manual fallback review completed: missing artwork surfaces show one calm placeholder rule across installed, local, and remote modpack content instead of mixing launcher icons, launcher marks, or cube-heavy filler, and empty-state surfaces no longer read like a branded hero
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- FMCL keeps app identity deliberate while treating missing-content surfaces as product utility, not brand promotion
</success_criteria>

<output>
After completion, create `.planning/phases/32-shell-identity-and-sidebar-cohesion/32-03-SUMMARY.md`
</output>
