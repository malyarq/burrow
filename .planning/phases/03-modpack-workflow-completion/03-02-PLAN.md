---
phase: 03-modpack-workflow-completion
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/modpacks/ModpackList.tsx
  - src/components/modpacks/ModpackDetails.tsx
  - src/components/modpacks/details/ModpackDetailsActions.tsx
  - electron/services/instances/instanceService.ts
  - electron/services/modpacks/storage.ts
  - electron/services/instances/__tests__/instanceMetadataCrud.test.ts
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
user_setup: []
must_haves:
  truths:
    - Duplicate and rename are accessible from an explicit installed-card action surface instead of depending on right-click discovery or the details page.
    - List and details flows use consistent duplicate and rename behavior where those actions overlap.
    - Backend rename and duplicate preserve metadata truth so the new card actions do not surface stale names or stripped metadata.
  artifacts:
    - src/components/modpacks/ModpackList.tsx
    - src/components/modpacks/ModpackDetails.tsx
    - src/components/modpacks/details/ModpackDetailsActions.tsx
    - electron/services/instances/instanceService.ts
    - electron/services/modpacks/storage.ts
    - electron/services/instances/__tests__/instanceMetadataCrud.test.ts
  key_links:
    - Reuse the existing confirm, toast, and CRUD handler path instead of adding a parallel action stack for card actions.
    - Keep metadata truth in the same slice as the new UI exposure; Phase 3 should not ship more visible rename or duplicate actions on top of drift-prone backend behavior.
---

<objective>
Expose duplicate and rename from installed modpack cards and make those actions metadata-safe.

Purpose: finish the installed-modpack management workflow so users can rename or duplicate directly from the list surface without revealing backend metadata inconsistencies.
Output: explicit card actions, consistent action behavior across list and details, and backend tests proving rename and duplicate preserve metadata truth.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-modpack-workflow-completion/03-RESEARCH.md
@.planning/phases/03-modpack-workflow-completion/03-VALIDATION.md
@src/components/modpacks/ModpackList.tsx
@src/components/modpacks/ModpackDetails.tsx
@src/components/modpacks/details/ModpackDetailsActions.tsx
@src/contexts/instances/hooks/useInstanceCrudActions.ts
@src/services/ipc/modpacksIPC.ts
@electron/services/instances/instanceService.ts
@electron/services/modpacks/storage.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make duplicate and rename explicit list-card actions</name>
  <files>src/components/modpacks/ModpackList.tsx, src/components/modpacks/ModpackDetails.tsx, src/components/modpacks/details/ModpackDetailsActions.tsx</files>
  <action>Refactor the installed-modpack action surface so rename and duplicate are accessible from an explicit card affordance, such as visible buttons or an overflow action menu, rather than requiring right-click discovery. Reuse the existing confirm and toast flow where practical, and resolve the current list-vs-details behavior drift if the same action is still exposed in both places.</action>
  <verify>npx eslint src/components/modpacks/ModpackList.tsx src/components/modpacks/ModpackDetails.tsx src/components/modpacks/details/ModpackDetailsActions.tsx</verify>
  <done>Users can trigger duplicate and rename directly from the installed-modpack list surface without opening the details page.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve metadata truth across backend rename and duplicate flows</name>
  <files>electron/services/instances/instanceService.ts, electron/services/modpacks/storage.ts, electron/services/instances/__tests__/instanceMetadataCrud.test.ts</files>
  <action>Update the backend rename and duplicate behavior so metadata-facing name, description, icon, and source information remain correct after those operations. Keep the changes local to the existing instance and metadata services, and add focused tests that prove rename and duplicate preserve the intended metadata contract.</action>
  <verify>npx vitest run electron/services/instances/__tests__/instanceMetadataCrud.test.ts && npx eslint electron/services/instances/instanceService.ts electron/services/modpacks/storage.ts</verify>
  <done>The more visible card actions do not ship stale or stripped metadata after rename or duplicate completes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run electron/services/instances/__tests__/instanceMetadataCrud.test.ts`
- [ ] `npx eslint src/components/modpacks/ModpackList.tsx src/components/modpacks/ModpackDetails.tsx src/components/modpacks/details/ModpackDetailsActions.tsx`
- [ ] `npx eslint electron/services/instances/instanceService.ts electron/services/modpacks/storage.ts`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- FMCL exposes duplicate and rename from the installed-modpack list surface without regressing metadata truth or forcing the details view
</success_criteria>

<output>
After completion, create `.planning/phases/03-modpack-workflow-completion/03-02-SUMMARY.md`
</output>
