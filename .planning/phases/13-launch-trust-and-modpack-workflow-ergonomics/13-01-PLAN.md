---
phase: 13-launch-trust-and-modpack-workflow-ergonomics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/contracts/launcher.ts
  - src/features/launcher/services/launcherService.ts
  - src/features/launcher/hooks/useLauncherState.ts
  - src/features/launcher/hooks/useLauncherIPC.ts
  - src/features/launcher/hooks/useLauncher.ts
  - src/components/sidebar/LaunchControls.tsx
  - src/components/Sidebar.tsx
  - src/components/SimplePlayDashboard.tsx
  - src/components/sidebar/__tests__/LaunchControls.status.test.tsx
  - src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx
autonomous: true
requirements:
  - LAUNCH-01
  - LAUNCH-02
user_setup: []
must_haves:
  truths:
    - FMCL must stop modeling launch trust as one generic loading string; the main play surface needs explicit preparing, downloading, launching, waiting, running, and failed states.
    - Busy-state protection must block conflicting launch actions without turning every busy state into a restart-only flow or hiding the reason FMCL is still working.
    - The fix must reuse the existing launcher IPC, progress, and log seam instead of inventing a separate global activity-center subsystem.
    - This plan owns launch-state truth only; it must not absorb packaging, release-documentation, or broader Phase 14 verification work.
  artifacts:
    - shared/contracts/launcher.ts
    - src/features/launcher/services/launcherService.ts
    - src/features/launcher/hooks/useLauncherState.ts
    - src/features/launcher/hooks/useLauncherIPC.ts
    - src/features/launcher/hooks/useLauncher.ts
    - src/components/sidebar/LaunchControls.tsx
    - src/components/Sidebar.tsx
    - src/components/SimplePlayDashboard.tsx
  key_links:
    - Phase 13 research identified `useLauncher.ts`, `useLauncherIPC.ts`, `useLauncherState.ts`, `LaunchControls.tsx`, `Sidebar.tsx`, and `SimplePlayDashboard.tsx` as one renderer trust seam.
    - The backend already emits richer milestones through the launcher orchestration path, so execution should promote that truth into a stage-aware renderer contract rather than polishing vague copy.
---

<objective>
Make the launcher's active play state understandable and trustworthy.

Purpose: expose explicit launch stages, keep failures actionable, and prevent conflicting launch actions from feeling like a freeze or random button lockout.
Output: a stage-aware launch-state contract, honest busy and failure feedback on the main play surface, and focused regression coverage for the new behavior.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-launch-trust-and-modpack-workflow-ergonomics/13-RESEARCH.md
@.planning/phases/13-launch-trust-and-modpack-workflow-ergonomics/13-VALIDATION.md
@shared/contracts/launcher.ts
@src/features/launcher/services/launcherService.ts
@src/features/launcher/hooks/useLauncherState.ts
@src/features/launcher/hooks/useLauncherIPC.ts
@src/features/launcher/hooks/useLauncher.ts
@src/components/sidebar/LaunchControls.tsx
@src/components/Sidebar.tsx
@src/components/SimplePlayDashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish an explicit launch-stage contract and durable busy-state model</name>
  <files>shared/contracts/launcher.ts, src/features/launcher/services/launcherService.ts, src/features/launcher/hooks/useLauncherState.ts, src/features/launcher/hooks/useLauncherIPC.ts, src/features/launcher/hooks/useLauncher.ts</files>
  <action>Refine the renderer launch model so FMCL can distinguish preparing, downloading or provisioning, launching, waiting for the game process, running, and failed states instead of exposing one generic loading string. Route existing launcher events and logs through that model, preserve brownfield-safe compatibility with the current IPC seam, and make failure state durable enough that the user can still react after a failed attempt. Keep the work scoped to state truth and action safety, not to a new global download center.</action>
  <verify>npx eslint src/features/launcher/services/launcherService.ts src/features/launcher/hooks/useLauncherState.ts src/features/launcher/hooks/useLauncherIPC.ts src/features/launcher/hooks/useLauncher.ts && npx tsc --noEmit</verify>
  <done>The launcher exposes one truthful state model that later UI rendering can trust instead of inferring from one boolean and a status string.</done>
</task>

<task type="auto">
  <name>Task 2: Render stage-aware launch feedback on the main play surfaces and lock it with tests</name>
  <files>src/components/sidebar/LaunchControls.tsx, src/components/Sidebar.tsx, src/components/SimplePlayDashboard.tsx, src/components/sidebar/__tests__/LaunchControls.status.test.tsx, src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx</files>
  <action>Update the shared play controls and dashboard status presentation so users can tell whether FMCL is preparing, downloading, launching, waiting, running, or failed without opening logs. Make conflicting actions visibly unavailable while FMCL is busy, keep recovery obvious when launch fails, and add focused seam tests that prove the new state hierarchy instead of only snapshotting copy.</action>
  <verify>npx vitest run src/components/sidebar/__tests__/LaunchControls.status.test.tsx src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx</verify>
  <done>The main play surface communicates launch state truthfully and is regression-protected against sliding back into vague or spam-prone behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/sidebar/__tests__/LaunchControls.status.test.tsx src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx`
- [ ] `npx eslint src/features/launcher/services/launcherService.ts src/features/launcher/hooks/useLauncherState.ts src/features/launcher/hooks/useLauncherIPC.ts src/features/launcher/hooks/useLauncher.ts src/components/sidebar/LaunchControls.tsx src/components/Sidebar.tsx src/components/SimplePlayDashboard.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- FMCL main play surfaces now explain launch progress and keep conflicting actions safely gated without feeling frozen
</success_criteria>

<output>
After completion, create `.planning/phases/13-launch-trust-and-modpack-workflow-ergonomics/13-01-SUMMARY.md`
</output>
