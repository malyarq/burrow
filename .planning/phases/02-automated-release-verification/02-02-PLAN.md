---
phase: 02-automated-release-verification
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/utils/__tests__/format.test.ts
  - electron/services/sharing/__tests__/shareService.test.ts
  - electron/services/content/__tests__/contentManager.test.ts
autonomous: true
requirements:
  - TEST-02
user_setup: []
must_haves:
  truths:
    - Deterministic formatting behavior, share-code round trips, malformed share-code rejection, and content-store dedupe or cleanup behavior all fail fast in the automated suite when regressed.
    - The tests run without Electron boot or network access by using temp directories and narrow service stubs only.
    - This slice proves the new test lane has meaningful product coverage, not just framework smoke.
  artifacts:
    - src/utils/__tests__/format.test.ts
    - electron/services/sharing/__tests__/shareService.test.ts
    - electron/services/content/__tests__/contentManager.test.ts
  key_links:
    - `shareService` coverage should emphasize malformed-input and round-trip behavior because the roadmap explicitly calls that out for this phase.
    - `contentManager` tests should use real temp files instead of mocking `fs`, so later `modpackService` coverage has a stable pattern to follow.
---

<objective>
Add the cheapest high-value automated coverage in the new fast lane.

Purpose: cover the pure and low-dependency release-critical targets first so Phase 2 starts producing real regression signal quickly.
Output: Vitest suites for formatting helpers, share-code encode/decode behavior, and content-store filesystem behavior.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-automated-release-verification/02-RESEARCH.md
@.planning/phases/02-automated-release-verification/02-VALIDATION.md
@src/utils/format.ts
@electron/services/sharing/shareService.ts
@electron/services/content/contentManager.ts
@electron/services/instances/manifestManager.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cover formatting helper boundaries with deterministic assertions</name>
  <files>src/utils/__tests__/format.test.ts</files>
  <action>Add focused tests for `formatSize(...)` and `formatDate(...)`, including unknown or zero values, unit transitions, and a deterministic timestamp path. Keep locale-sensitive assertions stable and avoid overfitting to host-specific formatting noise.</action>
  <verify>npx vitest run src/utils/__tests__/format.test.ts</verify>
  <done>Formatting helper regressions are caught by fast deterministic tests instead of manual inspection.</done>
</task>

<task type="auto">
  <name>Task 2: Cover `shareService` round-trip and malformed-input behavior</name>
  <files>electron/services/sharing/__tests__/shareService.test.ts</files>
  <action>Test `generateShareCode(...)` and `resolveShareCode(...)` with narrow modpack-service stubs plus temp manifest fixtures. Cover valid round-trip behavior, unsupported versions, bad prefixes, invalid base64, and corrupted gzip payloads. Keep the tests at the public service surface and avoid invasive refactors unless a tiny seam is truly required.</action>
  <verify>npx vitest run electron/services/sharing/__tests__/shareService.test.ts</verify>
  <done>Share-code regressions now fail the automated suite for both success and failure paths.</done>
</task>

<task type="auto">
  <name>Task 3: Cover `contentManager` with temp-directory integration tests</name>
  <files>electron/services/content/__tests__/contentManager.test.ts</files>
  <action>Exercise hash calculation, store import, link-or-copy behavior, deduplication, stats, and cleanup using temporary directories and real files. Prefer outcome-based assertions over brittle implementation trivia, except where link-count behavior is an explicit contract of the code.</action>
  <verify>npx vitest run electron/services/content/__tests__/contentManager.test.ts</verify>
  <done>The content-store behavior that later modpack flows depend on is guarded by realistic fast tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/utils/__tests__/format.test.ts`
- [ ] `npx vitest run electron/services/sharing/__tests__/shareService.test.ts`
- [ ] `npx vitest run electron/services/content/__tests__/contentManager.test.ts`
- [ ] `npm test`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- FMCL has meaningful fast coverage for format utilities, share-code behavior, and content-store behavior
</success_criteria>

<output>
After completion, create `.planning/phases/02-automated-release-verification/02-02-SUMMARY.md`
</output>
