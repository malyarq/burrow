---
phase: 02-automated-release-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
  - tests/setup/vitest.setup.ts
  - tests/smoke/vitest.smoke.test.ts
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
autonomous: true
requirements:
  - TEST-01
user_setup:
  - Allow dependency installation for new devDependencies (`vitest`, `@testing-library/react`, `jsdom`, and any minimal supporting packages required by the final config).
must_haves:
  truths:
    - Maintainers can run a non-watch `npm test` command locally without invoking the heavy full-install harness.
    - The same fast test command is enforced anywhere CI or release automation builds artifacts, so local and remote verification do not drift.
    - The existing `test:full` scripts remain available for manual installation validation and are not replaced by the new fast lane.
  artifacts:
    - package.json
    - vitest.config.ts
    - tests/setup/vitest.setup.ts
    - tests/smoke/vitest.smoke.test.ts
    - .github/workflows/ci.yml
    - .github/workflows/release.yml
  key_links:
    - Wave 2 plans depend on this harness and should not each invent their own runner or setup layer.
    - The smoke proof only exists to show the lane executes; real product-behavior coverage lands in later plans.
---

<objective>
Establish the fast automated test lane for FMCL.

Purpose: give the repo a real `npm test` command and enforce it in automation before adding service-specific coverage.
Output: Vitest-based test harness, minimal smoke proof, and CI or release workflows that execute the same command maintainers run locally.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-automated-release-verification/02-RESEARCH.md
@.planning/phases/02-automated-release-verification/02-VALIDATION.md
@package.json
@.github/workflows/ci.yml
@.github/workflows/release.yml
@vite.config.ts
@tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and wire the Vitest foundation for this Vite + Electron repo</name>
  <files>package.json, package-lock.json, vitest.config.ts, tests/setup/vitest.setup.ts</files>
  <action>Add the minimal devDependencies and config needed for a Node-first Vitest suite that understands the repo's TypeScript and `@shared` alias setup. Keep the default command non-watch and brownfield-safe; do not redesign the full-install harness or pull in broad coverage tooling.</action>
  <verify>npm test -- --passWithNoTests</verify>
  <done>The repository has a working Vitest config and a stable `npm test` entrypoint that can execute in CI without interactive flags.</done>
</task>

<task type="auto">
  <name>Task 2: Add a minimal smoke proof and enforce the same command in automation</name>
  <files>tests/smoke/vitest.smoke.test.ts, .github/workflows/ci.yml, .github/workflows/release.yml, package.json</files>
  <action>Add the smallest real smoke test needed to prove the new fast lane executes, then wire CI and any release workflow that builds artifacts to run `npm test` before packaging. Preserve existing job ordering where practical and keep the workflows readable.</action>
  <verify>npm test && npx tsc --noEmit</verify>
  <done>Maintainers and automation both run the same fast test command, and the lane is proven by at least one real passing test.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test`
- [ ] `npx tsc --noEmit`
- [ ] CI and release workflow YAML both show the fast test lane before packaging or publish steps
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- FMCL has a real fast test harness and `npm test` command before service-level coverage begins
</success_criteria>

<output>
After completion, create `.planning/phases/02-automated-release-verification/02-01-SUMMARY.md`
</output>
