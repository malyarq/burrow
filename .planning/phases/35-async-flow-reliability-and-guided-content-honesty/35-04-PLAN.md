---
phase: 35-async-flow-reliability-and-guided-content-honesty
plan: "04"
type: execute
wave: 4
depends_on:
  - 35-01
  - 35-02
  - 35-03
files_modified:
  - src/contexts/ModpackContext.tsx
  - src/contexts/instances/hooks/useInstanceCrudActions.ts
  - src/components/modpacks/ModpackList.tsx
  - src/components/modpacks/ModpackDetails.tsx
  - src/features/modpacks/__tests__/modpackNavigationState.test.tsx
  - src/contexts/__tests__/ModpackContext.selection-stability.test.ts
  - src/verification/manual/views.ts
  - src/verification/manual/scenarios.tsx
  - src/verification/manual/__tests__/guidedContentProof.test.tsx
  - src/verification/manual/__tests__/views.test.ts
autonomous: true
requirements:
  - MODPACK-14
  - CONTENT-09
user_setup: []
must_haves:
  truths:
    - Active-modpack switching reaches the final selected config without obvious route-visible churn or duplicate refresh work.
    - Details launch and list selection reuse the stabilized selection path instead of layering extra refreshes on top.
    - Manual proof routes instruct reviewers to check fixed CTA rails, actionable failure explanations, runtime honesty, and active-modpack continuity.
    - Guided proof fixtures remain useful, but they no longer hide gaps in the live product contract.
  artifacts:
    - src/contexts/ModpackContext.tsx
    - src/contexts/instances/hooks/useInstanceCrudActions.ts
    - src/components/modpacks/ModpackList.tsx
    - src/components/modpacks/ModpackDetails.tsx
    - src/contexts/__tests__/ModpackContext.selection-stability.test.ts
    - src/verification/manual/views.ts
  key_links:
    - `src/contexts/instances/hooks/useInstanceCrudActions.ts` -> `src/contexts/ModpackContext.tsx` via active-modpack selection and config loading
    - `src/components/modpacks/ModpackList.tsx` -> `src/contexts/ModpackContext.tsx` via optimistic selection behavior and final reload path
    - `src/components/modpacks/ModpackDetails.tsx` -> `src/contexts/instances/hooks/useInstanceCrudActions.ts` via details launch selection path
    - `src/contexts/__tests__/ModpackContext.selection-stability.test.ts` -> `src/contexts/ModpackContext.tsx` via no-churn selection continuity assertions
    - `src/verification/manual/views.ts` -> `src/verification/manual/scenarios.tsx` via Phase 35 proof-route descriptions
---

<objective>
Stabilize active-modpack continuity and refresh the proof harness to the real Phase 35 contract.

Purpose: remove the remaining visible churn when changing the active modpack, then make sure future closeout checks review the actual async and guided-trust behaviors instead of older proof boundaries.
Output: one stable selection path plus manual proof routes that explicitly review fixed create/add CTAs, actionable recovery, guided runtime honesty, and no-flicker active selection.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ru/product-feedback-2026-04-20.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-async-flow-reliability-and-guided-content-honesty/35-RESEARCH.md
@.planning/phases/35-async-flow-reliability-and-guided-content-honesty/35-VALIDATION.md
@src/contexts/ModpackContext.tsx
@src/contexts/instances/hooks/useInstanceCrudActions.ts
@src/components/modpacks/ModpackList.tsx
@src/components/modpacks/ModpackDetails.tsx
@src/verification/manual/views.ts
@src/verification/manual/scenarios.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse active-modpack selection onto one continuity-safe path</name>
  <files>src/contexts/ModpackContext.tsx, src/contexts/instances/hooks/useInstanceCrudActions.ts, src/components/modpacks/ModpackList.tsx, src/components/modpacks/ModpackDetails.tsx, src/features/modpacks/__tests__/modpackNavigationState.test.tsx, src/contexts/__tests__/ModpackContext.selection-stability.test.ts</files>
  <action>Stabilize active-modpack selection so list selection and details launch no longer layer redundant refreshes and route-visible state churn on top of each other. Add a provider-level continuity seam that proves the final selected config arrives without obvious blank-state fallback or duplicate transition logic.</action>
  <verify>npx vitest run src/features/modpacks/__tests__/modpackNavigationState.test.tsx src/contexts/__tests__/ModpackContext.selection-stability.test.ts && npx eslint src/contexts/ModpackContext.tsx src/contexts/instances/hooks/useInstanceCrudActions.ts src/components/modpacks/ModpackList.tsx src/components/modpacks/ModpackDetails.tsx && npx tsc --noEmit</verify>
  <done>Active-modpack switching now follows one stable path instead of visibly bouncing through overlapping refresh work.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh the proof harness around Phase 35 async and guided trust criteria</name>
  <files>src/verification/manual/views.ts, src/verification/manual/scenarios.tsx, src/verification/manual/__tests__/guidedContentProof.test.tsx, src/verification/manual/__tests__/views.test.ts</files>
  <action>Rewrite the relevant proof-route descriptions so `modpack-create`, `modpack-add`, `modpack-add-modal`, and the guided content routes explicitly tell reviewers to check fixed CTA rails, actionable failure explanations, runtime honesty, and active-modpack continuity. Keep the proof routes aligned with the live contract rather than using legacy “route-owned action” wording as the success boundary.</action>
  <verify>npx vitest run src/verification/manual/__tests__/guidedContentProof.test.tsx src/verification/manual/__tests__/views.test.ts && npx eslint src/verification/manual/views.ts src/verification/manual/scenarios.tsx && npx tsc --noEmit</verify>
  <done>The manual proof hub now reviews the real Phase 35 trust contract instead of older async or guided-content proof boundaries.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/features/modpacks/__tests__/modpackNavigationState.test.tsx src/contexts/__tests__/ModpackContext.selection-stability.test.ts src/verification/manual/__tests__/guidedContentProof.test.tsx src/verification/manual/__tests__/views.test.ts`
- [ ] `npx eslint src/contexts/ModpackContext.tsx src/contexts/instances/hooks/useInstanceCrudActions.ts src/components/modpacks/ModpackList.tsx src/components/modpacks/ModpackDetails.tsx src/verification/manual/views.ts src/verification/manual/scenarios.tsx`
- [ ] `npx tsc --noEmit`
- [ ] Manual shell review completed: active-modpack switching no longer visibly flickers, and proof routes clearly communicate the Phase 35 contract
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Active-modpack switching feels stable, and future closeout work cannot review the wrong async/guided contract
</success_criteria>

<output>
After completion, create `.planning/phases/35-async-flow-reliability-and-guided-content-honesty/35-04-SUMMARY.md`
</output>
