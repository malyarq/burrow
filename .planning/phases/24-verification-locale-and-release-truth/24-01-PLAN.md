---
phase: 24-verification-locale-and-release-truth
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/verification/manual/ManualVerificationApp.tsx
  - src/verification/manual/views.ts
  - src/verification/manual/scenarios.tsx
  - src/verification/manual/mockEnvironment.ts
autonomous: true
requirements:
  - VER-01
user_setup: []
must_haves:
  truths:
    - Phase 24 starts by turning the existing `manual-verification.html` seam into a `v0.5.0` closeout registry instead of creating a new verification harness or leaving the current mixed historical view list in place.
    - The manual hub, navigation metadata, and readiness contract must all speak `v0.5.0` and explicit Phase 24 closeout ownership rather than stale `v0.4.0` or older phase labels.
    - Visible proof data must be deterministic before screenshot work lands: no `Date.now()`-driven timestamps, no ambient theme or locale drift, and no animation defaults that would make later baselines noisy by design.
    - This plan owns the closeout registry, hub copy, and deterministic fixture foundation only; it must not yet widen degraded-state proof or land the Playwright lane.
  artifacts:
    - src/verification/manual/ManualVerificationApp.tsx
    - src/verification/manual/views.ts
    - src/verification/manual/scenarios.tsx
    - src/verification/manual/mockEnvironment.ts
  key_links:
    - Research explicitly locked the existing `manual-verification.html` seam as the Phase 24 proof entry and rejected a second harness.
    - `src/verification/manual/ManualVerificationApp.tsx` and `src/verification/manual/views.ts` still present `v0.4.0` wording and a flat mixed registry, so Phase 24 needs a closeout-owned model first.
    - `src/verification/manual/mockEnvironment.ts` still includes visible time-based fixture data and incomplete proof seeding, which would make a screenshot lane flaky by design if left untouched.
---

<objective>
Establish the `v0.5.0` closeout registry and deterministic proof foundation on the existing manual seam.

Purpose: convert the current mixed manual hub into a named Phase 24 closeout entry that downstream screenshot and release-truth work can trust.
Output: a `v0.5.0` closeout registry, updated manual hub copy, and deterministic proof seeding for later closeout views.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-verification-locale-and-release-truth/24-CONTEXT.md
@.planning/phases/24-verification-locale-and-release-truth/24-RESEARCH.md
@.planning/phases/24-verification-locale-and-release-truth/24-VALIDATION.md
@src/verification/manual/ManualVerificationApp.tsx
@src/verification/manual/views.ts
@src/verification/manual/scenarios.tsx
@src/verification/manual/mockEnvironment.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the `v0.5.0` closeout registry and update the manual hub copy on the existing seam</name>
  <files>src/verification/manual/ManualVerificationApp.tsx, src/verification/manual/views.ts, src/verification/manual/scenarios.tsx</files>
  <action>Replace the flat mixed manual-view list with a Phase 24 closeout-owned registry on the existing seam. Keep legacy views available for historical use, but make the manual hub and navigation lead with a named `v0.5.0` closeout set that includes the planned `phase-24-home-closeout`, `phase-24-modpacks-closeout`, `phase-24-degraded-closeout`, `phase-24-theme-dark`, `phase-24-theme-light`, `phase-24-locale-en`, and `phase-24-locale-ru` ownership slots. Update the visible hub copy and readiness messaging to `v0.5.0` so reviewers no longer have to infer the active milestone from stale text.</action>
  <verify>git diff --check -- src/verification/manual/ManualVerificationApp.tsx src/verification/manual/views.ts src/verification/manual/scenarios.tsx && npx eslint src/verification/manual/ManualVerificationApp.tsx src/verification/manual/views.ts src/verification/manual/scenarios.tsx && npx tsc --noEmit</verify>
  <done>The manual verification entry clearly advertises a `v0.5.0` closeout registry on the existing seam instead of a stale mixed historical view list.</done>
</task>

<task type="auto">
  <name>Task 2: Make closeout proof fixtures deterministic and explicitly seeded for later screenshot capture</name>
  <files>src/verification/manual/scenarios.tsx, src/verification/manual/mockEnvironment.ts</files>
  <action>Remove visible time and motion nondeterminism from the manual proof layer so later screenshot baselines can trust the closeout registry. Replace visible `Date.now()`-style fixture values with fixed timestamps, make closeout view seeding set language, theme, preset, accent, and reduced-motion or animation-off defaults deliberately, and keep the readiness contract bound to deterministic shell-integrated mounts rather than ambient local storage or one-off manual steps.</action>
  <verify>npx vitest run src/contexts/settings/__tests__/themeRuntimeContract.test.ts src/utils/__tests__/format.test.ts src/components/settings/__tests__/AppearanceTab.state-fidelity.test.tsx && npx tsc --noEmit</verify>
  <done>The Phase 24 closeout seam is deterministic enough for explicit theme, locale, and screenshot proof work in later waves.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `git diff --check -- src/verification/manual/ManualVerificationApp.tsx src/verification/manual/views.ts src/verification/manual/scenarios.tsx src/verification/manual/mockEnvironment.ts`
- [ ] `npx eslint src/verification/manual/ManualVerificationApp.tsx src/verification/manual/views.ts src/verification/manual/scenarios.tsx src/verification/manual/mockEnvironment.ts`
- [ ] `npx vitest run src/contexts/settings/__tests__/themeRuntimeContract.test.ts src/utils/__tests__/format.test.ts src/components/settings/__tests__/AppearanceTab.state-fidelity.test.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 24 has a `v0.5.0` closeout registry and deterministic proof foundation on the existing manual seam
</success_criteria>

<output>
After completion, create `.planning/phases/24-verification-locale-and-release-truth/24-01-SUMMARY.md`
</output>
