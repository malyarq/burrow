---
phase: 01-release-baseline-and-trust-boundaries
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-02"
files_modified:
  - electron/ipc/validation/privilegedPayloads.ts
  - electron/ipc/handlers/accountHandlers.ts
  - electron/ipc/handlers/mirrorsHandlers.ts
  - electron/ipc/handlers/shareHandlers.ts
  - electron/ipc/handlers/settingsHandlers.ts
  - electron/ipc/handlers/modpacksHandlers.ts
autonomous: true
requirements:
  - SEC-01
user_setup: []
must_haves:
  truths:
    - Insecure remote HTTP auth servers and custom mirrors are blocked before the current request reaches service or network work, while explicit local exceptions remain available under the chosen policy.
    - Malformed share codes, unsafe settings path targets, and bad privileged payload shapes fail at the Electron edge with actionable recovery information before service or network work starts.
    - The ingress rules live in one reusable validator layer, so the same malformed payload is not accepted in one handler and rejected in another.
  artifacts:
    - electron/ipc/validation/privilegedPayloads.ts
    - electron/ipc/handlers/accountHandlers.ts
    - electron/ipc/handlers/mirrorsHandlers.ts
    - electron/ipc/handlers/shareHandlers.ts
    - electron/ipc/handlers/settingsHandlers.ts
    - electron/ipc/handlers/modpacksHandlers.ts
  key_links:
    - The shared validator helpers drive the handler decisions for account, mirror, share, settings, and modpack ingress instead of letting each handler drift.
    - The typed preload and wrapper surface from `01-08` stays thin because malformed payloads are rejected in main before service logic runs.
---

<objective>
Enforce the first real trust decisions at privileged ingress before persisted-state cleanup and deeper filesystem containment land.

Purpose: Phase 1 cannot claim safer defaults if malformed or unsafe privileged payloads still reach service logic first.
Output: Shared ingress validators plus hardened account, mirror, share, settings, and modpacks handler entry points that fail unsafe payloads early and consistently.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-CONTEXT.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-RESEARCH.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-VALIDATION.md
@electron/ipc/validation/privilegedPayloads.ts
@electron/ipc/handlers/accountHandlers.ts
@electron/ipc/handlers/mirrorsHandlers.ts
@electron/ipc/handlers/shareHandlers.ts
@electron/ipc/handlers/settingsHandlers.ts
@electron/ipc/handlers/modpacksHandlers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable validator helpers for endpoint URLs, bounded strings, path-like values, and share-code preflight</name>
  <files>electron/ipc/validation/privilegedPayloads.ts</files>
  <action>Centralize validation for URL trust policy, length-bounded strings, object shape checks, path-like inputs, and share-code sanity checks before decompression. Keep the helpers expressive enough for handler reuse, but do not silently coerce unsafe remote HTTP endpoints into allowed values.</action>
  <verify>npx eslint electron/ipc/validation/privilegedPayloads.ts</verify>
  <done>The main process has one reusable source of truth for the Phase 1 ingress rules instead of scattered ad hoc checks.</done>
</task>

<task type="auto">
  <name>Task 2: Reject malformed privileged payloads before handler work begins</name>
  <files>electron/ipc/handlers/accountHandlers.ts, electron/ipc/handlers/mirrorsHandlers.ts, electron/ipc/handlers/shareHandlers.ts, electron/ipc/handlers/settingsHandlers.ts, electron/ipc/handlers/modpacksHandlers.ts</files>
  <action>Apply the shared validators at handler ingress so bad account URLs, mirror URLs, share payloads, settings paths, and modpack-related privileged values fail before service methods or network calls start. Return actionable failures that the renderer can display without leaking raw internals by default.</action>
  <verify>npx eslint electron/ipc/handlers/accountHandlers.ts electron/ipc/handlers/mirrorsHandlers.ts electron/ipc/handlers/shareHandlers.ts electron/ipc/handlers/settingsHandlers.ts electron/ipc/handlers/modpacksHandlers.ts && npx tsc --noEmit</verify>
  <done>Unsafe payloads are rejected at the Electron edge, and the renderer receives recoverable failure information instead of post-hoc crashes or console-only errors.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx eslint electron/ipc/validation/privilegedPayloads.ts electron/ipc/handlers/accountHandlers.ts electron/ipc/handlers/mirrorsHandlers.ts electron/ipc/handlers/shareHandlers.ts electron/ipc/handlers/settingsHandlers.ts electron/ipc/handlers/modpacksHandlers.ts`
- [ ] `npx tsc --noEmit`
- [ ] Manual smoke covers invalid remote HTTP auth or mirror submission, malformed share input, and unsafe settings path input before any service work or reload-based recovery begins
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- FMCL stops accepting malformed or unsafe privileged payloads at handler ingress, and later persisted-state work can build on one consistent validator policy
</success_criteria>

<output>
After completion, create `.planning/phases/01-release-baseline-and-trust-boundaries/01-03-SUMMARY.md`
</output>
