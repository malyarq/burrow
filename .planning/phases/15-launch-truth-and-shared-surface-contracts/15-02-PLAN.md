---
phase: 15-launch-truth-and-shared-surface-contracts
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SimplePlayDashboard.tsx
  - src/components/sidebar/LaunchControls.tsx
  - src/components/sidebar/modpackRuntimeDependencies.ts
  - src/components/ui/LazyImage.tsx
  - src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx
autonomous: true
requirements:
  - LAUNCH-01
  - LAUNCH-02
user_setup: []
must_haves:
  truths:
    - Missing or invalid launch artwork on the main play surface must resolve to an intentional branded fallback rather than a browser-broken image.
    - The visible loader value on the classic screen must come from the same active launch configuration used by launch-affecting controls, not from a stale summary path.
    - Fallback art should reuse the existing bundled branding and fallback-aware image seams rather than introducing another special-case asset pipeline.
    - This plan owns the audited classic surface and shared launch controls only; it must not expand into catalog or modpack-detail fallback work.
  artifacts:
    - src/components/SimplePlayDashboard.tsx
    - src/components/sidebar/LaunchControls.tsx
    - src/components/sidebar/modpackRuntimeDependencies.ts
    - src/components/ui/LazyImage.tsx
  key_links:
    - Phase 15 research found parallel loader label logic in `SimplePlayDashboard.tsx` while other surfaces already use runtime dependency helpers, which is a likely source of `BUG-03`.
    - The discussion decisions require the launch fallback to feel calm, branded, and reusable later, which points to extending existing shared fallback seams instead of a one-off patch.
---

<objective>
Repair the audited launch surface so the user can trust what they see before clicking Play.

Purpose: replace the broken-art failure mode with a branded fallback and make the current-settings summary agree with the active launch configuration shown elsewhere on the classic screen.
Output: a deliberate launch hero fallback and one consistent visible loader summary on the main play surface.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-launch-truth-and-shared-surface-contracts/15-CONTEXT.md
@.planning/phases/15-launch-truth-and-shared-surface-contracts/15-RESEARCH.md
@.planning/phases/15-launch-truth-and-shared-surface-contracts/15-VALIDATION.md
@src/components/SimplePlayDashboard.tsx
@src/components/sidebar/LaunchControls.tsx
@src/components/sidebar/modpackRuntimeDependencies.ts
@src/components/ui/LazyImage.tsx
@src/app/assets/branding.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce a deliberate branded fallback for the launch hero state</name>
  <files>src/components/SimplePlayDashboard.tsx, src/components/ui/LazyImage.tsx</files>
  <action>Refactor the classic launch hero so missing or invalid artwork no longer leaks a broken browser image. Reuse bundled launcher branding and the existing fallback-aware image seam to create an intentional fallback that keeps the active pack or instance identity visible without surfacing technical error copy. Keep the fallback family reusable for later surfaces, but land only the audited main play screen in this phase.</action>
  <verify>npx eslint src/components/SimplePlayDashboard.tsx src/components/ui/LazyImage.tsx && npx tsc --noEmit</verify>
  <done>The main play surface shows a calm branded fallback instead of looking technically broken when artwork is unavailable.</done>
</task>

<task type="auto">
  <name>Task 2: Align current-settings loader truth with the active launch selection</name>
  <files>src/components/SimplePlayDashboard.tsx, src/components/sidebar/LaunchControls.tsx, src/components/sidebar/modpackRuntimeDependencies.ts, src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx</files>
  <action>Remove conflicting loader display paths so the classic dashboard summary, launch controls, and other launch-adjacent cues all reflect the same active configuration. Prefer shared loader display helpers over hard-coded label tables, and add or update focused dashboard tests so the summary cannot drift back to stale `Vanilla` or mismatched loader output while the current selection says otherwise.</action>
  <verify>npx vitest run src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx && npx eslint src/components/SimplePlayDashboard.tsx src/components/sidebar/LaunchControls.tsx src/components/sidebar/modpackRuntimeDependencies.ts && npx tsc --noEmit</verify>
  <done>The visible modloader value is truthful and consistent across the audited classic launch surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx`
- [ ] `npx eslint src/components/SimplePlayDashboard.tsx src/components/sidebar/LaunchControls.tsx src/components/sidebar/modpackRuntimeDependencies.ts src/components/ui/LazyImage.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The audited classic screen now has an intentional fallback treatment and one truthful modloader summary
</success_criteria>

<output>
After completion, create `.planning/phases/15-launch-truth-and-shared-surface-contracts/15-02-SUMMARY.md`
</output>
