---
phase: 05-accessibility-and-release-truthfulness
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Sidebar.tsx
  - src/components/modpacks/ModpackBrowser.tsx
  - src/components/modpacks/ModpackList.tsx
  - src/components/__tests__/Sidebar.keyboard.test.tsx
  - src/components/modpacks/__tests__/ModpackBrowser.a11y.test.tsx
  - src/components/modpacks/__tests__/ModpackList.keyboard.test.tsx
autonomous: true
requirements:
  - A11Y-01
  - A11Y-02
user_setup: []
must_haves:
  truths:
    - Keyboard-only users can reach and operate the launcher shell plus core modpack browse or installed-card workflows without relying on pointer-only context.
    - Core flow controls expose names, roles, and states that are understandable to assistive technologies instead of only iconography or layout.
    - Phase 3 and 4 workflow gains remain accessible after the new keyboard or ARIA pass rather than regressing into hidden actions.
  artifacts:
    - src/components/Sidebar.tsx
    - src/components/modpacks/ModpackBrowser.tsx
    - src/components/modpacks/ModpackList.tsx
    - src/components/__tests__/Sidebar.keyboard.test.tsx
    - src/components/modpacks/__tests__/ModpackBrowser.a11y.test.tsx
    - src/components/modpacks/__tests__/ModpackList.keyboard.test.tsx
  key_links:
    - Build on the shared modal and tab fixes from `05-01` when those seams are reused, but keep this plan focused on launcher and modpack workflows rather than every screen in the repo.
    - Reuse the existing browser and list interaction models from Phase 3 instead of redesigning navigation or card layouts for accessibility alone.
---

<objective>
Make FMCL's core launcher and modpack workflows truly keyboard-usable and assistive-technology friendly.

Purpose: satisfy the release requirement that users can operate the launcher shell, browse modpacks, and use installed-modpack actions without depending on pointer-first interactions.
Output: keyboard-complete launcher and modpack flow surfaces with focused renderer regression tests.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-accessibility-and-release-truthfulness/05-RESEARCH.md
@.planning/phases/05-accessibility-and-release-truthfulness/05-VALIDATION.md
@src/components/Sidebar.tsx
@src/components/modpacks/ModpackBrowser.tsx
@src/components/modpacks/ModpackList.tsx
@src/components/modpacks/__tests__/ModpackBrowser.history.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete keyboard and named-control behavior for launcher and modpack flow surfaces</name>
  <files>src/components/Sidebar.tsx, src/components/modpacks/ModpackBrowser.tsx, src/components/modpacks/ModpackList.tsx</files>
  <action>Add the missing landmark, label, and keyboard interaction behavior required for the launcher shell and modpack workflows to be honestly usable without a mouse. That includes icon-only shell controls, modpack browser result actions, history or favorites affordances, and installed-modpack card actions that still skew toward pointer usage or implicit context menus. Keep the work inside the current layouts and workflow model instead of redesigning the shell.</action>
  <verify>npx eslint src/components/Sidebar.tsx src/components/modpacks/ModpackBrowser.tsx src/components/modpacks/ModpackList.tsx && npx tsc --noEmit</verify>
  <done>Core FMCL launcher and modpack flows become keyboard-operable with explicit accessible names, roles, and states.</done>
</task>

<task type="auto">
  <name>Task 2: Protect launcher and modpack keyboard behavior with focused renderer tests</name>
  <files>src/components/__tests__/Sidebar.keyboard.test.tsx, src/components/modpacks/__tests__/ModpackBrowser.a11y.test.tsx, src/components/modpacks/__tests__/ModpackList.keyboard.test.tsx</files>
  <action>Add renderer tests that prove launcher shell actions, browser result controls, and installed-modpack card actions remain reachable and usable from the keyboard. Reuse the existing browser-history test seam where that reduces duplication, but extend coverage to the new semantics and keyboard paths introduced here.</action>
  <verify>npx vitest run src/components/__tests__/Sidebar.keyboard.test.tsx src/components/modpacks/__tests__/ModpackBrowser.history.test.tsx src/components/modpacks/__tests__/ModpackBrowser.a11y.test.tsx src/components/modpacks/__tests__/ModpackList.keyboard.test.tsx</verify>
  <done>Keyboard and named-control behavior for FMCL's core launcher and modpack flows is regression-tested instead of living only in manual smoke.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/__tests__/Sidebar.keyboard.test.tsx src/components/modpacks/__tests__/ModpackBrowser.history.test.tsx src/components/modpacks/__tests__/ModpackBrowser.a11y.test.tsx src/components/modpacks/__tests__/ModpackList.keyboard.test.tsx`
- [ ] `npx eslint src/components/Sidebar.tsx src/components/modpacks/ModpackBrowser.tsx src/components/modpacks/ModpackList.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- FMCL's launcher shell and modpack workflows are keyboard-usable and understandable to assistive technologies in real release paths
</success_criteria>

<output>
After completion, create `.planning/phases/05-accessibility-and-release-truthfulness/05-02-SUMMARY.md`
</output>
