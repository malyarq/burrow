---
phase: 33-classic-truth-and-catalog-density-repair
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Sidebar.tsx
  - src/components/SimplePlayDashboard.tsx
  - src/components/sidebar/modpackRuntimeDependencies.ts
  - src/locales/en.json
  - src/locales/ru.json
  - src/components/__tests__/Sidebar.classic-truth.test.tsx
autonomous: true
requirements:
  - MODPACK-07
user_setup: []
must_haves:
  truths:
    - Classic startup truth is already mostly fixed in the provider layer; this plan closes the remaining visible lie at the renderer surface.
    - The user complaint is not only about stale runtime data, but also about overloaded vanilla wording that makes the classic state feel untrustworthy even when the backing config is correct.
    - This plan owns classic-mode label truth and sidebar proof coverage only; it must not drift into modpack details or broader catalog cleanup.
    - The classic sidebar is the primary read-before-launch seam, so it needs its own regression test instead of relying on dashboard coverage alone.
  artifacts:
    - src/components/Sidebar.tsx
    - src/components/SimplePlayDashboard.tsx
    - src/components/sidebar/modpackRuntimeDependencies.ts
    - src/locales/en.json
    - src/locales/ru.json
    - src/components/__tests__/Sidebar.classic-truth.test.tsx
  key_links:
    - `docs/ru/product-feedback-2026-04-20.md` calls out stale classic runtime presentation and the overlong vanilla loader wording.
    - `33-RESEARCH.md` identifies `Sidebar.tsx` plus `modpackRuntimeDependencies.ts` as the remaining hotspot after Phase 32 hardened cold-start gating.
    - `33-VALIDATION.md` marks the dedicated sidebar truth seam as a Wave 0 gap for `MODPACK-07`.
---

<objective>
Make Classic mode read short and truthful on cold start.

Purpose: ensure the first version and loader labels a user sees in Classic match the persisted launch target and use restrained vanilla wording instead of overloaded explanatory text.
Output: a classic sidebar and launch summary that expose truthful runtime state with a dedicated sidebar regression seam.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ru/product-feedback-2026-04-20.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-classic-truth-and-catalog-density-repair/33-RESEARCH.md
@.planning/phases/33-classic-truth-and-catalog-density-repair/33-VALIDATION.md
@src/components/Sidebar.tsx
@src/components/SimplePlayDashboard.tsx
@src/components/sidebar/modpackRuntimeDependencies.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shorten classic runtime wording and remove any leftover stale-source fallback at the visible surface</name>
  <files>src/components/Sidebar.tsx, src/components/SimplePlayDashboard.tsx, src/components/sidebar/modpackRuntimeDependencies.ts, src/locales/en.json, src/locales/ru.json</files>
  <action>Make the classic sidebar and launch-summary surfaces read only from the authoritative persisted runtime state that Phase 32 already protects, and shorten the vanilla loader copy so it fits naturally without bracketed explanation. Keep the wording neutral and product-truthful instead of instructional.</action>
  <verify>npx vitest run src/contexts/__tests__/ModpackContext.startup-truth.test.ts src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx && npx eslint src/components/Sidebar.tsx src/components/SimplePlayDashboard.tsx src/components/sidebar/modpackRuntimeDependencies.ts && npx tsc --noEmit</verify>
  <done>Classic mode exposes the real version and loader on cold start, and vanilla wording no longer feels overloaded or misleading.</done>
</task>

<task type="auto">
  <name>Task 2: Add a dedicated classic sidebar truth seam</name>
  <files>src/components/__tests__/Sidebar.classic-truth.test.tsx, src/components/Sidebar.tsx</files>
  <action>Create a focused renderer regression test for the classic sidebar that proves persisted runtime truth and short vanilla wording survive a cold-start render. Make the seam fail on stale `1.12.2` fallback behavior or the older `Vanilla (no modloader)` style wording.</action>
  <verify>npx vitest run src/components/__tests__/Sidebar.classic-truth.test.tsx && npx tsc --noEmit</verify>
  <done>The sidebar has a narrow product-facing seam that protects truthful classic runtime presentation instead of assuming startup gating is enough.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/contexts/__tests__/ModpackContext.startup-truth.test.ts src/components/__tests__/SimplePlayDashboard.launch-state.test.tsx src/components/__tests__/Sidebar.classic-truth.test.tsx`
- [ ] `npx eslint src/components/Sidebar.tsx src/components/SimplePlayDashboard.tsx src/components/sidebar/modpackRuntimeDependencies.ts`
- [ ] `npx tsc --noEmit`
- [ ] Manual cold-start check completed: Classic opens with the persisted version and loader, and vanilla wording reads short enough to trust at a glance
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Classic mode no longer feels like it is guessing at runtime state during the first read-before-launch interaction
</success_criteria>

<output>
After completion, create `.planning/phases/33-classic-truth-and-catalog-density-repair/33-01-SUMMARY.md`
</output>
