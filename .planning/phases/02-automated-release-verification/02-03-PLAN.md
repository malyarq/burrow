---
phase: 02-automated-release-verification
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - electron/services/modpacks/__tests__/modpackService.test.ts
  - electron/services/modpacks/modpackService.ts
autonomous: true
requirements:
  - TEST-01
  - TEST-02
user_setup: []
must_haves:
  truths:
    - Release-critical `modpackService` behavior fails `npm test` when manifest-driven creation, override writing, or selected download/content-manager paths regress.
    - The tests stay fast and deterministic by mocking Electron, download, and platform-client seams rather than booting the app or calling the network.
    - Phase 2 closes by proving the new test lane coexists with the existing release gate instead of weakening it.
  artifacts:
    - electron/services/modpacks/__tests__/modpackService.test.ts
    - electron/services/modpacks/modpackService.ts
  key_links:
    - If a tiny seam extraction is required for testability, keep it local to `modpackService` and do not turn this plan into a service refactor.
    - Final verification must run the full repo gate with `npm test` added so the Phase 1 baseline stays intact.
---

<objective>
Finish Phase 2 with focused `modpackService` coverage and a full gate closeout.

Purpose: add automated signal around the heaviest remaining service target, then prove the repo's release checks still pass with the new fast test lane enabled.
Output: Vitest coverage for selected manifest/content/download paths in `modpackService` plus a green full verification gate.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-automated-release-verification/02-RESEARCH.md
@.planning/phases/02-automated-release-verification/02-VALIDATION.md
@electron/services/modpacks/modpackService.ts
@electron/services/content/contentManager.ts
@electron/services/download/downloadQueue.ts
@electron/services/sharing/shareService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cover manifest and override behavior in `modpackService`</name>
  <files>electron/services/modpacks/__tests__/modpackService.test.ts, electron/services/modpacks/modpackService.ts</files>
  <action>Add tests for the most deterministic high-value seams first: `updateModpackOverrides(...)`, `createFromManifest(...)`, and the `getContentStats()` / `cleanupContent()` passthrough behavior. Use temporary roots and narrow stubs for collaborators. Only adjust production code if a tiny, local seam extraction is necessary for stable testing.</action>
  <verify>npx vitest run electron/services/modpacks/__tests__/modpackService.test.ts --testNamePattern=\"override|manifest|content\"</verify>
  <done>`modpackService` regressions in manifest-driven local creation and override handling fail quickly in the automated suite.</done>
</task>

<task type="auto">
  <name>Task 2: Cover selected download-path branches without booting Electron</name>
  <files>electron/services/modpacks/__tests__/modpackService.test.ts, electron/services/modpacks/modpackService.ts</files>
  <action>Extend the suite to cover representative `installModsFromManifest(...)` branches by mocking `electron.app`, `downloadQueue`, `DownloadManager`, and the platform-service clients. Exercise at least one stored-content path and one download path so the tests guard the release-critical orchestration logic without becoming networked integration tests.</action>
  <verify>npx vitest run electron/services/modpacks/__tests__/modpackService.test.ts</verify>
  <done>The automated suite now covers the highest-risk `modpackService` behaviors named by this phase without relying on runtime installation tests.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run the full repo gate with `npm test` included</name>
  <files>electron/services/modpacks/__tests__/modpackService.test.ts, electron/services/modpacks/modpackService.ts</files>
  <action>Run and fix any fallout needed for the repo-wide verification path now that the fast test lane exists. Do not broaden scope into unrelated cleanup; only resolve issues that directly block the Phase 2 gate.</action>
  <verify>npm test && npm run lint && npx tsc --noEmit && npm run contracts:check && npm run ipc:check && npm run build -- --publish never</verify>
  <done>Phase 2 finishes with a green automated lane and the existing release gate still intact.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run electron/services/modpacks/__tests__/modpackService.test.ts`
- [ ] `npm test`
- [ ] `npm run lint`
- [ ] `npx tsc --noEmit`
- [ ] `npm run contracts:check`
- [ ] `npm run ipc:check`
- [ ] `npm run build -- --publish never`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- FMCL's fast automated lane catches regressions in the core services named by Phase 2 while preserving the existing release baseline
</success_criteria>

<output>
After completion, create `.planning/phases/02-automated-release-verification/02-03-SUMMARY.md`
</output>
