---
phase: 07-ui-system-foundations
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - src/contexts/settings/theme.ts
  - src/contexts/SettingsContext.tsx
  - src/components/settings/tabs/AppearanceTab.tsx
  - src/components/settings/__tests__/AppearanceTab.i18n.test.tsx
  - src/index.css
  - src/locales/en.json
  - src/locales/ru.json
  - src/contexts/settings/__tests__/themeDocument.test.ts
autonomous: true
requirements:
  - THEME-01
user_setup: []
must_haves:
  truths:
    - One document-level theme and accent contract drives the launcher shell and shared surfaces instead of a mix of persisted state and hardcoded color exceptions.
    - Appearance settings expose only honest, supported controls and visibly affect the shared launcher foundations when changed.
    - Any touched settings copy remains locale-safe in both EN and RU without expanding this plan into the full milestone localization sweep.
  artifacts:
    - src/contexts/settings/theme.ts
    - src/contexts/SettingsContext.tsx
    - src/components/settings/tabs/AppearanceTab.tsx
    - src/components/settings/__tests__/AppearanceTab.i18n.test.tsx
    - src/index.css
    - src/locales/en.json
    - src/locales/ru.json
    - src/contexts/settings/__tests__/themeDocument.test.ts
  key_links:
    - Do not broaden this slice into a route-by-route theme rollout; Phase 7 only needs visible payoff on shared foundations and appearance settings.
    - Remove or fence legacy appearance branches that no longer represent real behavior rather than preserving dead UI for compatibility theater.
---

<objective>
Make theme and accent behavior visibly authoritative on FMCL foundations.

Purpose: ensure appearance settings are no longer a partial or cosmetic promise.
Output: a document-level theme contract, aligned appearance controls, and focused regression coverage for the shared theme seam.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-ui-system-foundations/07-RESEARCH.md
@.planning/phases/07-ui-system-foundations/07-VALIDATION.md
@src/contexts/settings/theme.ts
@src/contexts/SettingsContext.tsx
@src/components/settings/tabs/AppearanceTab.tsx
@src/index.css
@src/locales/en.json
@src/locales/ru.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden the shared theme or accent document contract</name>
  <files>src/contexts/settings/theme.ts, src/contexts/SettingsContext.tsx, src/index.css</files>
  <action>Make the existing theme seam the clear source of truth for shared launcher foundations. Remove or collapse styling exceptions and legacy branches that make appearance settings behave differently from the actual shell contract, and keep the implementation centered on the current CSS-variable approach instead of introducing a new state layer.</action>
  <verify>npx eslint src/contexts/settings/theme.ts src/contexts/SettingsContext.tsx src/components/settings/tabs/AppearanceTab.tsx && npx tsc --noEmit</verify>
  <done>Theme and accent changes are applied through one shared contract that foundational surfaces can trust.</done>
</task>

<task type="auto">
  <name>Task 2: Align appearance settings with the supported theme system and add regression coverage</name>
  <files>src/components/settings/tabs/AppearanceTab.tsx, src/components/settings/__tests__/AppearanceTab.i18n.test.tsx, src/locales/en.json, src/locales/ru.json, src/contexts/settings/__tests__/themeDocument.test.ts</files>
  <action>Update the appearance tab so it accurately represents the supported foundation-level theme and accent model, and keep any new or changed UI copy mirrored in both locale files. Add focused regression coverage for both the document theme application seam and the touched appearance-copy contract so Phase 7 can actually prove locale-safe settings changes instead of assuming them.</action>
  <verify>npx vitest run src/contexts/settings/__tests__/themeDocument.test.ts src/components/settings/__tests__/AppearanceTab.i18n.test.tsx && npx eslint src/contexts/settings/theme.ts src/contexts/SettingsContext.tsx src/components/settings/tabs/AppearanceTab.tsx && npx tsc --noEmit</verify>
  <done>Appearance settings have honest controls, locale-safe copy, and automated proof that the shared theme contract and touched copy still behave correctly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/contexts/settings/__tests__/themeDocument.test.ts src/components/settings/__tests__/AppearanceTab.i18n.test.tsx`
- [ ] `npx eslint src/contexts/settings/theme.ts src/contexts/SettingsContext.tsx src/components/settings/tabs/AppearanceTab.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- FMCL’s appearance settings and foundational shell now agree on real theme and accent behavior
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-system-foundations/07-03-SUMMARY.md`
</output>
