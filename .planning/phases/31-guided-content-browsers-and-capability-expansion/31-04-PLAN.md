---
phase: 31-guided-content-browsers-and-capability-expansion
plan: "04"
type: execute
wave: 4
depends_on:
  - 31-03
files_modified:
  - src/components/modpacks/AddModPage.tsx
  - src/locales/en.json
  - src/locales/ru.json
  - src/components/modpacks/__tests__/GuidedContentFallback.test.tsx
  - src/components/modpacks/__tests__/AddModFlow.async-recovery.test.tsx
  - src/components/modpacks/__tests__/AddModPage.layout.test.tsx
autonomous: true
requirements:
  - CONTENT-01
  - CONTENT-04
user_setup: []
must_haves:
  truths:
    - Users can still choose local `.zip` import when guided browsing is not the right fit, but that fallback now lives inside the guided route instead of reclaiming the primary CTA on parent surfaces.
    - Fallback actions must stay explicit, type-aware, and instance-scoped so the route feels like one coherent add-content workflow rather than a hidden return to the legacy file-picker model.
    - Guided browsing remains primary in the hierarchy even after local import is restored as an explicit fallback path.
  artifacts:
    - src/components/modpacks/AddModPage.tsx
    - src/locales/en.json
    - src/locales/ru.json
    - src/components/modpacks/__tests__/GuidedContentFallback.test.tsx
    - src/components/modpacks/__tests__/AddModFlow.async-recovery.test.tsx
    - src/components/modpacks/__tests__/AddModPage.layout.test.tsx
  key_links:
    - `31-RESEARCH.md` calls current local import behavior inverted: primary on the dashboard, absent as an explicit fallback inside the guided route.
    - `31-VALIDATION.md` keeps `CONTENT-04` separate from canonical entry so fallback affordances do not disappear or regain top-level dominance by accident.
---

<objective>
Embed explicit local-file fallback inside the guided route.

Purpose: satisfy `CONTENT-04` without reopening the legacy parent-surface file-picker model as the primary resource-pack or shader path.
Output: the guided browser owns both remote browsing and explicit local `.zip` import fallback.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-guided-content-browsers-and-capability-expansion/31-RESEARCH.md
@.planning/phases/31-guided-content-browsers-and-capability-expansion/31-VALIDATION.md
@src/components/modpacks/AddModPage.tsx
@src/components/modpacks/__tests__/GuidedContentFallback.test.tsx
@src/components/modpacks/__tests__/AddModFlow.async-recovery.test.tsx
@src/components/modpacks/__tests__/AddModPage.layout.test.tsx
@src/locales/en.json
@src/locales/ru.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit resource-pack and shader local-import fallback inside the guided browser</name>
  <files>src/components/modpacks/AddModPage.tsx, src/locales/en.json, src/locales/ru.json, src/components/modpacks/__tests__/GuidedContentFallback.test.tsx, src/components/modpacks/__tests__/AddModFlow.async-recovery.test.tsx, src/components/modpacks/__tests__/AddModPage.layout.test.tsx</files>
  <action>Expose resource-pack and shader local `.zip` import actions inside the guided browser itself, backed by the structured outcome contracts from `31-01` and the non-mod finalize truth from `31-02`. Keep the fallback wording explicit and secondary to the browse path, ensure the fallback stays instance-scoped, and do not reintroduce direct parent-surface file-picker buttons as the default entry.</action>
  <verify><automated>npx vitest run src/components/modpacks/__tests__/GuidedContentFallback.test.tsx src/components/modpacks/__tests__/AddModFlow.async-recovery.test.tsx src/components/modpacks/__tests__/AddModPage.layout.test.tsx && npx tsc --noEmit</automated></verify>
  <done>The guided route now owns both remote browsing and local import fallback, satisfying the in-app-primary plus local-fallback product contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/modpacks/__tests__/GuidedContentFallback.test.tsx src/components/modpacks/__tests__/AddModFlow.async-recovery.test.tsx src/components/modpacks/__tests__/AddModPage.layout.test.tsx`
- [ ] `npx tsc --noEmit`
- [ ] Manual walkthrough completed: local import is visible and usable inside the guided resource-pack and shader flows without replacing the guided route as the primary entry
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Local import remains available as an explicit fallback inside the guided route instead of as a competing parent-surface primary path
</success_criteria>

<output>
After completion, create `.planning/phases/31-guided-content-browsers-and-capability-expansion/31-04-SUMMARY.md`
</output>
