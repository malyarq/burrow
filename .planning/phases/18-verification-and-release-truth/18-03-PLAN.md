---
phase: 18-verification-and-release-truth
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - README.md
  - docs/en/roadmap.md
  - docs/ru/roadmap.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - LAUNCH-01
  - LAUNCH-02
  - LAUNCH-03
  - LAUNCH-04
  - DETAIL-01
  - DETAIL-02
  - DETAIL-03
  - CATALOG-01
  - CATALOG-02
  - CATALOG-03
  - SET-01
  - SET-02
user_setup: []
must_haves:
  truths:
    - Release-facing docs and planning docs must be refreshed from the recorded Phase 15 to 17 proof, not from midpoint assumptions.
    - This plan owns only the sections that currently lie about `v0.4.0`, including public walkthrough status, active phase progress, and the stale pending state for `SET-01` and `SET-02`.
    - Workflow metadata drift should be fixed only as far as milestone closeout tooling needs it; do not rewrite archived history or normalize every summary file in the repo without proof it matters.
    - Phase 18 still must not claim the milestone is shipped until the final repo and packaging gate succeeds.
  artifacts:
    - README.md
    - docs/en/roadmap.md
    - docs/ru/roadmap.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
  key_links:
    - README still describes the current verified release as `v0.3.0`.
    - The public roadmap pages still say the active walkthrough only covers `manual-verification.html?view=dashboard` and still speak as if only Phase 15 is complete.
    - `.planning/REQUIREMENTS.md` still leaves `SET-01` and `SET-02` pending, and `gsd-tools init milestone-op` still reports stale `v0.2` metadata.
---

<objective>
Align release truth and planning truth to the recorded `v0.4.0` evidence.

Purpose: make the public and planning-facing story tell the truth about what Phases 15 to 17 shipped and what Phase 18 has already proven, without prematurely claiming final milestone closure.
Output: refreshed docs and planning state that match the current proof set, plus the minimum workflow metadata cleanup needed for a credible Phase 18 finish.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-verification-and-release-truth/18-RESEARCH.md
@.planning/phases/18-verification-and-release-truth/18-VALIDATION.md
@.planning/phases/18-verification-and-release-truth/18-02-PLAN.md
@README.md
@docs/en/roadmap.md
@docs/ru/roadmap.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Roll public docs and active planning docs forward from the Phase 15 to 17 proof set</name>
  <files>README.md, docs/en/roadmap.md, docs/ru/roadmap.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>Refresh the public roadmap pages, README, and active planning files so they describe the actual `v0.4.0` state after Phases 15 to 17 and the current Phase 18 proof set. Update the walkthrough language to reference all three verification views, mark `SET-01` and `SET-02` complete, and keep the milestone status honest by stopping short of "shipped" until the final gate passes.</action>
  <verify>rg -n 'manual-verification\\.html\\?view=(dashboard|modpack-details|phase-17-polish)|Phase 17|v0\\.4\\.0' README.md docs/en/roadmap.md docs/ru/roadmap.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md && rg -n 'SET-01 \\| Phase 17 \\| Complete|SET-02 \\| Phase 17 \\| Complete' .planning/REQUIREMENTS.md && ! rg -n 'The current `v0\\.3\\.0` UX-hardening release|Status: active, with Phase 15 complete|Статус: активен, Phase 15 завершена' README.md docs/en/roadmap.md docs/ru/roadmap.md</verify>
  <done>Public and planning docs stop understating the shipped `v0.4.0` proof set and the requirements traceability becomes truthful.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve workflow-truth drift only as far as closeout tooling requires</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <action>Rerun the milestone-op initialization and inspect any remaining drift in milestone version, progress, or phase-truth extraction. Normalize only the minimum active planning state or metadata required so the closeout tooling reports the live `v0.4.0` milestone honestly. Do not rewrite archived milestone content or every legacy summary unless the tool output proves they block Phase 18 closure.</action>
  <verify>node -e "const { execFileSync } = require('node:child_process'); const out = execFileSync('node', [process.env.HOME + '/.codex/get-shit-done/bin/gsd-tools.cjs', 'init', 'milestone-op'], { encoding: 'utf8' }); const data = JSON.parse(out); if (data.milestone_version !== 'v0.4' || data.completed_phases < 17) process.exit(1);" && npx tsc --noEmit</verify>
  <done>The active planning layer reports the current milestone truth closely enough that Phase 18 closeout and archive steps can rely on it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n 'manual-verification\\.html\\?view=(dashboard|modpack-details|phase-17-polish)|Phase 17|v0\\.4\\.0' README.md docs/en/roadmap.md docs/ru/roadmap.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md`
- [ ] `rg -n 'SET-01 \\| Phase 17 \\| Complete|SET-02 \\| Phase 17 \\| Complete' .planning/REQUIREMENTS.md`
- [ ] `! rg -n 'The current `v0\\.3\\.0` UX-hardening release|Status: active, with Phase 15 complete|Статус: активен, Phase 15 завершена' README.md docs/en/roadmap.md docs/ru/roadmap.md`
- [ ] `node -e "const { execFileSync } = require('node:child_process'); const out = execFileSync('node', [process.env.HOME + '/.codex/get-shit-done/bin/gsd-tools.cjs', 'init', 'milestone-op'], { encoding: 'utf8' }); const data = JSON.parse(out); if (data.milestone_version !== 'v0.4' || data.completed_phases < 17) process.exit(1);"`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Public docs and active planning state tell the truth about shipped `v0.4.0` behavior and remaining Phase 18 work
</success_criteria>

<output>
After completion, create `.planning/phases/18-verification-and-release-truth/18-03-SUMMARY.md`
</output>
