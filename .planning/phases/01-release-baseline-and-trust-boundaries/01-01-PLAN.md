---
phase: 01-release-baseline-and-trust-boundaries
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/BackgroundLayer.tsx
  - src/features/accounts/AccountsPage.tsx
  - src/features/accounts/AddAccountDialog.tsx
  - src/features/share/ShareModal.tsx
  - src/features/share/ImportShareModal.tsx
  - src/components/settings/tabs/StorageTab.tsx
  - src/contexts/ConfirmContext.tsx
  - src/components/ui/ConfirmDialog.tsx
  - src/locales/en.json
  - src/locales/ru.json
autonomous: true
requirements:
  - REL-01
user_setup:
  - Install workspace dependencies with `npm ci` before running verification if `node_modules/` is absent.
  - Prepare the Phase 1 manual abuse fixtures before automated verification: insecure auth URL, insecure mirror URL, traversal archive, unsafe settings path, unfamiliar external domain, and the warning-only renderer hotspots tracked in `01-VALIDATION.md`.
must_haves:
  truths:
    - Users can switch backgrounds, manage accounts, share or import data, and clean storage without hook-order crashes, stale-effect regressions, reload-based recovery, or browser-native confirmations interrupting the flow.
    - When FMCL blocks invalid account input, bad share input, or blocked storage cleanup, the user stays on the current screen with a localized explanation and an immediate retry path.
    - Core renderer instability is removed from the Phase 1 critical path so later Electron hardening failures are not masked by pre-existing React behavior bugs.
  artifacts:
    - src/components/layout/BackgroundLayer.tsx
    - src/features/accounts/AccountsPage.tsx
    - src/features/accounts/AddAccountDialog.tsx
    - src/features/share/ShareModal.tsx
    - src/features/share/ImportShareModal.tsx
    - src/components/settings/tabs/StorageTab.tsx
  key_links:
    - Accounts, share, and storage flows reuse the same in-app confirmation and inline error pattern so blocked actions stay consistent across the renderer.
    - Hook and effect fixes in background, accounts, and storage keep the retry UI stable while async reloads or refreshes are in flight.
---

<objective>
Re-establish the core renderer baseline in the exact Phase 1 hotspots already failing lint or falling back to brittle recovery behavior.

Purpose: Later Electron hardening is only trustworthy if the affected UI flows already render deterministically and keep users in-app when an action is rejected.
Output: Stabilized renderer flows for background, accounts, share, and storage, plus localized retry and confirmation UX for blocked actions in those primary release-critical paths.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-CONTEXT.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-RESEARCH.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-VALIDATION.md
@docs/KNOWN_ISSUES.md
@src/components/layout/BackgroundLayer.tsx
@src/features/accounts/AccountsPage.tsx
@src/features/accounts/AddAccountDialog.tsx
@src/features/share/ShareModal.tsx
@src/features/share/ImportShareModal.tsx
@src/components/settings/tabs/StorageTab.tsx
@src/contexts/ConfirmContext.tsx
@src/components/ui/ConfirmDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair hook ordering and effect dependency regressions in release-critical renderer flows</name>
  <files>src/components/layout/BackgroundLayer.tsx, src/features/accounts/AccountsPage.tsx, src/components/settings/tabs/StorageTab.tsx</files>
  <action>Move every hook call above early returns, convert async loaders into stable callbacks that satisfy effect dependencies, and keep background memoization outside conditional branches. Preserve current visuals and data flow; this slice is about deterministic React behavior, not redesigning the screens.</action>
  <verify>npx eslint src/components/layout/BackgroundLayer.tsx src/features/accounts/AccountsPage.tsx src/components/settings/tabs/StorageTab.tsx</verify>
  <done>`BackgroundLayer`, `AccountsPage`, and `StorageTab` no longer violate hook-order or stale-effect rules, and the same user-visible flows render without runtime reference hazards.</done>
</task>

<task type="auto">
  <name>Task 2: Replace brittle blocked-action recovery with inline retryable FMCL UX in accounts and share flows</name>
  <files>src/features/accounts/AccountsPage.tsx, src/features/accounts/AddAccountDialog.tsx, src/features/share/ShareModal.tsx, src/features/share/ImportShareModal.tsx, src/components/settings/tabs/StorageTab.tsx, src/contexts/ConfirmContext.tsx, src/components/ui/ConfirmDialog.tsx, src/locales/en.json, src/locales/ru.json</files>
  <action>Replace browser `confirm`, reload-based recovery, and toast-only failure handling in the accounts, share, and storage cleanup flows with localized inline errors plus the existing FMCL confirmation primitives. Keep the user in place with a correction path instead of forcing page reloads or browser-native prompts.</action>
  <verify>npx eslint src/features/accounts/AccountsPage.tsx src/features/accounts/AddAccountDialog.tsx src/features/share/ShareModal.tsx src/features/share/ImportShareModal.tsx src/components/settings/tabs/StorageTab.tsx src/contexts/ConfirmContext.tsx src/components/ui/ConfirmDialog.tsx</verify>
  <done>Users can correct invalid input or retry a blocked action in-place across accounts, share, and storage cleanup without reloading the app or dropping into browser-native dialogs.</done>
</task>

<task type="auto">
  <name>Task 3: Stabilize lint and type fallout inside the core baseline slice</name>
  <files>src/components/layout/BackgroundLayer.tsx, src/features/accounts/AccountsPage.tsx, src/features/accounts/AddAccountDialog.tsx, src/features/share/ShareModal.tsx, src/features/share/ImportShareModal.tsx, src/components/settings/tabs/StorageTab.tsx, src/locales/en.json, src/locales/ru.json</files>
  <action>Resolve lint and type fallout inside the touched files only, including replacing newly exposed `any` usage if it blocks this slice. Keep scope disciplined: no mirror, modpack-list, appearance, or preload cleanup in this plan.</action>
  <verify>npx eslint src/components/layout/BackgroundLayer.tsx src/features/accounts/AccountsPage.tsx src/features/accounts/AddAccountDialog.tsx src/features/share/ShareModal.tsx src/features/share/ImportShareModal.tsx src/components/settings/tabs/StorageTab.tsx && npx tsc --noEmit</verify>
  <done>The primary renderer baseline files are green, and any remaining repo-wide warning signal is isolated to later planned slices instead of hidden inside the core release-critical paths.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx eslint src/components/layout/BackgroundLayer.tsx src/features/accounts/AccountsPage.tsx src/features/accounts/AddAccountDialog.tsx src/features/share/ShareModal.tsx src/features/share/ImportShareModal.tsx src/components/settings/tabs/StorageTab.tsx`
- [ ] `npx tsc --noEmit`
- [ ] Manual smoke of background switching, account add/remove/select, share export/import, and storage cleanup confirms blocked actions keep the user in a retryable in-app state
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Phase 1 core renderer surfaces stop masking later trust-boundary regressions with pre-existing UI failures or brittle recovery behavior
</success_criteria>

<output>
After completion, create `.planning/phases/01-release-baseline-and-trust-boundaries/01-01-SUMMARY.md`
</output>
