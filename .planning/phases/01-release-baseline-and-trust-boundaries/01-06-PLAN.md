---
phase: 01-release-baseline-and-trust-boundaries
plan: "06"
type: execute
wave: 3
depends_on:
  - "01-04"
files_modified:
  - electron/services/modpacks/importers/localInstaller.ts
  - electron/services/instances/importer/InstanceImporterService.ts
  - electron/services/modpacks/modpackService.ts
autonomous: true
requirements:
  - SEC-02
user_setup: []
must_haves:
  truths:
    - Archives with `..`, absolute paths, or unsafe rewrite targets are blocked or explicitly confirmed before FMCL writes files.
    - Clean imports still land under the chosen launcher root instead of failing generically after containment is added.
    - Backup, export, override, and import paths derived during these flows stay inside approved launcher locations.
  artifacts:
    - electron/services/modpacks/importers/localInstaller.ts
    - electron/services/instances/importer/InstanceImporterService.ts
    - electron/services/modpacks/modpackService.ts
  key_links:
    - Archive extraction and modpack service writes reuse the same containment rules as the handler and content-service layers instead of inventing a special-case path policy.
    - Blocked archive or import targets return clean failures that the Wave 1 retry UI can surface without forcing a reload or silent partial import.
---

<objective>
Contain the archive and import surfaces so crafted entries or rewrite targets cannot escape the launcher root during modpack or instance ingestion.

Purpose: Phase 1 does not meet `SEC-02` if archive traversal and import-time path rewrites still bypass the new path-guard foundation.
Output: Hardened archive extraction and modpack import services that block unsafe entries early and keep all write destinations inside approved launcher roots.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-CONTEXT.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-RESEARCH.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-VALIDATION.md
@electron/security/pathGuards.ts
@electron/services/modpacks/importers/localInstaller.ts
@electron/services/instances/importer/InstanceImporterService.ts
@electron/services/modpacks/modpackService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard archive extraction and entry resolution before write-time expansion</name>
  <files>electron/services/modpacks/importers/localInstaller.ts, electron/services/instances/importer/InstanceImporterService.ts</files>
  <action>Validate archive entry paths, manifest-derived destinations, and override rewrites before extraction or copy begins. Reject traversal, absolute paths, and unsafe per-entry destinations by default, and only allow an explicit confirmation path when the Phase 1 context says trust is genuinely ambiguous rather than clearly unsafe.</action>
  <verify>npx eslint electron/services/modpacks/importers/localInstaller.ts electron/services/instances/importer/InstanceImporterService.ts && npx tsc --noEmit</verify>
  <done>Unsafe archive entries cannot escape the selected instance or modpack root during import, and clean archives still preview and import successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Keep modpack backup, export, override, and import paths inside approved roots</name>
  <files>electron/services/modpacks/modpackService.ts</files>
  <action>Reuse the shared containment helpers before backup creation, override writes, import destinations, and any archive-derived output paths proceed. Fail fast with actionable errors instead of partial writes, so the renderer retry UI can keep the user in the flow when import or export targets are blocked.</action>
  <verify>npx eslint electron/services/modpacks/modpackService.ts && npx tsc --noEmit</verify>
  <done>Modpack import and backup flows stop writing outside approved launcher roots, and blocked targets surface as retryable failures instead of silent corruption or partial state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx eslint electron/services/modpacks/importers/localInstaller.ts electron/services/instances/importer/InstanceImporterService.ts electron/services/modpacks/modpackService.ts`
- [ ] `npx tsc --noEmit`
- [ ] Manual abuse checks cover crafted traversal archives, absolute-path archive entries, and unsafe modpack backup or import destinations
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Archive and import flows stop being the remaining path-escape vector inside Phase 1
</success_criteria>

<output>
After completion, create `.planning/phases/01-release-baseline-and-trust-boundaries/01-06-SUMMARY.md`
</output>
