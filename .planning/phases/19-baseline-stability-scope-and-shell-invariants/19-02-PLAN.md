---
phase: 19-baseline-stability-scope-and-shell-invariants
plan: "02"
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/components/modpacks/ModpackRouter.tsx
  - src/components/Sidebar.tsx
  - src/components/sidebar/LaunchControls.tsx
  - src/components/modpacks/ModpackDetails.tsx
  - src/components/modpacks/details/ModpackDetailsActions.tsx
  - src/components/__tests__/Sidebar.primary-action.test.tsx
  - src/components/sidebar/__tests__/LaunchControls.status.test.tsx
  - src/components/modpacks/__tests__/ModpackDetails.actions.test.tsx
autonomous: true
requirements:
  - SHELL-03
user_setup: []
must_haves:
  truths:
    - Primary-action ownership must come from navigation context, not from whichever button happens to be rendered with stronger accent styles.
    - The sidebar launch control stays state-focused; route ownership should be decided above `LaunchControls`, not by hardcoding route knowledge into the button component itself.
    - Shell-owned contexts are the launcher-home and exploratory index routes (`simple` home, modpack list, and modpack browser); those contexts may keep shell launch as the dominant primary action.
    - Route-owned contexts are every task flow under `ModpackRouter` that leaves the launcher-home/index surface: modpack details, creation, add-content routes (`addMod`, `addResourcePack`, `addShader`), plus `export`, `install`, and `importPreview`; those contexts must demote or remove shell launch from primary competition.
    - This plan owns CTA truth only; it must not turn into broad action-layout or dense-content redesign.
  artifacts:
    - src/components/modpacks/ModpackRouter.tsx
    - src/components/Sidebar.tsx
    - src/components/sidebar/LaunchControls.tsx
    - src/components/modpacks/ModpackDetails.tsx
    - src/components/modpacks/details/ModpackDetailsActions.tsx
    - src/components/__tests__/Sidebar.primary-action.test.tsx
    - src/components/sidebar/__tests__/LaunchControls.status.test.tsx
    - src/components/modpacks/__tests__/ModpackDetails.actions.test.tsx
  key_links:
    - Phase 19 research found that `Sidebar` always renders a strong `PLAY` CTA because it only knows launchability, not route ownership.
    - The same research showed `ModpackDetailsActions` can render both `Play` and `Update Available` as route-primary affordances, producing ambiguity even before the shell `PLAY` is considered.
---

<objective>
Define one truthful primary-action ownership policy across shell-owned and route-owned contexts.

Purpose: stop the shell and deep routes from competing for the same primary action and collapse modpack-details to one route-primary slot.
Output: a navigation-aware CTA ownership seam plus focused regression coverage for sidebar and modpack-details action truth.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-baseline-stability-scope-and-shell-invariants/19-CONTEXT.md
@.planning/phases/19-baseline-stability-scope-and-shell-invariants/19-RESEARCH.md
@.planning/phases/19-baseline-stability-scope-and-shell-invariants/19-VALIDATION.md
@src/components/modpacks/ModpackRouter.tsx
@src/components/Sidebar.tsx
@src/components/sidebar/LaunchControls.tsx
@src/components/modpacks/ModpackDetails.tsx
@src/components/modpacks/details/ModpackDetailsActions.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce a shared shell-versus-route CTA ownership seam</name>
  <files>src/components/modpacks/ModpackRouter.tsx, src/components/Sidebar.tsx, src/components/sidebar/LaunchControls.tsx</files>
  <action>Define a navigation-aware ownership seam that tells the shell when the current context is shell-owned versus route-owned. Implement the explicit Phase 19 ownership matrix: launcher-home, modpack list, and modpack browser stay shell-owned; modpack details, creation, add-content flows, `export`, `install`, and `importPreview` become route-owned. Use that seam to demote or suppress the sidebar launch emphasis on route-owned contexts while keeping `LaunchControls` centered on launch state and wording rather than route-specific branching.</action>
  <verify>npx eslint src/components/modpacks/ModpackRouter.tsx src/components/Sidebar.tsx src/components/sidebar/LaunchControls.tsx && npx tsc --noEmit</verify>
  <done>The shell no longer assumes `PLAY` is globally primary, and the ownership matrix for shell-owned versus route-owned contexts is expressed through one shared context seam rather than per-route ad hoc conditions.</done>
</task>

<task type="auto">
  <name>Task 2: Collapse modpack-details to one route-primary action and lock the rule with focused tests</name>
  <files>src/components/modpacks/ModpackDetails.tsx, src/components/modpacks/details/ModpackDetailsActions.tsx, src/components/__tests__/Sidebar.primary-action.test.tsx, src/components/sidebar/__tests__/LaunchControls.status.test.tsx, src/components/modpacks/__tests__/ModpackDetails.actions.test.tsx</files>
  <action>Simplify the modpack-details action hierarchy so the route exposes exactly one dominant primary action at a time, even when update state is present. Add focused regression coverage proving the shared ownership matrix holds for both shell-owned contexts and every route-owned `ModpackRouter` family Phase 19 claims to cover (`details`, `create`, `add*`, `export`, `install`, `importPreview`), while preserving the existing launch-state semantics already covered at the launch-control seam.</action>
  <verify>npx vitest run src/components/__tests__/Sidebar.primary-action.test.tsx src/components/sidebar/__tests__/LaunchControls.status.test.tsx src/components/modpacks/__tests__/ModpackDetails.actions.test.tsx && npx tsc --noEmit</verify>
  <done>Deep modpack contexts expose one route-primary action, the sidebar no longer visually competes for the same task, and focused tests prove both shell-owned and route-owned `ModpackRouter` context families follow the Phase 19 ownership matrix.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/__tests__/Sidebar.primary-action.test.tsx src/components/sidebar/__tests__/LaunchControls.status.test.tsx src/components/modpacks/__tests__/ModpackDetails.actions.test.tsx`
- [ ] `npx eslint src/components/modpacks/ModpackRouter.tsx src/components/Sidebar.tsx src/components/sidebar/LaunchControls.tsx src/components/modpacks/ModpackDetails.tsx src/components/modpacks/details/ModpackDetailsActions.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Each affected context exposes one unambiguous primary action, with shell launch ownership and route launch ownership no longer competing
</success_criteria>

<output>
After completion, create `.planning/phases/19-baseline-stability-scope-and-shell-invariants/19-02-SUMMARY.md`
</output>
