---
phase: 36-settings-predictability-and-shared-control-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SettingsPage.tsx
  - src/components/settings/SettingsTabsHeader.tsx
  - src/components/settings/settingsTabs.ts
  - src/features/settings/statistics/StatisticsTab.tsx
  - src/components/__tests__/SettingsPage.navigation.test.tsx
  - src/components/settings/__tests__/SecondarySettingsTabs.test.tsx
  - src/components/settings/__tests__/SettingsTabsHeader.a11y.test.tsx
  - src/components/settings/__tests__/SettingsTabsHeader.state-fidelity.test.tsx
  - src/components/__tests__/SettingsPage.layout.test.tsx
autonomous: true
requirements:
  - SETTINGS-05
user_setup: []
must_haves:
  truths:
    - The settings shell uses one readable header and copy contract instead of stacked tab cards plus a second summary strip.
    - Tab selection remains accessible and stateful while consuming less vertical space above the real controls.
    - Embedded settings surfaces stop restating shell-level guidance unless they add genuinely new information.
    - Wave 0 shell-layout coverage lands here so duplicate-copy regressions fail immediately.
  artifacts:
    - src/components/SettingsPage.tsx
    - src/components/settings/SettingsTabsHeader.tsx
    - src/components/settings/settingsTabs.ts
    - src/features/settings/statistics/StatisticsTab.tsx
    - src/components/__tests__/SettingsPage.layout.test.tsx
  key_links:
    - `src/components/settings/settingsTabs.ts` -> `src/components/SettingsPage.tsx` via the shell-owned tab label and description contract
    - `src/components/settings/SettingsTabsHeader.tsx` -> `src/components/SettingsPage.tsx` via lighter tab chrome and navigation ownership
    - `src/features/settings/statistics/StatisticsTab.tsx` -> `src/components/SettingsPage.tsx` via embedded copy deduplication
    - `src/components/__tests__/SettingsPage.layout.test.tsx` -> `src/components/SettingsPage.tsx` via shell-level duplicate-copy and chrome-density assertions
---

<objective>
Flatten the settings shell so users reach real controls without wading through repeated chrome.

Purpose: remove leftover settings heaviness and duplicate explanatory copy before deeper preset and control-system work lands.
Output: a lighter settings shell with one authoritative header/copy contract, accessible tabs, and embedded panels that no longer restate shell guidance by default.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ru/product-feedback-2026-04-20.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-settings-predictability-and-shared-control-contract/36-RESEARCH.md
@.planning/phases/36-settings-predictability-and-shared-control-contract/36-VALIDATION.md
@src/components/SettingsPage.tsx
@src/components/settings/SettingsTabsHeader.tsx
@src/components/settings/settingsTabs.ts
@src/features/settings/statistics/StatisticsTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace stacked settings chrome with one lighter shell contract and land the shell-layout seam</name>
  <files>src/components/SettingsPage.tsx, src/components/settings/SettingsTabsHeader.tsx, src/components/settings/settingsTabs.ts, src/components/__tests__/SettingsPage.layout.test.tsx, src/components/__tests__/SettingsPage.navigation.test.tsx</files>
  <action>Reduce the amount of shell chrome above the panel by moving away from descriptive card-tabs plus a second summary strip, keep keyboard-accessible tab navigation intact, and add the missing shell-level layout seam so duplicate header copy or top-heavy settings chrome immediately breaks tests.</action>
  <verify>npx vitest run src/components/__tests__/SettingsPage.layout.test.tsx src/components/__tests__/SettingsPage.navigation.test.tsx src/components/settings/__tests__/SettingsTabsHeader.a11y.test.tsx && npx eslint src/components/SettingsPage.tsx src/components/settings/SettingsTabsHeader.tsx src/components/settings/settingsTabs.ts && npx tsc --noEmit</verify>
  <done>The settings modal reaches its real controls faster and the shell-level contract is explicitly tested.</done>
</task>

<task type="auto">
  <name>Task 2: Remove shell-level copy duplication from tabs and embedded statistics without weakening context</name>
  <files>src/components/settings/settingsTabs.ts, src/features/settings/statistics/StatisticsTab.tsx, src/components/settings/__tests__/SecondarySettingsTabs.test.tsx, src/components/settings/__tests__/SettingsTabsHeader.state-fidelity.test.tsx, src/features/settings/statistics/__tests__/StatisticsTab.layout.test.tsx</files>
  <action>Trim tab descriptions and embedded statistics copy so the shell owns the first-level explanation and embedded surfaces only add new information. Preserve tab readability and accent-backed active state while removing duplicated prose that makes settings feel noisy and repetitive.</action>
  <verify>npx vitest run src/components/settings/__tests__/SecondarySettingsTabs.test.tsx src/components/settings/__tests__/SettingsTabsHeader.state-fidelity.test.tsx src/features/settings/statistics/__tests__/StatisticsTab.layout.test.tsx && npx eslint src/components/settings/settingsTabs.ts src/features/settings/statistics/StatisticsTab.tsx && npx tsc --noEmit</verify>
  <done>Settings surfaces no longer repeat the same explanatory copy across shell and panel layers.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/__tests__/SettingsPage.layout.test.tsx src/components/__tests__/SettingsPage.navigation.test.tsx src/components/settings/__tests__/SecondarySettingsTabs.test.tsx src/components/settings/__tests__/SettingsTabsHeader.a11y.test.tsx src/components/settings/__tests__/SettingsTabsHeader.state-fidelity.test.tsx src/features/settings/statistics/__tests__/StatisticsTab.layout.test.tsx`
- [ ] `npx eslint src/components/SettingsPage.tsx src/components/settings/SettingsTabsHeader.tsx src/components/settings/settingsTabs.ts src/features/settings/statistics/StatisticsTab.tsx`
- [ ] `npx tsc --noEmit`
- [ ] Manual settings-shell review completed: tabs and shell copy no longer dominate the first screen, and statistics does not restate shell-level guidance
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Settings stop feeling top-heavy and repetitive before any deeper preset or control cleanup begins
</success_criteria>

<output>
After completion, create `.planning/phases/36-settings-predictability-and-shared-control-contract/36-01-SUMMARY.md`
</output>
