---
phase: 01-release-baseline-and-trust-boundaries
plan: "07"
type: execute
wave: 3
depends_on:
  - "01-08"
files_modified:
  - shared/contracts/externalLinks.ts
  - shared/contracts/index.ts
  - shared/contracts/windowApi.ts
  - docs/ru/contracts-map.md
  - src/services/ipc/externalLinksIPC.ts
  - src/vite-env.d.ts
  - electron/ipc/handlers/externalLinksHandlers.ts
  - electron/ipc/ipcManager.ts
  - electron/security/externalUrls.ts
  - electron/window/windowManager.ts
  - electron/preload.ts
  - electron/preload/bridges/ExternalLinksBridge.ts
  - electron/preload/bridges/IpcRendererBridge.ts
  - src/components/modpacks/details/ModsTab.tsx
  - src/components/modpacks/details/ModpackDetailsModsTab.tsx
autonomous: true
requirements:
  - SEC-03
user_setup: []
must_haves:
  truths:
    - Trusted external domains open directly, unfamiliar domains require an explicit FMCL confirmation, and dangerous schemes are blocked outright.
    - Renderer-triggered modpack detail links follow an explicit typed path from renderer wrapper to shared contract to preload bridge to main-process classifier, so there is no direct `window.*` or raw-anchor bypass around confirmation.
    - If sandbox cannot be enabled yet, FMCL still ships with explicit compensating controls that are implemented and verified, not merely documented.
  artifacts:
    - shared/contracts/externalLinks.ts
    - docs/ru/contracts-map.md
    - src/services/ipc/externalLinksIPC.ts
    - electron/ipc/handlers/externalLinksHandlers.ts
    - electron/security/externalUrls.ts
    - electron/window/windowManager.ts
    - electron/preload.ts
    - electron/preload/bridges/ExternalLinksBridge.ts
    - src/components/modpacks/details/ModsTab.tsx
    - src/components/modpacks/details/ModpackDetailsModsTab.tsx
  key_links:
    - The same external-URL policy governs renderer-initiated link requests, window navigation, and popup creation through one shared classifier.
    - Sandbox posture is decided together with the reviewed preload and raw IPC surface so the fallback path still leaves FMCL measurably safer by default.
---

<objective>
Tighten FMCL's external-link and window security posture so the launcher stops delegating unfamiliar or dangerous URLs blindly to the OS.

Purpose: `SEC-03` is not complete when `contextIsolation` exists but unexpected URLs still bypass trust decisions or renderer code reaches external navigation without the supported typed IPC path.
Output: A typed renderer-to-main external-link flow, a shared external-URL trust classifier reused by window events, and an explicit sandbox outcome with compensating controls if enablement is still blocked.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-CONTEXT.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-RESEARCH.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-VALIDATION.md
@shared/contracts/externalLinks.ts
@shared/contracts/windowApi.ts
@docs/ru/contracts-map.md
@src/services/ipc/externalLinksIPC.ts
@src/vite-env.d.ts
@electron/ipc/handlers/externalLinksHandlers.ts
@electron/ipc/ipcManager.ts
@electron/window/windowManager.ts
@electron/preload.ts
@electron/preload/bridges/ExternalLinksBridge.ts
@electron/preload/bridges/IpcRendererBridge.ts
@src/components/modpacks/details/ModsTab.tsx
@src/components/modpacks/details/ModpackDetailsModsTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the typed renderer request path for external-link actions</name>
  <files>shared/contracts/externalLinks.ts, shared/contracts/index.ts, shared/contracts/windowApi.ts, src/services/ipc/externalLinksIPC.ts, src/vite-env.d.ts, src/components/modpacks/details/ModsTab.tsx, src/components/modpacks/details/ModpackDetailsModsTab.tsx</files>
  <action>Create an explicit external-link contract plus a renderer IPC wrapper under `src/services/ipc/` and migrate the modpack detail tabs onto that wrapper. Keep UI code on the supported `window.api.*` path rather than direct globals or raw anchors, and make the request shape reusable for later trust prompts.</action>
  <verify>npx eslint shared/contracts/externalLinks.ts shared/contracts/windowApi.ts src/services/ipc/externalLinksIPC.ts src/components/modpacks/details/ModsTab.tsx src/components/modpacks/details/ModpackDetailsModsTab.tsx src/vite-env.d.ts && npm run contracts:check</verify>
  <done>The renderer uses one typed FMCL wrapper for external-link requests, and the modpack detail tabs no longer depend on direct window-level navigation behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the preload-to-main bridge and shared trust classifier for external URLs</name>
  <files>electron/preload.ts, electron/preload/bridges/ExternalLinksBridge.ts, electron/ipc/handlers/externalLinksHandlers.ts, electron/ipc/ipcManager.ts, electron/security/externalUrls.ts, electron/window/windowManager.ts, docs/ru/contracts-map.md</files>
  <action>Wire the typed renderer request through preload and an explicit main-process handler, then reuse the same external-URL classifier for renderer requests, popup creation, and unexpected navigation handling. Block malformed, `javascript:`, `data:`, and `file:` URLs outright, open trusted domains directly, and require confirmation before unfamiliar domains reach `shell.openExternal`. If this work changes the IPC or preload contract surface, update `docs/ru/contracts-map.md` in the same slice so the documented channel map stays truthful.</action>
  <verify>npx eslint electron/preload.ts electron/preload/bridges/ExternalLinksBridge.ts electron/ipc/handlers/externalLinksHandlers.ts electron/ipc/ipcManager.ts electron/security/externalUrls.ts electron/window/windowManager.ts && npm run ipc:check && npx tsc --noEmit</verify>
  <done>Unexpected navigation is no longer delegated blindly to the OS, and renderer-triggered links follow the same typed trust flow as popup or navigation events.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve sandbox posture with explicit compensating controls if enablement stays blocked</name>
  <files>electron/window/windowManager.ts, electron/preload.ts, electron/preload/bridges/IpcRendererBridge.ts</files>
  <action>Attempt sandbox enablement only if the existing preload bridge surface still boots and functions. If sandbox must remain disabled, keep it off only after locking in the compensating controls Phase 1 can actually verify: strict `contextIsolation` and `nodeIntegration` posture, no broader raw IPC surface than the reviewed allowlist, and enforced external-link trust gates. Record the concrete blocker, but do not treat documentation alone as acceptance.</action>
  <verify>npm run build -- --publish never</verify>
  <done>Either sandbox is enabled without breaking the app, or sandbox remains disabled with verified compensating controls that measurably reduce renderer exposure instead of leaving an inherited default untouched.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx eslint shared/contracts/externalLinks.ts shared/contracts/windowApi.ts src/services/ipc/externalLinksIPC.ts src/vite-env.d.ts electron/preload.ts electron/preload/bridges/ExternalLinksBridge.ts electron/preload/bridges/IpcRendererBridge.ts electron/ipc/handlers/externalLinksHandlers.ts electron/ipc/ipcManager.ts electron/security/externalUrls.ts electron/window/windowManager.ts src/components/modpacks/details/ModsTab.tsx src/components/modpacks/details/ModpackDetailsModsTab.tsx`
- [ ] `npm run contracts:check`
- [ ] `npm run ipc:check`
- [ ] `npx tsc --noEmit`
- [ ] `npm run build -- --publish never`
- [ ] Manual smoke covers trusted URL open, unfamiliar URL confirmation, dangerous-scheme blocking, and renderer security posture if `sandbox` remains disabled
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- External navigation and window posture have an explicit, testable security outcome rather than an inherited permissive default
</success_criteria>

<output>
After completion, create `.planning/phases/01-release-baseline-and-trust-boundaries/01-07-SUMMARY.md`
</output>
