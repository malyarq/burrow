---
phase: 03-modpack-workflow-completion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/modpacks/hooks/useModpackNavigation.ts
  - src/components/modpacks/ModpackRouter.tsx
  - src/components/modpacks/ModpackBrowser.tsx
  - src/components/modpacks/InstallModpackPage.tsx
  - src/features/modpacks/__tests__/modpackNavigationState.test.tsx
autonomous: true
requirements:
  - FLOW-03
  - FLOW-04
user_setup: []
must_haves:
  truths:
    - Leaving the modpack browser for install, import, or details does not discard the active browser session state.
    - Browser-state restoration is owned by an explicit router or navigation contract rather than ad hoc `localStorage` recovery.
    - Successful install no longer depends on `window.location.reload()` to get the launcher back into a good state.
  artifacts:
    - src/features/modpacks/hooks/useModpackNavigation.ts
    - src/components/modpacks/ModpackRouter.tsx
    - src/components/modpacks/ModpackBrowser.tsx
    - src/components/modpacks/InstallModpackPage.tsx
    - src/features/modpacks/__tests__/modpackNavigationState.test.tsx
  key_links:
    - `03-03` depends on this state contract and should not invent a separate browser-state store.
    - Keep `localStorage` for persisted preferences like history, favorites, and page size, but do not rely on remount side effects to restore the active browser session.
---

<objective>
Establish a real browser-state contract for modpack browsing.

Purpose: preserve search, filters, page, and browser mode across route changes so Phase 3 can complete history and pagination workflows without relying on full reloads.
Output: router-owned browser state, install-return behavior that preserves context, and automated coverage for the new state contract.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-modpack-workflow-completion/03-RESEARCH.md
@.planning/phases/03-modpack-workflow-completion/03-VALIDATION.md
@src/features/modpacks/hooks/useModpackNavigation.ts
@src/components/modpacks/ModpackRouter.tsx
@src/components/modpacks/ModpackBrowser.tsx
@src/components/modpacks/InstallModpackPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define a typed browser-state model in modpack navigation</name>
  <files>src/features/modpacks/hooks/useModpackNavigation.ts, src/components/modpacks/ModpackRouter.tsx, src/features/modpacks/__tests__/modpackNavigationState.test.tsx</files>
  <action>Extend the modpack navigation model so browser views can carry the state needed to restore a browsing session: query, filters, sort, page, page size, history mode, and any other state that must survive route transitions. Keep the model narrow to the browser seam and add automated coverage that proves the navigation stack preserves those snapshots correctly.</action>
  <verify>npx vitest run src/features/modpacks/__tests__/modpackNavigationState.test.tsx</verify>
  <done>The router can represent browser sessions explicitly instead of treating every browser view as an unparameterized `{ type: 'browser' }` entry.</done>
</task>

<task type="auto">
  <name>Task 2: Wire browser and install flows onto the preserved-state contract</name>
  <files>src/components/modpacks/ModpackBrowser.tsx, src/components/modpacks/InstallModpackPage.tsx, src/components/modpacks/ModpackRouter.tsx, src/features/modpacks/__tests__/modpackNavigationState.test.tsx</files>
  <action>Update the browser and install flow to read and write the new browser-state contract so back-navigation restores the prior browse session. Remove reload-based recovery from successful install flow if the new state contract makes it unnecessary, and keep the implementation brownfield-safe by touching only the modpack browser route seam.</action>
  <verify>npx vitest run src/features/modpacks/__tests__/modpackNavigationState.test.tsx && npx tsc --noEmit</verify>
  <done>Users can leave and return to the modpack browser without losing active browsing context, and install success no longer relies on a page reload to recover UI state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/features/modpacks/__tests__/modpackNavigationState.test.tsx`
- [ ] `npx eslint src/features/modpacks/hooks/useModpackNavigation.ts src/components/modpacks/ModpackRouter.tsx src/components/modpacks/ModpackBrowser.tsx src/components/modpacks/InstallModpackPage.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- FMCL preserves modpack-browser session state across navigation and install return instead of resetting users back to a cold browser view
</success_criteria>

<output>
After completion, create `.planning/phases/03-modpack-workflow-completion/03-01-SUMMARY.md`
</output>
