---
phase: 11-adaptive-layout-and-interaction-foundations
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/AnchoredOverlay.tsx
  - src/components/ui/Tooltip.tsx
  - src/components/onboarding/OnboardingTour.tsx
  - src/components/modpacks/ModpackList.tsx
  - src/components/ui/__tests__/AnchoredOverlay.test.tsx
  - src/components/modpacks/__tests__/ModpackList.actions.test.tsx
autonomous: true
requirements:
  - ADPT-03
user_setup: []
must_haves:
  truths:
    - FMCL menus and overlay-like interactions must stop relying on raw coordinate math that breaks at window edges or after resize.
    - The first overlay fix must establish a reusable anchoring contract, not just hardcode a better offset into one menu.
    - Installed-modpack card actions are the proving ground for the new seam because they already exhibit the exact user-reported overflow problem.
    - This plan owns overlay anchoring and overflow safety only; it must not drift into general modpack-browser ergonomics or walkthrough polish.
  artifacts:
    - src/components/ui/AnchoredOverlay.tsx
    - src/components/ui/Tooltip.tsx
    - src/components/onboarding/OnboardingTour.tsx
    - src/components/modpacks/ModpackList.tsx
    - src/components/ui/__tests__/AnchoredOverlay.test.tsx
    - src/components/modpacks/__tests__/ModpackList.actions.test.tsx
  key_links:
    - Use the new anchored overlay seam for the most brittle existing consumer first, then any foundational overlay consumer directly affected by the seam.
    - Keep the work reusable and viewport-aware so later phases can adopt it without new coordinate hacks.
---

<objective>
Harden overflow-safe anchored overlays.

Purpose: replace brittle menu-placement assumptions with a reusable overlay anchoring seam that stays attached to the trigger and inside the current window.
Output: one shared anchored overlay helper plus migrated foundational consumers, with installed-modpack actions no longer jumping off-screen.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-adaptive-layout-and-interaction-foundations/11-RESEARCH.md
@.planning/phases/11-adaptive-layout-and-interaction-foundations/11-VALIDATION.md
@src/components/modpacks/ModpackList.tsx
@src/components/ui/Tooltip.tsx
@src/components/onboarding/OnboardingTour.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a reusable viewport-safe anchored overlay seam</name>
  <files>src/components/ui/AnchoredOverlay.tsx, src/components/ui/Tooltip.tsx, src/components/onboarding/OnboardingTour.tsx</files>
  <action>Create or refine one shared overlay-positioning seam that can anchor to a trigger, clamp to the viewport, and react safely to resize or edge-position conditions. Use the smallest abstraction that removes raw coordinate duplication while staying easy to adopt in brownfield feature code. Migrate any foundational overlay consumer directly tied to this seam so the behavior is proven reusable instead of theoretical.</action>
  <verify>npx eslint src/components/ui/AnchoredOverlay.tsx src/components/ui/Tooltip.tsx src/components/onboarding/OnboardingTour.tsx && npx tsc --noEmit</verify>
  <done>FMCL has one reusable overlay anchoring rule instead of ad hoc position math scattered through feature surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate installed-modpack actions and lock the overflow behavior with tests</name>
  <files>src/components/modpacks/ModpackList.tsx, src/components/ui/__tests__/AnchoredOverlay.test.tsx, src/components/modpacks/__tests__/ModpackList.actions.test.tsx</files>
  <action>Move the installed-modpack action menu onto the anchored overlay seam and remove the brittle right-edge or bottom-edge assumptions from the current card action flow. Extend the existing action-menu test coverage and add a narrow overlay-seam test so keyboard-open, pointer-open, and viewport-edge behavior stay attached to the same positioning contract.</action>
  <verify>npx vitest run src/components/ui/__tests__/AnchoredOverlay.test.tsx src/components/modpacks/__tests__/ModpackList.actions.test.tsx && npx eslint src/components/modpacks/ModpackList.tsx && npx tsc --noEmit</verify>
  <done>The user-reported modpack action-menu overflow problem is fixed on a shared interaction seam rather than patched locally.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/ui/__tests__/AnchoredOverlay.test.tsx src/components/modpacks/__tests__/ModpackList.actions.test.tsx`
- [ ] `npx eslint src/components/ui/AnchoredOverlay.tsx src/components/ui/Tooltip.tsx src/components/onboarding/OnboardingTour.tsx src/components/modpacks/ModpackList.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- FMCL overlay placement is now trigger-anchored and viewport-safe on the foundational consumers touched by the phase
</success_criteria>

<output>
After completion, create `.planning/phases/11-adaptive-layout-and-interaction-foundations/11-02-SUMMARY.md`
</output>
