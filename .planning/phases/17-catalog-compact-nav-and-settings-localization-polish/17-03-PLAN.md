---
phase: 17-catalog-compact-nav-and-settings-localization-polish
plan: "03"
type: execute
wave: 2
depends_on: []
files_modified:
  - src/components/SettingsPage.tsx
  - src/components/settings/SettingsTabsHeader.tsx
  - src/components/settings/settingsTabs.ts
  - src/components/settings/tabs/AppearanceTab.tsx
  - src/contexts/settings/theme-presets.ts
  - src/locales/en.json
  - src/locales/ru.json
  - src/components/__tests__/SettingsPage.navigation.test.tsx
  - src/components/settings/__tests__/AppearanceTab.i18n.test.tsx
  - src/components/settings/__tests__/AppearanceTab.presets.test.tsx
autonomous: true
requirements:
  - SET-01
  - SET-02
user_setup: []
must_haves:
  truths:
    - Settings-shell copy must stop leaking raw localization keys even when locale data is incomplete; this is a shell hardening repair, not a global i18n subsystem rewrite.
    - Theme preset IDs and persistence semantics must remain stable while the display-name policy becomes deliberate and user-facing.
    - The preset naming policy must be explicit and consistent across headings, combobox options, export metadata, and both RU and EN surfaces.
    - This plan owns settings-shell localization hardening and preset naming only; it must not absorb unrelated settings IA redesign or broader catalog/navigation scope.
  artifacts:
    - src/components/SettingsPage.tsx
    - src/components/settings/SettingsTabsHeader.tsx
    - src/components/settings/tabs/AppearanceTab.tsx
    - src/contexts/settings/theme-presets.ts
    - src/locales/en.json
    - src/locales/ru.json
    - src/components/__tests__/SettingsPage.navigation.test.tsx
    - src/components/settings/__tests__/AppearanceTab.i18n.test.tsx
    - src/components/settings/__tests__/AppearanceTab.presets.test.tsx
  key_links:
    - Phase 17 research found the settings shell still renders multiple visible strings through raw `t(key)` calls, and `createTranslator()` returns the key itself on misses, making the shell structurally prone to raw-key leakage.
    - The same research confirmed preset display names are currently hard-coded English strings in `theme-presets.ts`, while persistence correctly relies on stable preset IDs instead of the display labels.
---

<objective>
Harden the settings shell against raw-key leakage and make preset naming intentional.

Purpose: keep the shipped settings surface truthful in both languages and implement one deliberate theme preset naming policy without breaking preset identity or persistence.
Output: a settings shell that no longer exposes raw localization keys plus a consistent RU or brand-style preset naming contract across the appearance surface.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-catalog-compact-nav-and-settings-localization-polish/17-RESEARCH.md
@.planning/phases/17-catalog-compact-nav-and-settings-localization-polish/17-VALIDATION.md
@src/components/SettingsPage.tsx
@src/components/settings/SettingsTabsHeader.tsx
@src/components/settings/settingsTabs.ts
@src/components/settings/tabs/AppearanceTab.tsx
@src/contexts/settings/theme-presets.ts
@src/contexts/settings/i18n.ts
@src/locales/en.json
@src/locales/ru.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden the settings shell against raw-key leakage on visible tab labels, descriptions, and panel hints</name>
  <files>src/components/SettingsPage.tsx, src/components/settings/SettingsTabsHeader.tsx, src/components/settings/settingsTabs.ts, src/locales/en.json, src/locales/ru.json, src/components/__tests__/SettingsPage.navigation.test.tsx</files>
  <action>Update the settings shell so the visible tab summaries, descriptions, and footer hints no longer depend on plain `t(key)` returning a real translation. Add or normalize any missing high-risk locale entries the shell needs, and reuse the existing fallback approach already used elsewhere in the launcher rather than introducing a new translation subsystem. Keep the work bounded to the shipped settings shell and launch-adjacent settings copy this phase owns.</action>
  <verify>npx vitest run src/components/__tests__/SettingsPage.navigation.test.tsx && npx eslint src/components/SettingsPage.tsx src/components/settings/SettingsTabsHeader.tsx src/components/settings/settingsTabs.ts src/locales/en.json src/locales/ru.json && npx tsc --noEmit</verify>
  <done>The settings shell remains human-readable even when locale coverage is imperfect, and the shipped settings view no longer leaks raw keys in the Phase 17 surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Encode one deliberate preset naming policy without changing preset identity or migrations</name>
  <files>src/components/settings/tabs/AppearanceTab.tsx, src/contexts/settings/theme-presets.ts, src/locales/en.json, src/locales/ru.json, src/components/settings/__tests__/AppearanceTab.i18n.test.tsx, src/components/settings/__tests__/AppearanceTab.presets.test.tsx</files>
  <action>Lock and implement the preset naming policy Phase 17 needs, then render preset labels through that contract consistently in the appearance heading, combobox options, and exported or summarized preset copy. Preserve stable preset IDs and current migration behavior, and make the tests assert the chosen user-facing contract so the preset names cannot silently drift back to accidental English leakage.</action>
  <verify>npx vitest run src/components/settings/__tests__/AppearanceTab.i18n.test.tsx src/components/settings/__tests__/AppearanceTab.presets.test.tsx && npx eslint src/components/settings/tabs/AppearanceTab.tsx src/contexts/settings/theme-presets.ts src/locales/en.json src/locales/ru.json && npx tsc --noEmit</verify>
  <done>Theme presets follow one explicit naming policy on the shipped appearance surface while preset identity, persistence, and migration behavior remain stable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/__tests__/SettingsPage.navigation.test.tsx src/components/settings/__tests__/AppearanceTab.i18n.test.tsx src/components/settings/__tests__/AppearanceTab.presets.test.tsx`
- [ ] `npx eslint src/components/SettingsPage.tsx src/components/settings/SettingsTabsHeader.tsx src/components/settings/settingsTabs.ts src/components/settings/tabs/AppearanceTab.tsx src/contexts/settings/theme-presets.ts src/locales/en.json src/locales/ru.json`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The settings shell no longer leaks raw localization keys, and theme presets follow one deliberate user-facing naming policy without breaking preset identity
</success_criteria>

<output>
After completion, create `.planning/phases/17-catalog-compact-nav-and-settings-localization-polish/17-03-SUMMARY.md`
</output>
