---
phase: 08-core-route-rollout-and-ui-correctness
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SettingsPage.tsx
  - src/features/accounts/AccountsPage.tsx
  - src/features/accounts/AddAccountDialog.tsx
  - src/features/accounts/AccountSkinPanel.tsx
  - src/locales/en.json
  - src/locales/ru.json
  - src/features/accounts/__tests__/AccountsPage.a11y.test.tsx
  - src/features/accounts/__tests__/AccountSkinsPage.test.tsx
  - src/components/__tests__/SettingsPage.accounts.test.tsx
autonomous: true
requirements:
  - DSYS-03
  - LOCL-01
  - UX-03
user_setup: []
must_haves:
  truths:
    - Settings and account flows now feel like one shared launcher surface instead of a modern shell wrapped around legacy inner panels.
    - Account selection, add-account, remove-account, and skin-provider flows expose consistent structure, buttons, feedback, and locale-safe copy.
    - The old fixed-dark or route-local styling in the account skin seam is removed or absorbed into the shared token contract.
    - This slice stays on settings/accounts ownership and does not broaden into other settings tabs beyond fallout directly required for route coherence.
  artifacts:
    - src/components/SettingsPage.tsx
    - src/features/accounts/AccountsPage.tsx
    - src/features/accounts/AddAccountDialog.tsx
    - src/features/accounts/AccountSkinPanel.tsx
    - src/locales/en.json
    - src/locales/ru.json
    - src/features/accounts/__tests__/AccountsPage.a11y.test.tsx
    - src/features/accounts/__tests__/AccountSkinsPage.test.tsx
    - src/components/__tests__/SettingsPage.accounts.test.tsx
  key_links:
    - Build on the local WIP in `AccountsPage.tsx` and `AddAccountDialog.tsx`; do not re-open them with a conflicting route approach.
    - Keep account behavior anchored to the existing IPC seams and provider-aware skin contract rather than expanding auth scope in a UI phase.
---

<objective>
Complete the settings and accounts route rollout.

Purpose: make settings and account management feel structurally and visually consistent with the refreshed launcher shell.
Output: one coherent settings/accounts experience with aligned inner panels, skin management, and locale-safe copy.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-core-route-rollout-and-ui-correctness/08-RESEARCH.md
@.planning/phases/08-core-route-rollout-and-ui-correctness/08-VALIDATION.md
@src/components/SettingsPage.tsx
@src/features/accounts/AccountsPage.tsx
@src/features/accounts/AddAccountDialog.tsx
@src/features/accounts/AccountSkinPanel.tsx
@src/locales/en.json
@src/locales/ru.json
@src/features/accounts/__tests__/AccountsPage.a11y.test.tsx
@src/features/accounts/__tests__/AccountSkinsPage.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bring settings/accounts structure and visual language onto the shared route contract</name>
  <files>src/components/SettingsPage.tsx, src/features/accounts/AccountsPage.tsx, src/features/accounts/AddAccountDialog.tsx, src/features/accounts/AccountSkinPanel.tsx</files>
  <action>Finish the rollout of the settings/accounts route onto the Phase 7 system by aligning the settings shell footer, account cards, add-account dialog, and account skin panel around the same surface, spacing, and feedback grammar. Remove remaining fixed-dark or route-local styling seams inside account management, and keep behavior centered on the existing account and skin IPC flows rather than expanding the feature set.</action>
  <verify>npx eslint src/components/SettingsPage.tsx src/features/accounts/AccountsPage.tsx src/features/accounts/AddAccountDialog.tsx src/features/accounts/AccountSkinPanel.tsx && npx tsc --noEmit</verify>
  <done>Settings/accounts behaves as one coherent route instead of a refreshed modal shell wrapped around inconsistent account subpanels.</done>
</task>

<task type="auto">
  <name>Task 2: Complete touched settings/accounts copy and add route-focused regression coverage</name>
  <files>src/locales/en.json, src/locales/ru.json, src/features/accounts/__tests__/AccountsPage.a11y.test.tsx, src/features/accounts/__tests__/AccountSkinsPage.test.tsx, src/components/__tests__/SettingsPage.accounts.test.tsx</files>
  <action>Fill the visible EN and RU copy needed by the refreshed settings/accounts route, including labels, helper states, and action text touched by the rollout. Extend the existing account accessibility and skin-provider tests and add focused coverage for the settings/accounts integration seam so the route can prove both structure and provider-aware behavior after the visual cleanup.</action>
  <verify>npx vitest run src/features/accounts/__tests__/AccountsPage.a11y.test.tsx src/features/accounts/__tests__/AccountSkinsPage.test.tsx src/components/__tests__/SettingsPage.accounts.test.tsx && npx tsc --noEmit</verify>
  <done>The refreshed settings/accounts route is locale-safe and test-covered at the route seam instead of only at isolated account widgets.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/features/accounts/__tests__/AccountsPage.a11y.test.tsx src/features/accounts/__tests__/AccountSkinsPage.test.tsx src/components/__tests__/SettingsPage.accounts.test.tsx`
- [ ] `npx eslint src/components/SettingsPage.tsx src/features/accounts/AccountsPage.tsx src/features/accounts/AddAccountDialog.tsx src/features/accounts/AccountSkinPanel.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- FMCL’s settings/accounts flow now matches the rest of the launcher visually and structurally instead of feeling like a nested legacy module
</success_criteria>

<output>
After completion, create `.planning/phases/08-core-route-rollout-and-ui-correctness/08-02-SUMMARY.md`
</output>
