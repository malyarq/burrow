---
phase: 05-accessibility-and-release-truthfulness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/Modal.tsx
  - src/components/ui/Button.tsx
  - src/components/SettingsPage.tsx
  - src/components/settings/SettingsTabsHeader.tsx
  - src/components/ui/__tests__/Modal.a11y.test.tsx
  - src/components/settings/__tests__/SettingsTabsHeader.a11y.test.tsx
autonomous: true
requirements:
  - A11Y-01
  - A11Y-02
user_setup: []
must_haves:
  truths:
    - Reusable FMCL dialogs expose accessible dialog semantics, trap focus while open, and return focus to the invoking control when they close.
    - Settings navigation exposes a real tab model with predictable keyboard movement instead of only styled buttons, and the duplicate storage tab is removed.
    - Shared interactive primitives touched in this slice preserve accessible names and states rather than forcing later screens to patch around them individually.
  artifacts:
    - src/components/ui/Modal.tsx
    - src/components/SettingsPage.tsx
    - src/components/settings/SettingsTabsHeader.tsx
    - src/components/ui/__tests__/Modal.a11y.test.tsx
    - src/components/settings/__tests__/SettingsTabsHeader.a11y.test.tsx
  key_links:
    - Reuse the stronger dialog semantics already present in `ConfirmDialog` instead of inventing a second accessibility model for modals.
    - Keep the fix scoped to shared primitives and the settings shell so later plans can build on a stable keyboard and semantics foundation.
---

<objective>
Repair the shared accessibility foundations that later Phase 5 screens depend on.

Purpose: satisfy the release requirement that FMCL dialogs and settings navigation behave correctly for keyboard users and assistive technologies before auditing the rest of the launcher.
Output: an accessible modal and settings-tab foundation with targeted regression tests.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-accessibility-and-release-truthfulness/05-RESEARCH.md
@.planning/phases/05-accessibility-and-release-truthfulness/05-VALIDATION.md
@src/components/ui/Modal.tsx
@src/components/ui/Button.tsx
@src/components/SettingsPage.tsx
@src/components/settings/SettingsTabsHeader.tsx
@src/components/ui/ConfirmDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild modal semantics and settings-tab keyboard behavior on the shared shell</name>
  <files>src/components/ui/Modal.tsx, src/components/ui/Button.tsx, src/components/SettingsPage.tsx, src/components/settings/SettingsTabsHeader.tsx</files>
  <action>Upgrade the shared modal to expose dialog semantics, focus trap, focus return, and reduced-motion-safe opening or closing behavior. In the same slice, convert the settings header from styled buttons into a real tablist with active tab state, tab panels, and arrow-key navigation, and remove the duplicated storage tab entry while doing so. Keep the work brownfield-safe by adapting existing FMCL components rather than introducing a separate dialog or tabs framework.</action>
  <verify>npx eslint src/components/ui/Modal.tsx src/components/ui/Button.tsx src/components/SettingsPage.tsx src/components/settings/SettingsTabsHeader.tsx && npx tsc --noEmit</verify>
  <done>FMCL's modal and settings shell stop being accessibility debt and become a stable keyboard or AT foundation for the rest of Phase 5.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused regression tests for modal and settings-shell accessibility</name>
  <files>src/components/ui/__tests__/Modal.a11y.test.tsx, src/components/settings/__tests__/SettingsTabsHeader.a11y.test.tsx</files>
  <action>Add renderer tests that prove the shared modal exposes the expected semantics and focus behavior, and that settings tabs announce the right roles and support keyboard movement without duplicate stops. Keep the assertions targeted to the behavior most likely to regress under future UI refactors.</action>
  <verify>npx vitest run src/components/ui/__tests__/Modal.a11y.test.tsx src/components/settings/__tests__/SettingsTabsHeader.a11y.test.tsx</verify>
  <done>The shared accessibility foundation has automated regression coverage instead of relying on manual spot checks only.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/ui/__tests__/Modal.a11y.test.tsx src/components/settings/__tests__/SettingsTabsHeader.a11y.test.tsx`
- [ ] `npx eslint src/components/ui/Modal.tsx src/components/ui/Button.tsx src/components/SettingsPage.tsx src/components/settings/SettingsTabsHeader.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- FMCL has an accessible modal and settings-tab foundation that later Phase 5 plans can safely build on
</success_criteria>

<output>
After completion, create `.planning/phases/05-accessibility-and-release-truthfulness/05-01-SUMMARY.md`
</output>
