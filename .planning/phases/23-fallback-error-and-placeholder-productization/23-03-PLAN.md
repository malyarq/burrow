---
phase: 23-fallback-error-and-placeholder-productization
plan: "03"
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - src/components/ErrorBoundary.tsx
  - src/components/ErrorBoundaryWrapper.tsx
  - src/main.tsx
  - src/App.tsx
  - src/components/error/FatalErrorView.tsx
  - src/components/__tests__/ErrorBoundary.recovery.test.tsx
  - src/utils/__tests__/displayError.test.ts
autonomous: true
requirements:
  - FALL-03
  - FALL-01
user_setup: []
must_haves:
  truths:
    - The default fatal-error posture must be recovery-first, with restart as the primary action and technical details hidden by default.
    - The same crash surface needs to work both outside and inside `SettingsProvider`, so localization and display rules cannot depend on hooks inside the outer boundary path.
    - Raw React internals, duplicated exception text, localhost URLs, and `node_modules` paths must not appear on the first crash screen.
    - This plan owns renderer-local crash handling only; it must not expand into telemetry, crash reporting, or diagnostics product scope.
  artifacts:
    - src/components/ErrorBoundary.tsx
    - src/components/ErrorBoundaryWrapper.tsx
    - src/main.tsx
    - src/App.tsx
    - src/components/error/FatalErrorView.tsx
    - src/components/__tests__/ErrorBoundary.recovery.test.tsx
  key_links:
    - Research confirmed that both `main.tsx` and `App.tsx` mount the error boundary, while the current crash card renders `error.toString()` and `error.stack` inline by default.
    - The screenshot audit explicitly called out duplicated error text, raw React runtime output, localhost URLs, and `node_modules` paths on the current crash screen.
    - Phase 23 context locked technical details to a secondary affordance only, with restart launcher remaining the primary action.
---

<objective>
Replace the raw renderer crash dump with a recovery-first fatal error surface.

Purpose: make app-wide crashes survivable and user-safe without adding a broader diagnostics product.
Output: one shared fatal-error view reused by both boundary mounts plus regression coverage that keeps details hidden by default.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-fallback-error-and-placeholder-productization/23-CONTEXT.md
@.planning/phases/23-fallback-error-and-placeholder-productization/23-RESEARCH.md
@.planning/phases/23-fallback-error-and-placeholder-productization/23-VALIDATION.md
@src/components/ErrorBoundary.tsx
@src/components/ErrorBoundaryWrapper.tsx
@src/main.tsx
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build one recovery-first fatal error view and wire both boundary mounts to it</name>
  <files>src/components/ErrorBoundary.tsx, src/components/ErrorBoundaryWrapper.tsx, src/main.tsx, src/App.tsx, src/components/error/FatalErrorView.tsx</files>
  <action>Replace the current inline stack-dump card with a shared fatal-error view that keeps restart launcher as the primary action, moves technical details behind a secondary affordance, and renders user-safe localized copy in both the outer and inner boundary paths. Preserve the renderer-local recovery behavior, but stop showing raw React internals and duplicated exception text on the default surface.</action>
  <verify>npx eslint src/components/ErrorBoundary.tsx src/components/ErrorBoundaryWrapper.tsx src/main.tsx src/App.tsx src/components/error/FatalErrorView.tsx && npx tsc --noEmit</verify>
  <done>App-wide crashes land on one recovery-first surface with hidden technical details and a consistent restart path.</done>
</task>

<task type="auto">
  <name>Task 2: Add crash-focused regression coverage for hidden details and restart-first behavior</name>
  <files>src/components/__tests__/ErrorBoundary.recovery.test.tsx, src/utils/__tests__/displayError.test.ts</files>
  <action>Add focused tests proving that the fatal-error surface hides raw details by default, exposes restart as the primary action, and uses the shared display-sanitization rules when an error message would otherwise leak technical or duplicated content.</action>
  <verify>npx vitest run src/components/__tests__/ErrorBoundary.recovery.test.tsx src/utils/__tests__/displayError.test.ts && npx tsc --noEmit</verify>
  <done>The fatal crash path is regression-protected against drifting back to inline stack dumps or technical-first recovery.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/__tests__/ErrorBoundary.recovery.test.tsx src/utils/__tests__/displayError.test.ts`
- [ ] `npx eslint src/components/ErrorBoundary.tsx src/components/ErrorBoundaryWrapper.tsx src/main.tsx src/App.tsx src/components/error/FatalErrorView.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Fatal renderer crashes render through one recovery-first surface instead of raw React internals
</success_criteria>

<output>
After completion, create `.planning/phases/23-fallback-error-and-placeholder-productization/23-03-SUMMARY.md`
</output>
