---
phase: 01-release-baseline-and-trust-boundaries
plan: "05"
type: execute
wave: 3
depends_on:
  - "01-04"
files_modified:
  - electron/services/worlds/worldService.ts
  - electron/services/resourcePacks/resourcePackService.ts
  - electron/services/shaders/shaderService.ts
  - electron/services/screenshots/screenshotService.ts
autonomous: true
requirements:
  - SEC-02
user_setup: []
must_haves:
  truths:
    - If a bad path slips past the renderer and handler, world, resource-pack, shader, and screenshot mutations still stop at the intended instance or content root.
    - Out-of-root delete, rename, copy, or shell-open targets fail closed instead of touching arbitrary user files.
    - Normal in-root content management remains usable after containment is enforced at the service layer.
  artifacts:
    - electron/services/worlds/worldService.ts
    - electron/services/resourcePacks/resourcePackService.ts
    - electron/services/shaders/shaderService.ts
    - electron/services/screenshots/screenshotService.ts
  key_links:
    - Service-layer containment reuses the canonical path guards from `01-04` so handler and service decisions cannot drift.
    - Screenshot and shell-open flows follow the same root policy as delete, rename, and copy operations instead of remaining a side-door around containment.
---

<objective>
Apply containment rules at the service layer for the non-archive content surfaces covered by the new path-guard foundation.

Purpose: `SEC-02` is not met if handler checks exist but the underlying services still mutate whatever path a bad caller reaches.
Output: Hardened world, resource pack, shader, and screenshot services that fail closed on escaped targets while preserving valid in-root operations.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-CONTEXT.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-RESEARCH.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-VALIDATION.md
@electron/security/pathGuards.ts
@electron/services/worlds/worldService.ts
@electron/services/resourcePacks/resourcePackService.ts
@electron/services/shaders/shaderService.ts
@electron/services/screenshots/screenshotService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce containment for world, resource-pack, and shader mutations before filesystem writes or deletes</name>
  <files>electron/services/worlds/worldService.ts, electron/services/resourcePacks/resourcePackService.ts, electron/services/shaders/shaderService.ts</files>
  <action>Use the shared path guards before delete, copy, rename, import, or open-folder style operations proceed. Reject escaped child names and out-of-root targets instead of attempting silent sanitization, and preserve valid in-root behavior so the user-visible content tabs keep working.</action>
  <verify>npx eslint electron/services/worlds/worldService.ts electron/services/resourcePacks/resourcePackService.ts electron/services/shaders/shaderService.ts && npx tsc --noEmit</verify>
  <done>World, resource-pack, and shader services refuse escaped targets even if earlier layers miss them, while legitimate in-root mutations still succeed.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the same containment guarantees to screenshot and shell-open flows</name>
  <files>electron/services/screenshots/screenshotService.ts</files>
  <action>Harden screenshot copy, delete, rename, and open flows with the same containment policy used by the other content services. Explicitly cover shell-open targets so “open in folder” behavior cannot become the escape hatch left behind after mutation guards are added elsewhere.</action>
  <verify>npx eslint electron/services/screenshots/screenshotService.ts && npx tsc --noEmit</verify>
  <done>Screenshot management and shell-open actions fail closed on escaped paths and stay functional for normal in-root content.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx eslint electron/services/worlds/worldService.ts electron/services/resourcePacks/resourcePackService.ts electron/services/shaders/shaderService.ts electron/services/screenshots/screenshotService.ts`
- [ ] `npx tsc --noEmit`
- [ ] Manual abuse checks confirm delete, rename, copy, and open-folder attempts cannot touch files outside approved roots
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Non-archive content services stop being a second chance for path escapes after handler-level validation
</success_criteria>

<output>
After completion, create `.planning/phases/01-release-baseline-and-trust-boundaries/01-05-SUMMARY.md`
</output>
