---
phase: 01-release-baseline-and-trust-boundaries
plan: "04"
type: execute
wave: 2
depends_on:
  - "01-02"
files_modified:
  - electron/security/pathGuards.ts
  - electron/services/instances/paths.ts
  - electron/ipc/handlers/worldsHandlers.ts
  - electron/ipc/handlers/resourcePacksHandlers.ts
  - electron/ipc/handlers/shadersHandlers.ts
  - electron/ipc/handlers/screenshotsHandlers.ts
autonomous: true
requirements:
  - SEC-01
  - SEC-02
user_setup: []
must_haves:
  truths:
    - World, resource pack, shader, and screenshot requests reject absolute paths, traversal, and cross-root targets before handler work starts.
    - Approved in-root requests still resolve to the same launcher content locations the user expects.
    - The same path-containment rules apply across these content tabs so one escape pattern cannot succeed in one surface while failing in another.
  artifacts:
    - electron/security/pathGuards.ts
    - electron/services/instances/paths.ts
    - electron/ipc/handlers/worldsHandlers.ts
    - electron/ipc/handlers/resourcePacksHandlers.ts
    - electron/ipc/handlers/shadersHandlers.ts
    - electron/ipc/handlers/screenshotsHandlers.ts
  key_links:
    - Canonical path helpers are reused by both root resolution and handler ingress instead of letting each tab reinvent containment checks.
    - Handler-layer containment becomes the shared prerequisite for the later service-level guards in Waves 3.
---

<objective>
Create the canonical path-guard layer and apply it to the path-heavy handler surfaces before service mutation hardening begins.

Purpose: Phase 1 needs one reliable definition of “inside the allowed root” before deeper filesystem or archive changes build on top of it.
Output: Shared path guard helpers plus handler-side containment for worlds, resource packs, shaders, and screenshots.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-CONTEXT.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-RESEARCH.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-VALIDATION.md
@electron/services/instances/paths.ts
@electron/ipc/handlers/worldsHandlers.ts
@electron/ipc/handlers/resourcePacksHandlers.ts
@electron/ipc/handlers/shadersHandlers.ts
@electron/ipc/handlers/screenshotsHandlers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce canonical path guards for approved launcher roots and relative child names</name>
  <files>electron/security/pathGuards.ts, electron/services/instances/paths.ts</files>
  <action>Implement shared helpers that normalize approved roots, validate child-name inputs, and prove a target stays inside the intended directory before any action proceeds. Account for absolute paths, mixed separators, and traversal segments instead of relying on naive prefix checks.</action>
  <verify>npx eslint electron/security/pathGuards.ts electron/services/instances/paths.ts</verify>
  <done>FMCL has one reusable containment policy for launcher roots and child paths instead of multiple incompatible `path.join(...)` conventions.</done>
</task>

<task type="auto">
  <name>Task 2: Apply path guards at handler ingress for content-management actions</name>
  <files>electron/ipc/handlers/worldsHandlers.ts, electron/ipc/handlers/resourcePacksHandlers.ts, electron/ipc/handlers/shadersHandlers.ts, electron/ipc/handlers/screenshotsHandlers.ts</files>
  <action>Use the shared path guards before open, delete, rename, copy, or import-like requests continue into service logic. Keep the existing on-disk layout and public behavior for valid in-root requests; this slice is about blocking escapes early, not redesigning the content tabs.</action>
  <verify>npx eslint electron/ipc/handlers/worldsHandlers.ts electron/ipc/handlers/resourcePacksHandlers.ts electron/ipc/handlers/shadersHandlers.ts electron/ipc/handlers/screenshotsHandlers.ts && npx tsc --noEmit</verify>
  <done>Path-heavy handlers stop accepting out-of-root targets, and valid content-management requests still point at the same user-visible launcher folders.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx eslint electron/security/pathGuards.ts electron/services/instances/paths.ts electron/ipc/handlers/worldsHandlers.ts electron/ipc/handlers/resourcePacksHandlers.ts electron/ipc/handlers/shadersHandlers.ts electron/ipc/handlers/screenshotsHandlers.ts`
- [ ] `npx tsc --noEmit`
- [ ] Manual abuse checks cover `../`, absolute-path, and cross-root inputs across worlds, resource packs, shaders, and screenshots handler entry points
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Handler ingress consistently rejects out-of-root content-management requests before service logic begins
</success_criteria>

<output>
After completion, create `.planning/phases/01-release-baseline-and-trust-boundaries/01-04-SUMMARY.md`
</output>
