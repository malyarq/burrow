---
phase: 11-adaptive-layout-and-interaction-foundations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/components/AppLayout.tsx
  - src/components/Sidebar.tsx
  - src/components/SimplePlayDashboard.tsx
  - src/components/SimplePlayHome.tsx
  - src/components/SettingsPage.tsx
  - src/components/settings/SettingsTabsHeader.tsx
  - src/components/ui/Button.tsx
  - src/components/ui/Input.tsx
  - src/components/ui/Select.tsx
  - src/components/__tests__/Sidebar.keyboard.test.tsx
  - src/components/settings/__tests__/SettingsTabsHeader.a11y.test.tsx
  - src/components/__tests__/AppLayout.responsive.test.tsx
autonomous: true
requirements:
  - ADPT-01
  - ADPT-02
user_setup: []
must_haves:
  truths:
    - FMCL's main shell must stop assuming one lucky window size; the launcher structure needs to remain usable at narrow, default, and wider desktop bounds.
    - Shared control sizing and spacing rhythm must be expressed through foundational shell and primitive seams, not patched independently per route.
    - Classic-mode hero surfaces and settings shell consumers should inherit the same responsive contract instead of diverging into oversized or cramped layouts.
    - This plan establishes the layout and rhythm baseline only; it must not absorb theme preset correctness, launch-stage modeling, or modpack-browser redesign.
  artifacts:
    - src/index.css
    - src/components/AppLayout.tsx
    - src/components/Sidebar.tsx
    - src/components/SimplePlayDashboard.tsx
    - src/components/SimplePlayHome.tsx
    - src/components/SettingsPage.tsx
    - src/components/settings/SettingsTabsHeader.tsx
    - src/components/ui/Button.tsx
    - src/components/ui/Input.tsx
    - src/components/ui/Select.tsx
  key_links:
    - Reuse the existing shared primitive layer and shell seams from `v0.2.0` instead of reopening design-system scope.
    - Keep this slice focused on adaptive behavior and rhythm; later phases will consume this contract for theme truth and settings simplification.
---

<objective>
Normalize FMCL's responsive shell and surface rhythm.

Purpose: establish one adaptive layout and sizing baseline across the foundational launcher shell before overlay, theme, and workflow fixes build on top of it.
Output: shell, dashboard, settings shell, and core form controls that remain orderly and usable across common desktop window sizes.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-adaptive-layout-and-interaction-foundations/11-RESEARCH.md
@.planning/phases/11-adaptive-layout-and-interaction-foundations/11-VALIDATION.md
@src/index.css
@src/components/AppLayout.tsx
@src/components/Sidebar.tsx
@src/components/SimplePlayDashboard.tsx
@src/components/SimplePlayHome.tsx
@src/components/SettingsPage.tsx
@src/components/settings/SettingsTabsHeader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the adaptive shell and shared sizing contract</name>
  <files>src/index.css, src/components/AppLayout.tsx, src/components/Sidebar.tsx, src/components/SimplePlayDashboard.tsx, src/components/SimplePlayHome.tsx, src/components/SettingsPage.tsx, src/components/settings/SettingsTabsHeader.tsx, src/components/ui/Button.tsx, src/components/ui/Input.tsx, src/components/ui/Select.tsx</files>
  <action>Refine the shared shell and primitive sizing rules so FMCL behaves coherently across narrow, default, and wide desktop bounds. Normalize sidebar widths and collapse behavior, content min-width and overflow handling, panel padding, and foundational button/input/select sizing where they materially affect layout rhythm. Keep the work product-facing and brownfield-safe: remove the most obvious fixed-layout assumptions and mixed control sizing without turning this slice into a broad theme or route redesign.</action>
  <verify>npx eslint src/components/AppLayout.tsx src/components/Sidebar.tsx src/components/SimplePlayDashboard.tsx src/components/SimplePlayHome.tsx src/components/SettingsPage.tsx src/components/settings/SettingsTabsHeader.tsx src/components/ui/Button.tsx src/components/ui/Input.tsx src/components/ui/Select.tsx && npx tsc --noEmit</verify>
  <done>FMCL has one responsive shell and control-rhythm baseline that later phases can trust instead of re-tuning per screen.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the shell baseline with focused structure and keyboard coverage</name>
  <files>src/components/__tests__/Sidebar.keyboard.test.tsx, src/components/settings/__tests__/SettingsTabsHeader.a11y.test.tsx, src/components/__tests__/AppLayout.responsive.test.tsx</files>
  <action>Extend the focused shell tests so the new layout contract is not only visual. Cover the sidebar collapse or navigation seam, tab-header behavior, and at least one structural responsive expectation in the layout shell. Keep these tests narrow and deterministic: they should prove resilient structure and safe interaction states, not attempt pixel-perfect rendering assertions.</action>
  <verify>npx vitest run src/components/__tests__/Sidebar.keyboard.test.tsx src/components/settings/__tests__/SettingsTabsHeader.a11y.test.tsx src/components/__tests__/AppLayout.responsive.test.tsx</verify>
  <done>The responsive shell and sizing baseline is regression-protected instead of depending on manual memory.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/__tests__/Sidebar.keyboard.test.tsx src/components/settings/__tests__/SettingsTabsHeader.a11y.test.tsx src/components/__tests__/AppLayout.responsive.test.tsx`
- [ ] `npx eslint src/components/AppLayout.tsx src/components/Sidebar.tsx src/components/SimplePlayDashboard.tsx src/components/SimplePlayHome.tsx src/components/SettingsPage.tsx src/components/settings/SettingsTabsHeader.tsx src/components/ui/Button.tsx src/components/ui/Input.tsx src/components/ui/Select.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- FMCL's foundational shell no longer depends on one lucky window size or mixed control dimensions
</success_criteria>

<output>
After completion, create `.planning/phases/11-adaptive-layout-and-interaction-foundations/11-01-SUMMARY.md`
</output>
