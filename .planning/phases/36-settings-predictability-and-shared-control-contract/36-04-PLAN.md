---
phase: 36-settings-predictability-and-shared-control-contract
plan: "04"
type: execute
wave: 4
depends_on:
  - 36-01
  - 36-02
  - 36-03
files_modified:
  - src/components/settings/tabs/StorageTab.tsx
  - src/features/settings/statistics/StatisticsTab.tsx
  - src/components/__tests__/SettingsPage.storage.test.tsx
  - src/components/__tests__/SettingsPage.statistics.test.tsx
  - src/features/settings/statistics/__tests__/StatisticsTab.layout.test.tsx
  - src/features/settings/statistics/__tests__/StatisticsTab.test.tsx
  - src/verification/manual/scenarios.tsx
  - src/verification/manual/views.ts
  - src/verification/manual/__tests__/appearanceProof.test.tsx
  - src/verification/manual/__tests__/views.test.ts
autonomous: true
requirements:
  - SETTINGS-05
  - SETTINGS-06
  - SETTINGS-07
  - SETTINGS-08
  - DESIGN-01
user_setup: []
must_haves:
  truths:
    - Embedded utility surfaces inside settings follow the same shell and copy contract established by the earlier plans.
    - The final manual proof harness describes direct settings feedback closure instead of older Phase 30 appearance language.
    - This plan is a product-truth safeguard and depends on the earlier execution waves; it must not replace missing product work with proof-only copy.
    - Closeout routes explicitly ask reviewers to check duplicate-copy removal, preset predictability, shared control geometry, and visible-effect scope.
  artifacts:
    - src/components/settings/tabs/StorageTab.tsx
    - src/features/settings/statistics/StatisticsTab.tsx
    - src/verification/manual/scenarios.tsx
    - src/verification/manual/views.ts
    - src/verification/manual/__tests__/appearanceProof.test.tsx
    - src/verification/manual/__tests__/views.test.ts
  key_links:
    - `36-RESEARCH.md` identifies stale settings proof language as a risk because earlier closeouts already treated technical truth as sufficient product truth
    - `36-VALIDATION.md` assigns manual proof and embedded utility-surface honesty to this final plan
    - `src/features/settings/statistics/StatisticsTab.tsx` and `src/components/settings/tabs/StorageTab.tsx` are the last embedded settings surfaces most likely to drift back into duplicate-copy behavior
---

<objective>
Finish the settings closure packet and make the proof harness describe the real contract.

Purpose: ensure the last embedded settings surfaces inherit the new shell contract and that future walkthroughs review the actual Phase 36 behaviors instead of stale settings wording.
Output: utility tabs and manual verification routes that are aligned to the direct feedback contract and can support honest phase closeout.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ru/product-feedback-2026-04-20.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-settings-predictability-and-shared-control-contract/36-RESEARCH.md
@.planning/phases/36-settings-predictability-and-shared-control-contract/36-VALIDATION.md
@src/components/settings/tabs/StorageTab.tsx
@src/features/settings/statistics/StatisticsTab.tsx
@src/verification/manual/scenarios.tsx
@src/verification/manual/views.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keep storage and statistics honest inside the new settings shell contract</name>
  <files>src/components/settings/tabs/StorageTab.tsx, src/features/settings/statistics/StatisticsTab.tsx, src/components/__tests__/SettingsPage.storage.test.tsx, src/components/__tests__/SettingsPage.statistics.test.tsx, src/features/settings/statistics/__tests__/StatisticsTab.layout.test.tsx, src/features/settings/statistics/__tests__/StatisticsTab.test.tsx</files>
  <action>Finish the embedded settings cleanup by making storage and statistics inherit the new shell-level copy and layout rules without reintroducing standalone heroes, duplicated explanatory prose, or utility-only drift. Add or update the embedded route tests so these surfaces stay honest once the earlier settings shell and control work lands.</action>
  <verify>npx vitest run src/components/__tests__/SettingsPage.storage.test.tsx src/components/__tests__/SettingsPage.statistics.test.tsx src/features/settings/statistics/__tests__/StatisticsTab.layout.test.tsx src/features/settings/statistics/__tests__/StatisticsTab.test.tsx && npx eslint src/components/settings/tabs/StorageTab.tsx src/features/settings/statistics/StatisticsTab.tsx && npx tsc --noEmit</verify>
  <done>The last embedded settings utility surfaces no longer behave like separate mini-apps with their own competing copy contract.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh manual settings proof and lock it against stale preset-era wording</name>
  <files>src/verification/manual/scenarios.tsx, src/verification/manual/views.ts, src/verification/manual/__tests__/appearanceProof.test.tsx, src/verification/manual/__tests__/views.test.ts</files>
  <action>Rewrite the settings proof descriptions and readiness text so they explicitly direct reviewers to check duplicate-copy removal, preset predictability, aligned control geometry, and visible-effect scope. Keep the assertions narrow and product-facing so future closeouts cannot silently fall back to the older “preset ancestry and bounded customization” wording alone.</action>
  <verify>npx vitest run src/verification/manual/__tests__/appearanceProof.test.tsx src/verification/manual/__tests__/views.test.ts && npx eslint src/verification/manual/scenarios.tsx src/verification/manual/views.ts && npx tsc --noEmit</verify>
  <done>The settings proof harness now reflects the real Phase 36 contract and will fail when it drifts back to stale milestone language.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/__tests__/SettingsPage.storage.test.tsx src/components/__tests__/SettingsPage.statistics.test.tsx src/features/settings/statistics/__tests__/StatisticsTab.layout.test.tsx src/features/settings/statistics/__tests__/StatisticsTab.test.tsx src/verification/manual/__tests__/appearanceProof.test.tsx src/verification/manual/__tests__/views.test.ts`
- [ ] `npx eslint src/components/settings/tabs/StorageTab.tsx src/features/settings/statistics/StatisticsTab.tsx src/verification/manual/scenarios.tsx src/verification/manual/views.ts`
- [ ] `npx tsc --noEmit`
- [ ] Manual settings-proof review completed: proof routes tell reviewers to check duplicate-copy removal, preset predictability, aligned controls, and visible-effect scope
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Future closeout work cannot claim Phase 36 complete while still reviewing outdated settings assumptions
</success_criteria>

<output>
After completion, create `.planning/phases/36-settings-predictability-and-shared-control-contract/36-04-SUMMARY.md`
</output>
