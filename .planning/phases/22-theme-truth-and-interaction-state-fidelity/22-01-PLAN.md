---
phase: 22-theme-truth-and-interaction-state-fidelity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/contexts/SettingsContext.tsx
  - src/contexts/settings/theme.ts
  - src/contexts/settings/accent.ts
  - src/contexts/settings/theme-presets.ts
  - src/contexts/settings/persistence.ts
  - src/contexts/settings/i18n.ts
  - src/utils/format.ts
  - src/contexts/settings/__tests__/themeDocument.test.ts
  - src/contexts/settings/__tests__/themeRuntimeContract.test.ts
  - src/components/settings/__tests__/AppearanceTab.presets.test.tsx
  - src/utils/__tests__/format.test.ts
autonomous: true
requirements:
  - THEME-02
  - THEME-03
  - THEME-04
user_setup: []
must_haves:
  truths:
    - Theme tokens, accent derivation, preset identity, and locale formatting must have one runtime source of truth instead of drifting between document variables, imported settings payloads, and ambient browser locale defaults.
    - Imported and persisted presets must retain a truthful `presetId` so a preset does not silently degrade into an anonymous custom theme after round-tripping through storage or import/export.
    - Locale-sensitive formatting must follow the active FMCL language contract on phase-owned surfaces rather than inheriting the host environment locale.
    - This plan owns truth-layer contracts only; it must not reopen Phase 12 settings IA or Phase 20 brand and fallback work.
  artifacts:
    - src/index.css
    - src/contexts/SettingsContext.tsx
    - src/contexts/settings/theme.ts
    - src/contexts/settings/accent.ts
    - src/contexts/settings/theme-presets.ts
    - src/contexts/settings/persistence.ts
    - src/contexts/settings/i18n.ts
    - src/utils/format.ts
    - src/contexts/settings/__tests__/themeDocument.test.ts
    - src/contexts/settings/__tests__/themeRuntimeContract.test.ts
    - src/components/settings/__tests__/AppearanceTab.presets.test.tsx
    - src/utils/__tests__/format.test.ts
  key_links:
    - Phase 22 research identified `src/index.css`, `theme.ts`, `accent.ts`, `theme-presets.ts`, `persistence.ts`, and `format.ts` as one ownership seam for token truth, preset identity, and locale-sensitive presentation.
    - Screenshot evidence shows accent drift and weak state contrast downstream, so the truth layer must expose dependable state and formatting inputs before surface-specific fixes can remain stable.
    - Phase 12 already repaired basic preset application; this plan should extend that contract to persistence, import/export, accent derivation, and locale ownership instead of redesigning presets again.
---

<objective>
Repair the runtime truth layer for theme tokens, accent propagation, preset identity, and locale-sensitive formatting.

Purpose: give Phase 22 a stable state contract before any shared-control or route-level state work lands.
Output: one reliable theme/accent/preset/locale ownership seam plus regression coverage for preset round-trip and app-locale formatting truth.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-theme-truth-and-interaction-state-fidelity/22-RESEARCH.md
@.planning/phases/22-theme-truth-and-interaction-state-fidelity/22-VALIDATION.md
@src/index.css
@src/contexts/SettingsContext.tsx
@src/contexts/settings/theme.ts
@src/contexts/settings/accent.ts
@src/contexts/settings/theme-presets.ts
@src/contexts/settings/persistence.ts
@src/contexts/settings/i18n.ts
@src/utils/format.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair the runtime source of truth for tokens, accent derivation, preset identity, and locale formatting</name>
  <files>src/index.css, src/contexts/SettingsContext.tsx, src/contexts/settings/theme.ts, src/contexts/settings/accent.ts, src/contexts/settings/theme-presets.ts, src/contexts/settings/persistence.ts, src/contexts/settings/i18n.ts, src/utils/format.ts</files>
  <action>Refactor the theme runtime seam so FMCL has one explicit contract for document tokens, accent state colors, preset identity persistence, and locale-aware formatting. Preserve the existing Phase 12 preset model, but close the round-trip and app-locale gaps so imported or persisted presets and rendered dates or numbers remain truthful without relying on browser defaults.</action>
  <verify>git diff --check -- src/index.css && npx eslint src/contexts/SettingsContext.tsx src/contexts/settings/theme.ts src/contexts/settings/accent.ts src/contexts/settings/theme-presets.ts src/contexts/settings/persistence.ts src/contexts/settings/i18n.ts src/utils/format.ts && npx tsc --noEmit</verify>
  <done>The runtime theme layer exposes one dependable source of truth for preset identity, accent derivation, and app-locale formatting.</done>
</task>

<task type="auto">
  <name>Task 2: Add truth-layer regression coverage for preset round-trip, accent contract, and app-locale formatting</name>
  <files>src/contexts/settings/__tests__/themeDocument.test.ts, src/contexts/settings/__tests__/themeRuntimeContract.test.ts, src/components/settings/__tests__/AppearanceTab.presets.test.tsx, src/utils/__tests__/format.test.ts</files>
  <action>Extend the current theme tests with contract-level coverage proving that presets keep their identity through persistence or import-export, that theme token and accent derivation stay coherent, and that locale-sensitive formatting follows the active FMCL language rather than the host environment locale. Reuse existing theme and appearance seams wherever possible instead of inventing a new harness.</action>
  <verify>npx vitest run src/contexts/settings/__tests__/themeDocument.test.ts src/contexts/settings/__tests__/themeRuntimeContract.test.ts src/components/settings/__tests__/AppearanceTab.presets.test.tsx src/utils/__tests__/format.test.ts && npx tsc --noEmit</verify>
  <done>The truth layer is regression-protected for preset identity, accent derivation, and locale-sensitive formatting.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/contexts/settings/__tests__/themeDocument.test.ts src/contexts/settings/__tests__/themeRuntimeContract.test.ts src/components/settings/__tests__/AppearanceTab.presets.test.tsx src/utils/__tests__/format.test.ts`
- [ ] `git diff --check -- src/index.css`
- [ ] `npx eslint src/contexts/SettingsContext.tsx src/contexts/settings/theme.ts src/contexts/settings/accent.ts src/contexts/settings/theme-presets.ts src/contexts/settings/persistence.ts src/contexts/settings/i18n.ts src/utils/format.ts`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- FMCL exposes one truthful runtime seam for theme tokens, accent propagation, preset identity, and locale-aware formatting
</success_criteria>

<output>
After completion, create `.planning/phases/22-theme-truth-and-interaction-state-fidelity/22-01-SUMMARY.md`
</output>
