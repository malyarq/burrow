---
phase: 13-launch-trust-and-modpack-workflow-ergonomics
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/modpacks/CreateModpackModal.tsx
  - src/components/modpacks/ModpackCreationWizard.tsx
  - src/components/sidebar/ModloaderSection.tsx
  - electron/services/modpacks/modpackService.ts
  - src/components/modpacks/__tests__/CreateModpackDependencies.test.tsx
  - electron/services/modpacks/__tests__/modpackService.createLocalDependencies.test.ts
autonomous: true
requirements:
  - MPUX-01
user_setup: []
must_haves:
  truths:
    - Create-modpack dependency truth must come from the same data that gets persisted, not from a prettier summary layered on top of lossy runtime metadata.
    - Local modpack creation must stop emitting malformed manifest loader identifiers such as `forge-`, `fabric-`, or `neoforge-`.
    - The minimum truthful contract is explicit Minecraft version, explicit loader type when selected, and loader version only when it is actually known.
    - This plan must not expand into full remote loader-version resolution or a general modpack manifest redesign.
  artifacts:
    - src/components/modpacks/CreateModpackModal.tsx
    - src/components/modpacks/ModpackCreationWizard.tsx
    - src/components/sidebar/ModloaderSection.tsx
    - electron/services/modpacks/modpackService.ts
  key_links:
    - Phase 13 research found that the current create flow writes incomplete dependency data and malformed local manifest loader IDs, so UI truth and persistence must be repaired together.
    - Phase 12 already flattened settings IA; this plan should stay focused on the creation flow and dependency truth rather than reopening broader modal or form styling work.
---

<objective>
Make local modpack creation show and persist runtime dependencies truthfully.

Purpose: ensure users can see the Minecraft and loader dependencies they are creating, and ensure the resulting local pack does not silently store malformed or misleading dependency metadata.
Output: a truthful create-modpack dependency contract, corrected local manifest generation, and focused renderer plus service coverage.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-launch-trust-and-modpack-workflow-ergonomics/13-RESEARCH.md
@.planning/phases/13-launch-trust-and-modpack-workflow-ergonomics/13-VALIDATION.md
@src/components/modpacks/CreateModpackModal.tsx
@src/components/modpacks/ModpackCreationWizard.tsx
@src/components/sidebar/ModloaderSection.tsx
@electron/services/modpacks/modpackService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair the local create-modpack dependency write path</name>
  <files>electron/services/modpacks/modpackService.ts, src/components/modpacks/CreateModpackModal.tsx, src/components/modpacks/ModpackCreationWizard.tsx, src/components/sidebar/ModloaderSection.tsx</files>
  <action>Align the local creation flow so the runtime dependency information collected by the renderer is the same dependency information persisted to the modpack config and manifest. Remove malformed loader identifier generation, make the no-version case truthful instead of stringly, and keep the existing brownfield creation flow intact without inventing a full loader-resolution backend.</action>
  <verify>npx eslint src/components/modpacks/CreateModpackModal.tsx src/components/modpacks/ModpackCreationWizard.tsx src/components/sidebar/ModloaderSection.tsx electron/services/modpacks/modpackService.ts && npx tsc --noEmit</verify>
  <done>Local modpack creation writes truthful dependency metadata and no longer leaks broken manifest identifiers.</done>
</task>

<task type="auto">
  <name>Task 2: Surface dependency truth before confirmation and lock it with renderer plus service tests</name>
  <files>src/components/modpacks/CreateModpackModal.tsx, src/components/modpacks/ModpackCreationWizard.tsx, src/components/modpacks/__tests__/CreateModpackDependencies.test.tsx, electron/services/modpacks/__tests__/modpackService.createLocalDependencies.test.ts</files>
  <action>Expose a clear dependency summary in the create-modpack flow so the user can see the Minecraft version and selected loader before confirming creation. Add focused tests that prove the summary stays in sync with the selected options and that local manifest output remains truthful under the loader combinations Phase 13 owns.</action>
  <verify>npx vitest run src/components/modpacks/__tests__/CreateModpackDependencies.test.tsx electron/services/modpacks/__tests__/modpackService.createLocalDependencies.test.ts</verify>
  <done>Create-modpack dependencies are visible, trustworthy, and regression-protected at both the renderer and service seams.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/modpacks/__tests__/CreateModpackDependencies.test.tsx electron/services/modpacks/__tests__/modpackService.createLocalDependencies.test.ts`
- [ ] `npx eslint src/components/modpacks/CreateModpackModal.tsx src/components/modpacks/ModpackCreationWizard.tsx src/components/sidebar/ModloaderSection.tsx electron/services/modpacks/modpackService.ts`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Local modpack creation now shows and persists Minecraft and loader dependencies truthfully instead of relying on lossy or malformed data
</success_criteria>

<output>
After completion, create `.planning/phases/13-launch-trust-and-modpack-workflow-ergonomics/13-02-SUMMARY.md`
</output>
