---
phase: 01-release-baseline-and-trust-boundaries
plan: "10"
type: execute
wave: 4
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
  - "01-04"
  - "01-05"
  - "01-06"
  - "01-07"
  - "01-08"
  - "01-09"
files_modified:
  - src/components/modpacks/ModpackList.tsx
  - src/components/modpacks/ImportModpackPreviewModal.tsx
  - src/components/modpacks/ImportModpackPreviewPage.tsx
  - src/components/settings/tabs/AppearanceTab.tsx
  - src/contexts/ConfirmContext.tsx
  - src/components/ui/ConfirmDialog.tsx
  - src/locales/en.json
  - src/locales/ru.json
autonomous: true
requirements:
  - REL-01
  - REL-02
user_setup:
  - Install workspace dependencies with `npm ci` before running repo-wide release-gate verification if `node_modules/` is absent.
must_haves:
  truths:
    - The remaining known warning-only files no longer block the real release gate under `eslint . --max-warnings 0`.
    - Secondary modpack flows such as rename, duplicate, and import preview use FMCL-controlled retry and confirmation behavior instead of browser-native prompts or brittle silent failure paths.
    - Repo-wide lint, type, contract, IPC, and build gates are green after the entire Phase 1 plan set lands.
  artifacts:
    - src/components/modpacks/ModpackList.tsx
    - src/components/modpacks/ImportModpackPreviewModal.tsx
    - src/components/modpacks/ImportModpackPreviewPage.tsx
    - src/components/settings/tabs/AppearanceTab.tsx
    - src/contexts/ConfirmContext.tsx
    - src/components/ui/ConfirmDialog.tsx
  key_links:
    - Secondary renderer surfaces reuse the confirmation and inline-recovery primitives already established in `01-01` instead of reintroducing browser-native prompts.
    - The final release gate is owned explicitly here, after all security and renderer slices have had a chance to add lint or type fallout.
---

<objective>
Close the remaining warning-prone renderer gaps and prove the full release gate after all Phase 1 work lands.

Purpose: `REL-02` is not satisfied by partial `src/` lint checks when the actual ship bar is repo-wide `eslint . --max-warnings 0` plus the existing type, contract, IPC, and build gates.
Output: Cleaned-up secondary renderer surfaces for modpack list, import preview, and appearance settings, plus an explicit final release-gate verification slice.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-CONTEXT.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-RESEARCH.md
@.planning/phases/01-release-baseline-and-trust-boundaries/01-VALIDATION.md
@docs/KNOWN_ISSUES.md
@src/components/modpacks/ModpackList.tsx
@src/components/modpacks/ImportModpackPreviewModal.tsx
@src/components/modpacks/ImportModpackPreviewPage.tsx
@src/components/settings/tabs/AppearanceTab.tsx
@src/contexts/ConfirmContext.tsx
@src/components/ui/ConfirmDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove remaining browser-native prompt and retry-hostile behavior from secondary modpack flows</name>
  <files>src/components/modpacks/ModpackList.tsx, src/components/modpacks/ImportModpackPreviewModal.tsx, src/components/modpacks/ImportModpackPreviewPage.tsx, src/contexts/ConfirmContext.tsx, src/components/ui/ConfirmDialog.tsx, src/locales/en.json, src/locales/ru.json</files>
  <action>Replace `window.prompt` or equivalent brittle secondary-flow behavior in modpack list rename or duplicate and import-preview actions with FMCL-controlled dialog or inline recovery patterns. Keep scope focused on the remaining release-gate blockers and secondary UX surfaces moved out of `01-01`; do not reopen primary accounts or share flows here.</action>
  <verify>npx eslint src/components/modpacks/ModpackList.tsx src/components/modpacks/ImportModpackPreviewModal.tsx src/components/modpacks/ImportModpackPreviewPage.tsx src/contexts/ConfirmContext.tsx src/components/ui/ConfirmDialog.tsx && npx tsc --noEmit</verify>
  <done>Secondary modpack flows stay inside FMCL with explicit retry and confirmation behavior instead of relying on browser-native prompts or silent failure paths.</done>
</task>

<task type="auto">
  <name>Task 2: Clear warning-only lint and typing debt in `ModpackList` and `AppearanceTab`</name>
  <files>src/components/modpacks/ModpackList.tsx, src/components/settings/tabs/AppearanceTab.tsx, src/locales/en.json, src/locales/ru.json</files>
  <action>Resolve the known `no-explicit-any` and related warning-prone issues in `ModpackList.tsx` and `AppearanceTab.tsx`, and localize any newly surfaced user-facing strings while keeping the UI behavior intact. Use concrete types or `unknown`, not `any`, and avoid broad theme or launcher refactors in this final cleanup slice.</action>
  <verify>npx eslint src/components/modpacks/ModpackList.tsx src/components/settings/tabs/AppearanceTab.tsx && npx tsc --noEmit</verify>
  <done>The known warning-only files no longer block repo-wide lint, and the cleanup stays contained to the remaining release-gate hotspots.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the repo-wide Phase 1 release gate after all plan slices land</name>
  <files>src/components/modpacks/ModpackList.tsx, src/components/settings/tabs/AppearanceTab.tsx</files>
  <action>Run the real ship bar for this phase and fix any residual fallout revealed by the full suite, with special attention to warning-level lint failures that still trip `eslint . --max-warnings 0`. Treat this as closeout verification for the whole phase, not a place to introduce new feature work.</action>
  <verify>npm run lint && npx tsc --noEmit && npm run contracts:check && npm run ipc:check && npm run build -- --publish never</verify>
  <done>The repo-wide release gate is green after the entire Phase 1 plan set, and any remaining red signal is proven to be outside the planned Phase 1 scope.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint`
- [ ] `npx tsc --noEmit`
- [ ] `npm run contracts:check`
- [ ] `npm run ipc:check`
- [ ] `npm run build -- --publish never`
- [ ] Manual smoke covers modpack rename or duplicate, import preview rejection or retry, and appearance theme import or export after the final cleanup
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `REL-02` is covered by the actual repo-wide release gate instead of partial lint approximations
</success_criteria>

<output>
After completion, create `.planning/phases/01-release-baseline-and-trust-boundaries/01-10-SUMMARY.md`
</output>
