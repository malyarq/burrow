---
phase: 17-catalog-compact-nav-and-settings-localization-polish
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sidebar/SidebarHeader.tsx
  - src/components/sidebar/__tests__/SidebarHeader.compact-mode.test.tsx
autonomous: true
requirements:
  - CATALOG-03
user_setup: []
must_haves:
  truths:
    - The collapsed modpacks destination must stop rendering as a lone placeholder-like `M` and instead use a coherent compact-state affordance that matches the rest of the sidebar language.
    - Active-state visibility and keyboard semantics must stay intact while the collapsed presentation changes.
    - This plan owns compact-sidebar coherence only; it must not reopen broader launcher-shell or route-architecture work.
    - The current two-mode switch and `UIMode` model remain authoritative; the repair is visual and interaction polish, not a new navigation system.
  artifacts:
    - src/components/sidebar/SidebarHeader.tsx
    - src/components/sidebar/__tests__/SidebarHeader.compact-mode.test.tsx
  key_links:
    - Phase 17 research traced the audit screenshot directly to `SidebarHeader.tsx`, where collapsed `simple` mode renders as an icon but collapsed `modpacks` renders as the literal `M`.
    - Existing sidebar accessibility coverage sits above `SidebarHeader`, so this phase needs focused compact-mode proof at the header seam rather than relying on the current higher-level sidebar test alone.
---

<objective>
Make the collapsed launcher navigation read like an intentional compact shell.

Purpose: replace the placeholder-like collapsed modpacks destination with a coherent compact-state affordance while preserving the current mode-switch behavior and active-state clarity.
Output: a collapsed sidebar header that keeps the active destination understandable without stray single-letter output.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-catalog-compact-nav-and-settings-localization-polish/17-RESEARCH.md
@.planning/phases/17-catalog-compact-nav-and-settings-localization-polish/17-VALIDATION.md
@src/components/sidebar/SidebarHeader.tsx
@src/components/Sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the collapsed modpacks letter affordance with a coherent compact-mode representation</name>
  <files>src/components/sidebar/SidebarHeader.tsx</files>
  <action>Refactor the collapsed mode-switch UI so the modpacks destination no longer appears as a single placeholder-like letter while the simple destination uses an icon. Keep the current `UIMode` switch, titles, and active/inactive emphasis model, but make both collapsed options feel intentionally related instead of mixing iconography with leftover text.</action>
  <verify>npx eslint src/components/sidebar/SidebarHeader.tsx && npx tsc --noEmit</verify>
  <done>The collapsed sidebar no longer leaks the stray `M` affordance and instead presents a coherent compact navigation state for both destinations.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused compact-mode regression coverage at the header seam</name>
  <files>src/components/sidebar/__tests__/SidebarHeader.compact-mode.test.tsx</files>
  <action>Create a focused header test that proves the collapsed mode switch no longer renders a placeholder-like letter for modpacks, preserves an obvious active-state affordance, and retains accessible names or titles for both destinations. Keep this coverage at the header seam so future compact-nav regressions are caught even if higher-level sidebar tests continue mocking the header component.</action>
  <verify>npx vitest run src/components/sidebar/__tests__/SidebarHeader.compact-mode.test.tsx</verify>
  <done>The compact navigation repair is regression-protected exactly where the audit defect originates.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/sidebar/__tests__/SidebarHeader.compact-mode.test.tsx`
- [ ] `npx eslint src/components/sidebar/SidebarHeader.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The collapsed sidebar keeps a coherent active destination without any placeholder-like single-letter affordance
</success_criteria>

<output>
After completion, create `.planning/phases/17-catalog-compact-nav-and-settings-localization-polish/17-02-SUMMARY.md`
</output>
