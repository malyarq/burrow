---
phase: 19-baseline-stability-scope-and-shell-invariants
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AppLayout.tsx
  - src/components/TitleBar.tsx
  - src/components/Sidebar.tsx
  - src/components/sidebar/SidebarHeader.tsx
  - src/components/__tests__/AppLayout.responsive.test.tsx
autonomous: true
requirements:
  - SHELL-01
user_setup: []
must_haves:
  truths:
    - Phase 19 must establish one shared top-safe contract below the custom title bar instead of letting routes and shell fragments compensate independently.
    - The sidebar collapse strip must stop acting like a second top-edge geometry system with its own local padding workaround.
    - This plan owns shell-safe layout only; it must not absorb CTA hierarchy or dense-route action-flow work from later plans.
    - The repair must live in shared shell seams, not in per-route `pt-*`, sticky offsets, or screenshot-specific spacing patches.
  artifacts:
    - src/components/AppLayout.tsx
    - src/components/TitleBar.tsx
    - src/components/Sidebar.tsx
    - src/components/sidebar/SidebarHeader.tsx
    - src/components/__tests__/AppLayout.responsive.test.tsx
  key_links:
    - Phase 19 research traced the top-edge bug to `AppLayout` having no explicit safe-area seam below `TitleBar` while `Sidebar` adds its own absolute collapse strip and local `pt-6` compensation.
    - The screenshot audit's `R1` defect is milestone-owned shell breakage, so this plan must create a reusable baseline other routes can inherit rather than nudging individual pages downward.
---

<objective>
Define one shared shell top-safe contract for the shipped desktop layout.

Purpose: make every major launcher surface begin below the custom title bar through a shared shell seam instead of route-local padding or sidebar-only compensation.
Output: a reusable shell-safe layout contract that clears the title bar and removes the sidebar's duplicate top-edge spacing logic.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-baseline-stability-scope-and-shell-invariants/19-CONTEXT.md
@.planning/phases/19-baseline-stability-scope-and-shell-invariants/19-RESEARCH.md
@.planning/phases/19-baseline-stability-scope-and-shell-invariants/19-VALIDATION.md
@src/components/AppLayout.tsx
@src/components/TitleBar.tsx
@src/components/Sidebar.tsx
@src/components/sidebar/SidebarHeader.tsx
@src/components/__tests__/AppLayout.responsive.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce one shell-owned safe-area seam below the custom title bar</name>
  <files>src/components/AppLayout.tsx, src/components/TitleBar.tsx, src/components/Sidebar.tsx, src/components/sidebar/SidebarHeader.tsx</files>
  <action>Refactor the shell so the space below the custom title bar is owned by one shared layout contract instead of a mix of natural flow and sidebar-local compensation. Remove or absorb the sidebar collapse strip's local top-padding workaround into that shared contract, and keep the result independent of current route so later screens inherit it automatically.</action>
  <verify>npx eslint src/components/AppLayout.tsx src/components/TitleBar.tsx src/components/Sidebar.tsx src/components/sidebar/SidebarHeader.tsx && npx tsc --noEmit</verify>
  <done>Major launcher surfaces inherit one explicit title-bar-safe shell seam, and the sidebar no longer needs its own parallel top-edge spacing system.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the shell-safe contract with focused structural regression coverage</name>
  <files>src/components/__tests__/AppLayout.responsive.test.tsx</files>
  <action>Expand the shared shell test seam so it proves the new top-safe layout contract exists at `AppLayout` level rather than depending on route-local spacing. Keep the test structural and route-agnostic: it should verify the shell establishes the safe-area wrapper or equivalent contract, not pixel-tune individual screens.</action>
  <verify>npx vitest run src/components/__tests__/AppLayout.responsive.test.tsx</verify>
  <done>The top-safe shell contract is regression-protected at the shared layout seam instead of relying on screenshot memory.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/__tests__/AppLayout.responsive.test.tsx`
- [ ] `npx eslint src/components/AppLayout.tsx src/components/TitleBar.tsx src/components/Sidebar.tsx src/components/sidebar/SidebarHeader.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The shipped shell owns title-bar clearance through one reusable seam instead of route-local or sidebar-local spacing hacks
</success_criteria>

<output>
After completion, create `.planning/phases/19-baseline-stability-scope-and-shell-invariants/19-01-SUMMARY.md`
</output>
