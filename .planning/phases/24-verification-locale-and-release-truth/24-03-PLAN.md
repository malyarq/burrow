---
phase: 24-verification-locale-and-release-truth
plan: "03"
type: execute
wave: 3
depends_on:
  - 24-01
  - 24-02
files_modified:
  - package.json
  - playwright.config.ts
  - tests/visual/manual-closeout.spec.ts
  - tests/visual/manual-closeout.spec.ts-snapshots/
  - src/verification/manual/views.ts
  - .planning/phases/24-verification-locale-and-release-truth/24-VALIDATION.md
autonomous: true
requirements:
  - VER-01
  - VER-02
  - VER-03
user_setup:
  - Install the committed browser dependency for the screenshot lane with `npx playwright install chromium`.
  - If the closeout registry changed and no baselines exist yet, seed the initial snapshots with `npx playwright test tests/visual/manual-closeout.spec.ts --project=chromium --update-snapshots` before running the strict gate.
must_haves:
  truths:
    - Screenshot regression must be strict but bounded to the milestone-owned Phase 24 closeout registry; this plan must not freeze unrelated legacy routes or keep one-off CDP scripts as the primary gate.
    - The Playwright lane must consume closeout registry metadata directly so navigation, screenshot ownership, and proof docs cannot drift independently.
    - `24-VALIDATION.md` remains the authoritative requirement-to-view and requirement-to-test map and must be refreshed from the landed closeout set rather than duplicated in ad hoc notes.
    - This plan owns the committed visual regression contract and validation map only; it must not rewrite release docs or expand closeout scope beyond the curated proof set.
  artifacts:
    - package.json
    - playwright.config.ts
    - tests/visual/manual-closeout.spec.ts
    - tests/visual/manual-closeout.spec.ts-snapshots/
    - src/verification/manual/views.ts
    - .planning/phases/24-verification-locale-and-release-truth/24-VALIDATION.md
  key_links:
    - Research confirmed that the repo currently has no Playwright dependency, config, or checked-in visual lane, so Phase 24 must add that contract explicitly.
    - The recommended screenshot runner behavior is to open `manual-verification.html?view=<id>`, wait for `#verification-status` to report `{ ready: true }`, and capture one strict screenshot per closeout view.
    - The validation draft already positions `24-VALIDATION.md` as the authoritative matrix between `VER-01` through `VER-04`, the closeout views, and the final quick or full commands.
---

<objective>
Add a strict Playwright screenshot regression lane for the closeout registry and lock `24-VALIDATION.md` to the landed proof set.

Purpose: turn the final Phase 24 closeout views into a committed release gate instead of one-off screenshot evidence.
Output: a Chromium-only Playwright visual suite bound to the closeout registry plus an authoritative validation map that names the owned views, tests, and commands.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-verification-locale-and-release-truth/24-CONTEXT.md
@.planning/phases/24-verification-locale-and-release-truth/24-RESEARCH.md
@.planning/phases/24-verification-locale-and-release-truth/24-VALIDATION.md
@.planning/phases/24-verification-locale-and-release-truth/24-01-PLAN.md
@.planning/phases/24-verification-locale-and-release-truth/24-02-PLAN.md
@package.json
@src/verification/manual/views.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Chromium closeout screenshot lane bound to the Phase 24 registry</name>
  <files>package.json, playwright.config.ts, tests/visual/manual-closeout.spec.ts, src/verification/manual/views.ts</files>
  <action>Add `@playwright/test` if it is still missing, create a Chromium-only Playwright config, and land `tests/visual/manual-closeout.spec.ts` so it iterates the Phase 24 closeout registry instead of hard-coded route strings. Export whatever closeout metadata the suite needs from `src/verification/manual/views.ts`, including the owned view IDs and any view-level viewport or negative-check hints. The spec must open `manual-verification.html?view=<id>`, wait for deterministic `#verification-status` readiness, disable motion, and fail strictly on screenshot drift for the curated closeout set only. If the lane has no committed baselines yet, seed them in this plan and check the resulting snapshots into the owned snapshot directory.</action>
  <verify>npx playwright test tests/visual/manual-closeout.spec.ts --project=chromium --update-snapshots && npx playwright test tests/visual/manual-closeout.spec.ts --project=chromium && npx tsc --noEmit</verify>
  <done>The repo has a committed screenshot regression lane for the owned Phase 24 closeout views instead of one-off capture scripts.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh `24-VALIDATION.md` as the authoritative requirement-to-view and requirement-to-test map</name>
  <files>.planning/phases/24-verification-locale-and-release-truth/24-VALIDATION.md, src/verification/manual/views.ts</files>
  <action>Update `24-VALIDATION.md` from the landed closeout registry and Playwright suite so it remains the single source of truth for `VER-01` through `VER-04`. Replace draft or placeholder references with the actual Phase 24 view IDs, the committed screenshot lane, the quick and full commands, and the requirement-to-view plus requirement-to-test mapping that downstream execution and final verification will consume.</action>
  <verify>git diff --check -- .planning/phases/24-verification-locale-and-release-truth/24-VALIDATION.md playwright.config.ts tests/visual/manual-closeout.spec.ts package.json && npx vitest run src/contexts/settings/__tests__/themeRuntimeContract.test.ts src/utils/__tests__/format.test.ts src/components/settings/__tests__/AppearanceTab.state-fidelity.test.tsx src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx src/components/modpacks/__tests__/AddModPlaceholderTruth.test.tsx src/components/modpacks/__tests__/ModpackUpdateModal.degraded-state.test.tsx src/components/modpacks/details/__tests__/SecondaryContentTabs.test.tsx src/features/screenshots/components/__tests__/ScreenshotsExperience.test.tsx src/features/settings/statistics/__tests__/StatisticsTab.test.tsx src/features/share/__tests__/ShareFlows.test.tsx src/components/__tests__/ErrorBoundary.recovery.test.tsx && npx playwright test tests/visual/manual-closeout.spec.ts --project=chromium && npx tsc --noEmit</verify>
  <done>The validation document is the authoritative closeout matrix for requirements, views, automated proof, and final-gate commands.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `git diff --check -- .planning/phases/24-verification-locale-and-release-truth/24-VALIDATION.md playwright.config.ts tests/visual/manual-closeout.spec.ts package.json`
- [ ] `npx vitest run src/contexts/settings/__tests__/themeRuntimeContract.test.ts src/utils/__tests__/format.test.ts src/components/settings/__tests__/AppearanceTab.state-fidelity.test.tsx src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx src/components/modpacks/__tests__/AddModPlaceholderTruth.test.tsx src/components/modpacks/__tests__/ModpackUpdateModal.degraded-state.test.tsx src/components/modpacks/details/__tests__/SecondaryContentTabs.test.tsx src/features/screenshots/components/__tests__/ScreenshotsExperience.test.tsx src/features/settings/statistics/__tests__/StatisticsTab.test.tsx src/features/share/__tests__/ShareFlows.test.tsx src/components/__tests__/ErrorBoundary.recovery.test.tsx`
- [ ] `npx playwright install chromium`
- [ ] `npx playwright test tests/visual/manual-closeout.spec.ts --project=chromium --update-snapshots`
- [ ] `npx playwright test tests/visual/manual-closeout.spec.ts --project=chromium`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 24 has a committed Playwright screenshot lane and an authoritative validation map bound to the closeout registry
</success_criteria>

<output>
After completion, create `.planning/phases/24-verification-locale-and-release-truth/24-03-SUMMARY.md`
</output>
