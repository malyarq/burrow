---
phase: 16-modpack-detail-integrity-and-discoverable-dense-navigation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/modpacks/details/ModpackDetailsHeader.tsx
  - src/components/modpacks/__tests__/ModpackDetailsHeader.i18n.test.tsx
autonomous: true
requirements:
  - DETAIL-03
user_setup: []
must_haves:
  truths:
    - Primary modpack detail sections must become discoverable without depending on a hidden horizontal-scroll affordance as the default navigation model.
    - The dense-navigation fix must preserve real tab semantics, translated labels, and the current detail-page ownership model rather than introducing a new route architecture.
    - The resulting navigation should still read like the shared FMCL content workspace established earlier, not like a one-off utility strip bolted onto the detail page.
    - This plan owns navigation discoverability only; it must not reopen dependency-state or requirement-copy logic from the sibling mods-tab plan.
  artifacts:
    - src/components/modpacks/details/ModpackDetailsHeader.tsx
    - src/components/modpacks/__tests__/ModpackDetailsHeader.i18n.test.tsx
  key_links:
    - Research identified `ModpackDetailsHeader.tsx` as the exact overflow seam: a single `tablist` row with `overflow-x-auto` and `whitespace-nowrap`.
    - Phase 9 already aligned content-management tabs across launcher and modpack details, so the Phase 16 fix should stay inside that shared surface language rather than reopening route-level IA.
---

<objective>
Make the primary detail sections discoverable on dense desktop layouts.

Purpose: replace the horizontal-scroll-first detail tab strip with a visible dense-navigation pattern while preserving FMCL's current tab semantics and translated labels.
Output: a modpack-details header whose primary sections are visibly reachable without hidden overflow discovery.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-modpack-detail-integrity-and-discoverable-dense-navigation/16-CONTEXT.md
@.planning/phases/16-modpack-detail-integrity-and-discoverable-dense-navigation/16-RESEARCH.md
@.planning/phases/16-modpack-detail-integrity-and-discoverable-dense-navigation/16-VALIDATION.md
@src/components/modpacks/details/ModpackDetailsHeader.tsx
@src/components/modpacks/__tests__/ModpackDetailsHeader.i18n.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the overflow-first header strip with discoverable dense navigation</name>
  <files>src/components/modpacks/details/ModpackDetailsHeader.tsx</files>
  <action>Refactor the modpack-details header so the primary sections remain visibly reachable at the audited desktop widths without relying on default horizontal scrolling. Keep the active-tab model, tab semantics, and current translated labels intact, but move to a denser layout pattern that stays legible and discoverable when the full section set is present.</action>
  <verify>npx eslint src/components/modpacks/details/ModpackDetailsHeader.tsx && npx tsc --noEmit</verify>
  <done>The detail header exposes its main sections as an obvious navigation surface instead of a strip that looks partially clipped until the user discovers horizontal scroll.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the refreshed header contract with focused i18n and discoverability assertions</name>
  <files>src/components/modpacks/__tests__/ModpackDetailsHeader.i18n.test.tsx</files>
  <action>Extend the existing header tests so they still prove translated tab labels and add focused assertions for the refreshed dense-navigation structure or semantics. The test should fail if the header regresses back to a hidden overflow-default pattern or starts leaking raw i18n keys while the navigation changes.</action>
  <verify>npx vitest run src/components/modpacks/__tests__/ModpackDetailsHeader.i18n.test.tsx</verify>
  <done>The detail header's translated, discoverable navigation contract is regression-protected at the component seam.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/modpacks/__tests__/ModpackDetailsHeader.i18n.test.tsx`
- [ ] `npx eslint src/components/modpacks/details/ModpackDetailsHeader.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The primary detail sections stay discoverable on the shipped detail page without hidden horizontal-scroll-first navigation
</success_criteria>

<output>
After completion, create `.planning/phases/16-modpack-detail-integrity-and-discoverable-dense-navigation/16-02-SUMMARY.md`
</output>
