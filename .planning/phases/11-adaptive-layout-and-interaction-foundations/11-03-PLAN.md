---
phase: 11-adaptive-layout-and-interaction-foundations
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - src/components/SimplePlayDashboard.tsx
  - src/components/SimplePlayHome.tsx
  - src/components/TitleBar.tsx
  - src/components/layout/EmptyStateView.tsx
  - src/app/hooks/useAppIcon.ts
  - src/components/ui/LazyImage.tsx
  - public/icon.png
  - src/components/ui/__tests__/LazyImage.cache.test.tsx
  - src/components/__tests__/SimplePlayHome.visualTruth.test.tsx
autonomous: true
requirements:
  - VIS-01
user_setup: []
must_haves:
  truths:
    - Shipped classic and logo-driven surfaces must stop leaking placeholder-looking or broken fallback assets.
    - Asset fallback logic should stay intentional and product-safe across titlebar, empty-state, classic, and logo-heavy surfaces touched by the phase.
    - Fallback cleanup must preserve the responsive and overlay foundations from Plans 11-01 and 11-02 rather than reintroducing fixed-box assumptions.
    - This plan is about shipped visual truth, not theme preset polish or general copy cleanup.
  artifacts:
    - src/components/SimplePlayDashboard.tsx
    - src/components/SimplePlayHome.tsx
    - src/components/TitleBar.tsx
    - src/components/layout/EmptyStateView.tsx
    - src/app/hooks/useAppIcon.ts
    - src/components/ui/LazyImage.tsx
    - public/icon.png
    - src/components/ui/__tests__/LazyImage.cache.test.tsx
    - src/components/__tests__/SimplePlayHome.visualTruth.test.tsx
  key_links:
    - Audit fallback behavior where `/icon.png` and other bundled assets are reused; this phase should close leaks, not multiply fallback branches.
    - Keep the cleanup tied to real shipped surfaces users named: classic mode, logo/easter-egg path, titlebar, and foundational empty states.
---

<objective>
Close placeholder and fallback truth gaps on foundational launcher surfaces.

Purpose: remove the visible “unfinished product” signals that survive on classic and logo-driven paths even after the shared UI refresh.
Output: intentional bundled/fallback asset behavior across the foundational surfaces touched by Phase 11.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-adaptive-layout-and-interaction-foundations/11-RESEARCH.md
@.planning/phases/11-adaptive-layout-and-interaction-foundations/11-VALIDATION.md
@src/components/SimplePlayDashboard.tsx
@src/components/SimplePlayHome.tsx
@src/components/TitleBar.tsx
@src/components/layout/EmptyStateView.tsx
@src/app/hooks/useAppIcon.ts
@src/components/ui/LazyImage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix bundled asset fallback truth on shipped foundational surfaces</name>
  <files>src/components/SimplePlayDashboard.tsx, src/components/SimplePlayHome.tsx, src/components/TitleBar.tsx, src/components/layout/EmptyStateView.tsx, src/app/hooks/useAppIcon.ts, src/components/ui/LazyImage.tsx, public/icon.png</files>
  <action>Review the foundational surfaces that rely on bundled icon or fallback asset behavior and fix any placeholder-looking, broken, or misleading rendering states. Make the classic dashboard, logo/easter-egg path, titlebar icon handling, and foundational empty-state fallbacks feel intentionally shipped. If the current bundled asset itself is the wrong source of truth, replace it safely and propagate the new expectation through the shared fallback path instead of sprinkling surface-local fixes.</action>
  <verify>npx eslint src/components/SimplePlayDashboard.tsx src/components/SimplePlayHome.tsx src/components/TitleBar.tsx src/components/layout/EmptyStateView.tsx src/app/hooks/useAppIcon.ts src/components/ui/LazyImage.tsx && npx tsc --noEmit</verify>
  <done>FMCL no longer leaks placeholder-feeling bundled assets across the foundational surfaces touched by this phase.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused regression coverage for fallback and classic-surface truth</name>
  <files>src/components/ui/__tests__/LazyImage.cache.test.tsx, src/components/__tests__/SimplePlayHome.visualTruth.test.tsx</files>
  <action>Extend the fallback-image regression seam and add a focused classic-surface truth test so the fixed bundled asset behavior and placeholder cleanup are not lost in later UX passes. Keep the tests narrow and semantically meaningful: they should prove the intended asset or fallback path is used, not assert broad visual styling that belongs to live verification.</action>
  <verify>npx vitest run src/components/ui/__tests__/LazyImage.cache.test.tsx src/components/__tests__/SimplePlayHome.visualTruth.test.tsx</verify>
  <done>The placeholder and fallback cleanup is guarded by targeted tests instead of relying on later manual rediscovery.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/ui/__tests__/LazyImage.cache.test.tsx src/components/__tests__/SimplePlayHome.visualTruth.test.tsx`
- [ ] `npx eslint src/components/SimplePlayDashboard.tsx src/components/SimplePlayHome.tsx src/components/TitleBar.tsx src/components/layout/EmptyStateView.tsx src/app/hooks/useAppIcon.ts src/components/ui/LazyImage.tsx`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Foundational classic and fallback surfaces look intentionally shipped instead of placeholder-driven
</success_criteria>

<output>
After completion, create `.planning/phases/11-adaptive-layout-and-interaction-foundations/11-03-SUMMARY.md`
</output>
