---
phase: 36-settings-predictability-and-shared-control-contract
plan: "09"
type: execute
wave: 9
depends_on:
  - 36-05
  - 36-06
  - 36-07
  - 36-08
files_modified:
  - src/verification/manual/scenarios.tsx
  - src/verification/manual/views.ts
  - src/verification/manual/ManualVerificationApp.tsx
  - src/verification/manual/__tests__/appearanceProof.test.tsx
  - src/verification/manual/__tests__/views.test.ts
autonomous: true
requirements:
  - SETTINGS-05
  - SETTINGS-06
  - SETTINGS-07
  - SETTINGS-08
  - DESIGN-01
user_setup: []
must_haves:
  truths:
    - Manual settings proof does not mark the route ready from static text alone.
    - Proof readiness now depends on behavior or explicit observable checks, not wording-only closeout copy.
    - Closeout framing only appears when the route is genuinely reviewing the same product truths that UAT exercised.
  artifacts:
    - src/verification/manual/scenarios.tsx
    - src/verification/manual/views.ts
    - src/verification/manual/ManualVerificationApp.tsx
    - src/verification/manual/__tests__/appearanceProof.test.tsx
  key_links:
    - `36-UAT.md` -> proof-route honesty gap
    - `src/verification/manual/scenarios.tsx` -> settings ready-state logic
    - `src/verification/manual/__tests__/appearanceProof.test.tsx` -> proof staying green on mocked text only
---

<objective>
Make the manual settings proof honest enough that it cannot close the phase while the live settings surface is still wrong.

Purpose: remove the gap where proof wording claims closure without any behavioral truth behind it.
Output: a behavior-driven settings proof route whose readiness and tests align with the same direct-feedback truths the UAT session checked.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ru/product-feedback-2026-04-20.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-settings-predictability-and-shared-control-contract/36-UAT.md
@src/verification/manual/scenarios.tsx
@src/verification/manual/views.ts
@src/verification/manual/ManualVerificationApp.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace wording-only settings proof readiness with observable behavior checks</name>
  <files>src/verification/manual/scenarios.tsx, src/verification/manual/ManualVerificationApp.tsx</files>
  <action>Stop marking the settings proof route ready from static text alone. Require behavior-oriented checks or explicit observable review items tied to duplicate-copy removal, preset visibility, control cohesion, and visible-effect scope before the route can represent closeout readiness.</action>
  <verify>npx vitest run src/verification/manual/__tests__/appearanceProof.test.tsx && npx eslint src/verification/manual/scenarios.tsx src/verification/manual/ManualVerificationApp.tsx && npx tsc --noEmit</verify>
  <done>Settings proof readiness now depends on product truth rather than static strings.</done>
</task>

<task type="auto">
  <name>Task 2: Reframe proof route copy and tests so closeout cannot pass on wording alone</name>
  <files>src/verification/manual/views.ts, src/verification/manual/__tests__/appearanceProof.test.tsx, src/verification/manual/__tests__/views.test.ts</files>
  <action>Rewrite proof copy and tests so they reflect real behavior-driven closure and fail if the route slips back into text-only closeout framing. Keep the route honest about any remaining manual checks instead of implying success prematurely.</action>
  <verify>npx vitest run src/verification/manual/__tests__/appearanceProof.test.tsx src/verification/manual/__tests__/views.test.ts && npx eslint src/verification/manual/views.ts && npx tsc --noEmit</verify>
  <done>The manual verification harness can no longer imply direct-feedback closure while the live UI still violates the checked behaviors.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/verification/manual/__tests__/appearanceProof.test.tsx src/verification/manual/__tests__/views.test.ts`
- [ ] `npx eslint src/verification/manual/scenarios.tsx src/verification/manual/views.ts src/verification/manual/ManualVerificationApp.tsx`
- [ ] `npx tsc --noEmit`
- [ ] Manual proof review confirms ready-state and wording reflect actual behavior checks rather than static text-only completion
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- The proof harness now blocks false-positive closeout when behavior still disagrees with product truth
</success_criteria>

<output>
After completion, create `.planning/phases/36-settings-predictability-and-shared-control-contract/36-09-SUMMARY.md`
</output>
