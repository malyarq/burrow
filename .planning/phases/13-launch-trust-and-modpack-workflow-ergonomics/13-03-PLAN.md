---
phase: 13-launch-trust-and-modpack-workflow-ergonomics
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - src/components/modpacks/ModpackBrowser.tsx
  - src/features/modpacks/hooks/useModpackNavigation.ts
  - electron/services/mods/platform/modPlatformService.ts
  - src/locales/en.json
  - src/locales/ru.json
  - src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx
  - src/components/modpacks/__tests__/ModpackBrowser.history.test.tsx
autonomous: true
requirements:
  - MPUX-02
user_setup: []
must_haves:
  truths:
    - Remote modpack browsing must become easier to scan and filter on common window sizes without turning into a full browser rewrite.
    - Any provider affordance shown in the browser must stay honest; unavailable provider paths should be clearly non-primary instead of looking feature-complete.
    - The browser must preserve the existing persisted navigation and history contract unless a narrower change is required to support clearer scanning or paging ergonomics.
    - This plan does not own broader launcher parity or enablement work for provider paths that the backend still treats as unavailable.
  artifacts:
    - src/components/modpacks/ModpackBrowser.tsx
    - src/features/modpacks/hooks/useModpackNavigation.ts
    - electron/services/mods/platform/modPlatformService.ts
    - src/locales/en.json
    - src/locales/ru.json
  key_links:
    - Phase 13 research identified the remote browser as functionally correct but still scan-heavy, especially around density, results feedback, and provider honesty.
    - Phase 3 already repaired persisted browser state and Modrinth pagination; execution should build on those foundations instead of reopening them casually.
---

<objective>
Improve remote modpack browsing for scan speed, filter clarity, and adaptive density.

Purpose: make the browser easier to use at real window sizes while keeping provider affordances truthful and preserving the existing persisted navigation model.
Output: clearer results and filter ergonomics, better adaptive density for result browsing, and focused regression coverage for the browser seam.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-launch-trust-and-modpack-workflow-ergonomics/13-RESEARCH.md
@.planning/phases/13-launch-trust-and-modpack-workflow-ergonomics/13-VALIDATION.md
@src/components/modpacks/ModpackBrowser.tsx
@src/features/modpacks/hooks/useModpackNavigation.ts
@electron/services/mods/platform/modPlatformService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine the remote browser scan model and adaptive density</name>
  <files>src/components/modpacks/ModpackBrowser.tsx, src/features/modpacks/hooks/useModpackNavigation.ts, electron/services/mods/platform/modPlatformService.ts, src/locales/en.json, src/locales/ru.json</files>
  <action>Improve the browser so users can understand result state, filter state, and page density faster at common window sizes. Rework the browser card and controls hierarchy as needed to make scanning less linear, keep history or recall ergonomics lighter than a separate detour when possible, and make provider availability messaging honest. Keep the architecture brownfield-safe by reusing the existing navigation and platform-search seams instead of inventing a new browser subsystem.</action>
  <verify>npx eslint src/components/modpacks/ModpackBrowser.tsx src/features/modpacks/hooks/useModpackNavigation.ts electron/services/mods/platform/modPlatformService.ts && npx tsc --noEmit</verify>
  <done>The remote modpack browser becomes easier to scan and filter at real window sizes without regressing persisted navigation or provider truth.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused browser ergonomics coverage</name>
  <files>src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx, src/components/modpacks/__tests__/ModpackBrowser.history.test.tsx</files>
  <action>Add seam-level tests that prove the browser preserves its navigation state while the new scanning, filtering, or density behavior stays honest and usable. Reuse the existing history coverage where possible, extend it for any new state contract introduced here, and keep the suite focused on browser-owned behavior instead of full-route walkthroughs.</action>
  <verify>npx vitest run src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx src/components/modpacks/__tests__/ModpackBrowser.history.test.tsx</verify>
  <done>The remote browser ergonomics work is locked by focused tests instead of living only in manual inspection.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx src/components/modpacks/__tests__/ModpackBrowser.history.test.tsx`
- [ ] `npx eslint src/components/modpacks/ModpackBrowser.tsx src/features/modpacks/hooks/useModpackNavigation.ts electron/services/mods/platform/modPlatformService.ts`
- [ ] `npx tsc --noEmit`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Remote modpack browsing becomes easier to scan and filter on common widths without hiding provider truth or regressing state continuity
</success_criteria>

<output>
After completion, create `.planning/phases/13-launch-trust-and-modpack-workflow-ergonomics/13-03-SUMMARY.md`
</output>
