---
phase: 33-classic-truth-and-catalog-density-repair
plan: "03"
type: execute
wave: 2
depends_on:
  - 33-02
files_modified:
  - src/components/ui/Button.tsx
  - src/components/modpacks/ModpackList.tsx
  - src/components/modpacks/ModpackBrowser.tsx
  - src/components/modpacks/__tests__/ModpackCatalog.density.test.tsx
  - src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx
  - src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx
  - src/components/modpacks/__tests__/CatalogHeaderActions.test.tsx
autonomous: true
requirements:
  - MODPACK-09
  - MODPACK-10
user_setup: []
must_haves:
  truths:
    - The card-density complaint is about both metadata volume and the feeling of busy state indicators, especially on installed cards.
    - Primary catalog actions must read as one contract across list headers, installed cards, and browser cards; matching raw `Button` primitives is not enough if the surrounding geometry still drifts.
    - This plan depends on compact catalog shells landing first so it can tune card/action hierarchy against the final Phase 33 header composition.
    - The plan should move low-value state or explanation out of the cards instead of repainting busy surfaces with slightly different badges.
  artifacts:
    - src/components/ui/Button.tsx
    - src/components/modpacks/ModpackList.tsx
    - src/components/modpacks/ModpackBrowser.tsx
    - src/components/modpacks/__tests__/ModpackCatalog.density.test.tsx
    - src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx
    - src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx
    - src/components/modpacks/__tests__/CatalogHeaderActions.test.tsx
  key_links:
    - `docs/ru/product-feedback-2026-04-20.md` asks for cards that show only the minimum useful summary data and action controls that stop looking like different component families.
    - `33-RESEARCH.md` identifies installed-card heaviness and header/action composition mismatch as the remaining `MODPACK-09` and `MODPACK-10` gap after earlier density cleanup.
    - `33-VALIDATION.md` marks `CatalogHeaderActions.test.tsx` as a Wave 0 proof seam that must exist before action-contract claims can close.
---

<objective>
Reduce card metadata and normalize the primary catalog action contract.

Purpose: keep cards calm enough to scan quickly while making header and card actions share one visible geometry and hierarchy contract.
Output: installed and remote catalog cards with only high-value summary data plus regression coverage for coherent action geometry.
</objective>

<execution_context>
@/Users/kszinikov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/kszinikov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ru/product-feedback-2026-04-20.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-classic-truth-and-catalog-density-repair/33-RESEARCH.md
@.planning/phases/33-classic-truth-and-catalog-density-repair/33-VALIDATION.md
@src/components/ui/Button.tsx
@src/components/modpacks/ModpackList.tsx
@src/components/modpacks/ModpackBrowser.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut installed and remote cards to the minimum high-value summary contract</name>
  <files>src/components/modpacks/ModpackList.tsx, src/components/modpacks/ModpackBrowser.tsx</files>
  <action>Remove or calm card-level metadata, pills, and state fragments that do not help the user decide whether to open details. Keep only the small set of high-value facts such as runtime version and recent update context, and push anything else into details, menus, or secondary states.</action>
  <verify>npx vitest run src/components/modpacks/__tests__/ModpackCatalog.density.test.tsx src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx && npx tsc --noEmit</verify>
  <done>Modpack cards feel calm and comparable instead of busy with low-value summary fragments.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce one explicit catalog action-geometry seam and align header/card primary actions to it</name>
  <files>src/components/ui/Button.tsx, src/components/modpacks/ModpackList.tsx, src/components/modpacks/ModpackBrowser.tsx, src/components/modpacks/__tests__/CatalogHeaderActions.test.tsx</files>
  <action>Define and apply one visible contract for catalog primary-action height, icon scale, padding, and line wrapping across installed header CTAs, installed-card actions, and browser primary actions. Add a dedicated regression seam that fails when those clusters drift apart again.</action>
  <verify>npx vitest run src/components/modpacks/__tests__/CatalogHeaderActions.test.tsx src/components/modpacks/__tests__/ModpackCatalog.density.test.tsx src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx && npx eslint src/components/ui/Button.tsx src/components/modpacks/ModpackList.tsx src/components/modpacks/ModpackBrowser.tsx && npx tsc --noEmit</verify>
  <done>Primary catalog actions read as one intentional family instead of neighboring but unrelated compositions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/components/modpacks/__tests__/CatalogHeaderActions.test.tsx src/components/modpacks/__tests__/ModpackCatalog.density.test.tsx src/components/modpacks/__tests__/ModpackList.ergonomics.test.tsx src/components/modpacks/__tests__/ModpackBrowser.ergonomics.test.tsx`
- [ ] `npx eslint src/components/ui/Button.tsx src/components/modpacks/ModpackList.tsx src/components/modpacks/ModpackBrowser.tsx`
- [ ] `npx tsc --noEmit`
- [ ] Manual card/action check completed: cards show only high-value summary data, and installed header plus card actions feel visually matched to the browser primary actions
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Cards and actions no longer reintroduce catalog heaviness after the shell-density cleanup lands
</success_criteria>

<output>
After completion, create `.planning/phases/33-classic-truth-and-catalog-density-repair/33-03-SUMMARY.md`
</output>
